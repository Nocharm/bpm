# UI 개선 배치 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스펙 `docs/superpowers/specs/2026-07-09-ui-batch2-design.md`의 7항목(①②③④⑤⑦⑧) 구현 — 모달 뷰포트 제한, 목록 가로 스크롤 방지, 권한 기반 정렬/간격, 서브프로세스 노트 축약, 노드 URL 표시 필드·배지.

**Architecture:** 전 항목 프론트 전용(백엔드·스키마 무변경). 항목당 1커밋. ⑦⑧은 캔버스 컴포넌트(`process-node.tsx`)와 필드 상수(`node-actions.ts`)만 건드리고 에디터 본체(page.tsx)는 체크박스 라벨 1곳만 수정.

**Tech Stack:** Next.js + Tailwind 토큰 클래스 + lucide-react + @xyflow/react. 검증: vitest 147(무회귀) + `npm run lint` + Playwright(playwright-core+시스템 Chrome) 스크린샷.

## Global Constraints

- Raw hex 금지 — 토큰 클래스만 (`rules/frontend/design.md`). 이모지 금지, 아이콘은 Lucide strokeWidth 1.5.
- 커밋: `type(scope): English — 한국어` + **각 커밋에 PROGRESS.md 갱신 동봉**(`## 2026-07-09 — UI 개선 배치 2 설계` 섹션에 불릿 추가) (`rules/common/git.md`).
- 컴포넌트 테스트 하네스 없음 — 신규 테스트 파일 금지. 검증 = vitest 무회귀 + lint 0에러(기존 경고 1건 무시) + 스크린샷 육안.
- React Compiler: 새 핸들러는 plain 함수로(수동 useCallback 금지), effect 내 동기 setState 금지 (`frontend/AGENTS.md`).
- 비교뷰 안전: 비교 노드 data에는 `url`/`spUrl`/`commentCount`가 실리지 않음(compare/page.tsx:186-202) — url 게이팅은 `data.url` 존재만으로 충분.
- 검증 명령 (frontend/): `npx vitest run` → `Tests  147 passed`, `npm run lint` → `0 errors, 1 warning`(기존 pw-smoke-task8.mjs 경고).

---

### Task 1: ① 새맵 모달 — dvh 제한 + 내부 숨김 스크롤

**Files:**
- Modify: `frontend/src/components/permissions/create-map-dialog.tsx:309`(컨테이너), `:322-324`(본문 래퍼 열기), `:531-533`(본문 래퍼 닫기)
- Modify: `PROGRESS.md`

**Interfaces:** Consumes 없음 / Produces 없음.

- [ ] **Step 1: 컨테이너에 최대 높이 (line 309)**

```
old: <div className="relative flex w-full max-w-lg flex-col gap-5 rounded-md bg-surface p-6 shadow-lg">
new: <div className="relative flex max-h-[calc(100dvh-2rem)] w-full max-w-lg flex-col gap-5 rounded-md bg-surface p-6 shadow-lg">
```

- [ ] **Step 2: 본문 스크롤 래퍼 열기 — 헤더 블록 닫는 `</div>` 직후(line 322), 사용자 없음 경고 주석 앞에 삽입**

```tsx
        {/* 본문 — 작은 뷰포트에서만 내부 스크롤(스크롤바 숨김), 헤더·버튼 행은 고정 (batch2 ①) */}
        <div className="scrollbar-hidden flex min-h-0 flex-1 flex-col gap-5 overflow-y-auto">
```

- [ ] **Step 3: 래퍼 닫기 — `{/* 오류 / error */}` 주석 바로 앞(line ~531)에 `</div>` 삽입.** 오류 문단과 버튼 행은 래퍼 밖(고정).

- [ ] **Step 4: 검증** — Run (frontend/): `npx vitest run && npm run lint`. Expected: 147 passed, 0 errors. 큰 화면에서 레이아웃 동일(스크롤 미발생) 여부는 Task 8 스크린샷에서 확인.

- [ ] **Step 5: PROGRESS 불릿 추가 + 커밋**

PROGRESS.md 배치 2 섹션에: `- ① 새맵 모달 max-h-[calc(100dvh-2rem)]·본문 scrollbar-hidden 내부 스크롤.`

```bash
git add frontend/src/components/permissions/create-map-dialog.tsx PROGRESS.md
git commit -m "fix(home): cap create-map dialog to viewport with hidden inner scroll — 새맵 모달 뷰포트 제한·숨김 스크롤"
```

---

### Task 2: ② 맵카드 목록 — 가로 스크롤 방지

**Files:**
- Modify: `frontend/src/app/page.tsx:474`(검색 ul), `:486`(브라우즈 컨테이너)
- Modify: `frontend/src/components/maps/map-card.tsx`(재현 시에만 — 아래 Step 2)
- Modify: `PROGRESS.md`

**Interfaces:** 없음.

- [ ] **Step 1: 두 목록 컨테이너에 `overflow-x-hidden`**

```
old(474): <ul className="flex min-h-0 flex-1 flex-col gap-2 overflow-y-auto pr-1">
new(474): <ul className="flex min-h-0 flex-1 flex-col gap-2 overflow-x-hidden overflow-y-auto pr-1">

old(486): <div className="flex min-h-0 flex-1 flex-col gap-3 overflow-y-auto pr-1">
new(486): <div className="flex min-h-0 flex-1 flex-col gap-3 overflow-x-hidden overflow-y-auto pr-1">
```

- [ ] **Step 2: 원인 확인 — 좁은 폭(뷰포트 1280px 이하)에서 긴 맵 이름/호버 스왑 행(`map-card.tsx:195·199`의 `whitespace-nowrap`)이 카드 폭을 밀어내는지 Task 8 검증 환경에서 실측.** 밀어내면 두 nowrap 행의 공통 부모 그리드 셀(`map-card.tsx:187` `<div className="flex min-w-0 items-center gap-2">` 하위의 `grid` 스팬)에 `min-w-0` 추가 — 실측 없이 추가하지 말 것(서지컬 원칙).

- [ ] **Step 3: 검증** — `npx vitest run && npm run lint`. Expected: 147 passed, 0 errors.

- [ ] **Step 4: PROGRESS 불릿 + 커밋**

`- ② 맵카드 목록 overflow-x-hidden(+필요 시 map-card min-w-0) — 가로 스크롤 방지.`

```bash
git add frontend/src/app/page.tsx PROGRESS.md   # map-card.tsx는 수정한 경우에만
git commit -m "fix(home): prevent horizontal scroll in map list — 맵 목록 가로 스크롤 방지"
```

---

### Task 3: ③ 전체맵(브라우즈) — 권한 > 시간 정렬 + 순수 간격

**Files:**
- Modify: `frontend/src/app/page.tsx` — `:1`(React import에 Fragment), `:252` 근처(정렬 파생 추가), `:277`(useInfiniteSlice 소스 교체), `:563-571`(브라우즈 ul 렌더)
- Modify: `PROGRESS.md`

**Interfaces:**
- Consumes: `mapHits`(검색 훅 결과 `{item: MapSummary, matches}[]`), `MapSummary.my_role: string | null`, `MapSummary.updated_at: string`(ISO).
- Produces: `browseHits`(정렬된 동일 타입 배열) — 브라우즈 경로만 사용, 검색(`orderedHits`)·최근 밴드는 무변경.

- [ ] **Step 1: 정렬 파생 추가 — `mapHits` useMemo(≈252) 아래에 삽입**

```tsx
  // 브라우즈 전용 정렬 — 권한(owner→editor→viewer) > 최신 수정순. 검색·최근 밴드는 무변경 (batch2 ③)
  const browseHits = useMemo(() => {
    const rank = (r: string | null) => (r === "owner" ? 0 : r === "editor" ? 1 : 2);
    return [...mapHits].sort((a, b) => {
      const d = rank(a.item.my_role) - rank(b.item.my_role);
      if (d !== 0) return d;
      return b.item.updated_at.localeCompare(a.item.updated_at);
    });
  }, [mapHits]);
```

- [ ] **Step 2: 브라우즈 슬라이스 소스 교체 (line ≈277)**

```
old: } = useInfiniteSlice(mapHits, listKey);
new: } = useInfiniteSlice(browseHits, listKey);
```

(검색 쪽 `useInfiniteSlice(orderedHits, listKey)`는 그대로.)

- [ ] **Step 3: 역할 경계 스페이서 — 브라우즈 ul(≈563) 렌더 교체**

```tsx
old:
                  <ul className="flex flex-col gap-2">
                    {shownBrowseHits.map(({ item: processMap, matches }) =>
                      renderRow(
                        processMap,
                        matches.find((m) => m.field === "name")?.ranges ?? [],
                        undefined,
                      ),
                    )}
                    {hasMoreBrowse && <li ref={browseSentinelRef} className="h-px shrink-0" />}
                  </ul>

new:
                  <ul className="flex flex-col gap-2">
                    {shownBrowseHits.map(({ item: processMap, matches }, i) => (
                      <Fragment key={processMap.id}>
                        {/* 권한 그룹 경계 — 라벨 없는 순수 간격 (batch2 ③) */}
                        {i > 0 && shownBrowseHits[i - 1].item.my_role !== processMap.my_role && (
                          <li aria-hidden className="h-2 shrink-0" />
                        )}
                        {renderRow(
                          processMap,
                          matches.find((m) => m.field === "name")?.ranges ?? [],
                          undefined,
                        )}
                      </Fragment>
                    ))}
                    {hasMoreBrowse && <li ref={browseSentinelRef} className="h-px shrink-0" />}
                  </ul>
```

`Fragment`를 react import에 추가: `import { Fragment, useEffect, ... } from "react";` (기존 import 라인 유지 + Fragment만 추가. `renderRow`가 반환하는 `<li key={...}>`의 key는 Fragment key와 중복이지만 무해 — renderRow는 검색 경로와 공유하므로 건드리지 않는다.)

- [ ] **Step 4: 검증** — `npx vitest run && npm run lint`. Expected: 147 passed, 0 errors(React Compiler 통과 — 파생은 useMemo, 렌더는 순수).

- [ ] **Step 5: PROGRESS 불릿 + 커밋**

`- ③ 브라우즈 전체맵 owner→editor→viewer·updated_at 정렬 + 역할 경계 순수 간격(h-2).`

```bash
git add frontend/src/app/page.tsx PROGRESS.md
git commit -m "feat(home): sort browse list by role then recency with role-gap spacers — 전체맵 권한>시간 정렬·간격"
```

---

### Task 4: ④ 허용된 인원 — 역할 정렬 + 클러스터 간격

**Files:**
- Modify: `frontend/src/components/maps/map-detail-card.tsx` — `:8`(react import에 Fragment), `:42` 근처(ROLE_ORDER 상수), `:374-383`(rows 정렬), `:386`(map 인자), `:488-490`(Fragment 래핑)
- Modify: `PROGRESS.md`

**Interfaces:**
- Consumes: `MapPermission.role: string`("owner"|"editor"|"viewer"), `rows`(타입 그룹별 배열).
- Produces: 없음 (렌더 전용). 홈 우측 패널·에디터 Map 탭 공용 컴포넌트라 양쪽 자동 적용.

- [ ] **Step 1: 상수 추가 — `MEMBER_GROUPS` 정의(line 42) 위에**

```tsx
// 역할 정렬 순위 — 허용 인원 행을 owner→editor→viewer 클러스터로 (batch2 ④)
const ROLE_ORDER: Record<string, number> = { owner: 0, editor: 1, viewer: 2 };
```

- [ ] **Step 2: rows 정렬 교체 (lines 374-383)**

```tsx
old:
                  const rows =
                    g.type === "department"
                      ? [...unsorted].sort(
                          (a, b) =>
                            deptLevelRank(deptLeaf(a.principal_id)) -
                            deptLevelRank(deptLeaf(b.principal_id)),
                        )
                      : unsorted;

new:
                  // 역할 우선(owner→editor→viewer), 같은 역할 안에서 부서는 레벨 순 — sort는 stable이라 그 외는 원순서 유지 (batch2 ④)
                  const rows = [...unsorted].sort((a, b) => {
                    const d = (ROLE_ORDER[a.role] ?? 3) - (ROLE_ORDER[b.role] ?? 3);
                    if (d !== 0) return d;
                    return g.type === "department"
                      ? deptLevelRank(deptLeaf(a.principal_id)) -
                          deptLevelRank(deptLeaf(b.principal_id))
                      : 0;
                  });
```

- [ ] **Step 3: 역할 경계 간격 — `rows.map((perm) => {`(line 386)를 `rows.map((perm, i) => {`로 바꾸고, return의 row `<div key={perm.id}`(line 489-490)를 Fragment로 래핑**

```tsx
old:
                        return (
                          <div
                            key={perm.id}

new:
                        return (
                          <Fragment key={perm.id}>
                            {/* 역할 클러스터 경계 간격 (batch2 ④) */}
                            {i > 0 && rows[i - 1].role !== perm.role && (
                              <div aria-hidden className="h-1.5" />
                            )}
                          <div
```

행 div의 닫는 태그(line ~543 `</div>`) 뒤에 `</Fragment>` 추가. react import에 `Fragment` 추가: `import { Fragment, type ReactNode, useEffect, useState, useSyncExternalStore } from "react";`

- [ ] **Step 4: 검증** — `npx vitest run && npm run lint`. Expected: 147 passed, 0 errors.

- [ ] **Step 5: PROGRESS 불릿 + 커밋**

`- ④ 허용 인원 타입 그룹 내 역할 정렬(owner→editor→viewer)·클러스터 간격(h-1.5) — 홈·인스펙터 공용.`

```bash
git add frontend/src/components/maps/map-detail-card.tsx PROGRESS.md
git commit -m "feat(members): role-clustered member rows with gaps — 허용 인원 역할 정렬·간격"
```

---

### Task 5: ⑤ 서브프로세스 섹션 — 노트 한 줄 축약 + 툴팁

**Files:**
- Modify: `frontend/src/lib/i18n-messages.ts:445`(EN)·`:1693`(KO) + 신규 키 2개
- Modify: `frontend/src/components/subprocess-inspector-card.tsx:130-133`
- Modify: `PROGRESS.md`

**Interfaces:** Produces: i18n 키 `inspector.spNoteFull`(EN/KO) — 카드 툴팁이 소비.

- [ ] **Step 1: i18n — EN(445) 교체 + 아래에 신규 키**

```ts
old: "inspector.spNote": "Designation is how other maps link to this one — a designated map appears in the process library and can be embedded as a subprocess node.",
new: "inspector.spNote": "Designated maps can be embedded in other maps as subprocess nodes.",
     "inspector.spNoteFull": "Designation is how other maps link to this one — a designated map appears in the process library and can be embedded as a subprocess node.",
```

KO(1693) 동일 패턴:

```ts
old: "inspector.spNote": "지정은 다른 맵이 이 맵을 연결(임베드)하기 위한 절차입니다 — 지정된 맵만 프로세스 라이브러리에 노출되어 서브프로세스 노드로 사용할 수 있습니다.",
new: "inspector.spNote": "지정된 맵은 다른 맵에 서브프로세스 노드로 임베드할 수 있습니다.",
     "inspector.spNoteFull": "지정은 다른 맵이 이 맵을 연결(임베드)하기 위한 절차입니다 — 지정된 맵만 프로세스 라이브러리에 노출되어 서브프로세스 노드로 사용할 수 있습니다.",
```

- [ ] **Step 2: 카드 노트에 툴팁 (subprocess-inspector-card.tsx:130)**

```tsx
old: <p className="mb-2 flex items-start gap-1.5 rounded-sm bg-surface px-2 py-1.5 text-fine leading-snug text-ink-tertiary">
new: <p
       title={t("inspector.spNoteFull")}
       className="mb-2 flex items-start gap-1.5 rounded-sm bg-surface px-2 py-1.5 text-fine leading-snug text-ink-tertiary"
     >
```

- [ ] **Step 3: 검증** — `npx vitest run && npm run lint`. Expected: 147 passed, 0 errors.

- [ ] **Step 4: PROGRESS 불릿 + 커밋**

`- ⑤ 서브프로세스 노트 한 줄 축약 + 전체 문구 툴팁(spNoteFull) — 속성탭·Map 탭 공용.`

```bash
git add frontend/src/lib/i18n-messages.ts frontend/src/components/subprocess-inspector-card.tsx PROGRESS.md
git commit -m "feat(inspector): condense subprocess note to one line with tooltip — 서브프로세스 노트 축약·툴팁"
```

---

### Task 6: ⑦ 노드 표시 정보 — 타입 대신 URL(라벨/LINK)

**Files:**
- Modify: `frontend/src/lib/node-actions.ts:7-20`
- Modify: `frontend/src/components/process-node.tsx` — `:5-19`(lucide import), `:38-44`(FIELD_ICON), `:46-53`(NODE_TYPE_LABEL_KEY 제거), `:57-95`(NodeFields)
- Modify: `frontend/src/app/maps/[mapId]/page.tsx:7647-7656`(체크박스 labelKey)
- Modify: `PROGRESS.md`

**Interfaces:**
- Consumes: `AppNode["data"]`의 `url?: string`·`urlLabel?: string`·`spUrl?: string|null`·`spUrlLabel?: string|null`(`lib/canvas.ts:24-26·62-63`), i18n 키 `field.url`(기존).
- Produces: `NodeDisplayField`에 `"url"`(Task 7이 아님 — Task 7은 배지로 이 타입과 무관), `"nodeType"` 제거. localStorage 위생은 기존 hydration 필터(`NODE_DISPLAY_FIELDS.includes`, page.tsx:5473-5486)가 자동 처리 — 추가 코드 불요.

- [ ] **Step 1: node-actions.ts 타입·상수 교체**

```ts
old:
export type NodeDisplayField =
  | "assignee"
  | "department"
  | "system"
  | "duration"
  | "nodeType";

export const NODE_DISPLAY_FIELDS: NodeDisplayField[] = [
  "assignee",
  "department",
  "system",
  "duration",
  "nodeType",
];

new:
export type NodeDisplayField =
  | "assignee"
  | "department"
  | "system"
  | "duration"
  | "url";

export const NODE_DISPLAY_FIELDS: NodeDisplayField[] = [
  "assignee",
  "department",
  "system",
  "duration",
  "url",
];
```

(주석 "BPM 속성 + 유형" → "BPM 속성 + URL"로 갱신.)

- [ ] **Step 2: process-node.tsx — lucide import에서 `Tag` 제거, `Link as LinkIcon` 추가** (알파벳 순: `Clock` 다음쯤 `Link as LinkIcon`, `Tag,` 줄 삭제). `MessageKey` 타입 import는 Step 3 이후 미사용이면 함께 제거.

- [ ] **Step 3: FIELD_ICON·NODE_TYPE_LABEL_KEY 교체 (38-53)**

```tsx
old:
const FIELD_ICON: Record<NodeDisplayField, LucideIcon> = {
  assignee: User,
  department: Building2,
  system: Server,
  duration: Clock,
  nodeType: Tag,
};

const NODE_TYPE_LABEL_KEY: Record<ProcessNodeType, MessageKey> = { ... };  // 블록 전체 삭제(다른 사용처 없음 — grep로 확인)

new:
const FIELD_ICON: Record<NodeDisplayField, LucideIcon> = {
  assignee: User,
  department: Building2,
  system: Server,
  duration: Clock,
  url: LinkIcon,
};
```

삭제 전 `grep -n "NODE_TYPE_LABEL_KEY" components/process-node.tsx`로 사용처가 NodeFields뿐인지 확인. `ProcessNodeType` import가 다른 곳(타입 시그니처)에서 쓰이면 유지.

- [ ] **Step 4: NodeFields 값 로직 교체 (57-95)**

```tsx
old(발췌):
  const spValues: Record<Exclude<NodeDisplayField, "nodeType">, string | null | undefined> = {
    assignee: data.spAssignee,
    department: data.spDepartment,
    system: data.spSystem,
    duration: data.spDuration,
  };
  ...
        if (field !== "nodeType" && !hasBpmAttributes(data.nodeType) && !isSubprocess) {
          return null;
        }
        const value =
          field === "nodeType"
            ? t(NODE_TYPE_LABEL_KEY[data.nodeType])
            : isSubprocess
              ? spValues[field]
              : data[field];

new:
  const spValues: Record<NodeDisplayField, string | null | undefined> = {
    assignee: data.spAssignee,
    department: data.spDepartment,
    system: data.spSystem,
    duration: data.spDuration,
    url: data.spUrl,
  };
  ...
        // BPM 속성 줄은 process·decision(+지정 subprocess)만 — url도 동일 규칙 (batch2 ⑦)
        if (!hasBpmAttributes(data.nodeType) && !isSubprocess) {
          return null;
        }
        const raw = isSubprocess ? spValues[field] : data[field];
        // url — 라벨 있으면 라벨만, 없으면 고정 텍스트 LINK(원문 미노출) (batch2 ⑦)
        const urlLabel = isSubprocess ? data.spUrlLabel : data.urlLabel;
        const value = field === "url" ? (raw ? urlLabel || "LINK" : null) : raw;
```

`const { t } = useI18n();`가 NodeFields에서 미사용이 되면 해당 줄 제거(파일 상단 useI18n import는 다른 컴포넌트가 사용 — 유지).

- [ ] **Step 5: 에디터 체크박스 라벨 (page.tsx:7647-7656) — 마지막 폴백 교체**

```
old:                                   : "field.type";
new:                                   : "field.url";
```

(i18n `field.url` = EN "URL"/KO "URL" 기존재 — i18n 무변경.)

- [ ] **Step 6: 검증** — `npx vitest run && npm run lint`. Expected: 147 passed, 0 errors(미사용 import 잔존 시 lint가 잡음 — 제거).

- [ ] **Step 7: PROGRESS 불릿 + 커밋**

`- ⑦ 노드 표시 필드 nodeType→url(라벨 있으면 라벨, 없으면 LINK) — localStorage 위생은 기존 hydration 필터가 처리.`

```bash
git add frontend/src/lib/node-actions.ts frontend/src/components/process-node.tsx "frontend/src/app/maps/[mapId]/page.tsx" PROGRESS.md
git commit -m "feat(canvas): node display field url replaces type, label-or-LINK — 노드 표시 타입→URL"
```

---

### Task 7: ⑧ 노드 URL 배지 — 좌하단 표시 전용

**Files:**
- Modify: `frontend/src/components/process-node.tsx` — UrlBadge 신설(UnresolvedCommentBadge 아래, ≈275) + 3개 렌더 사이트(≈426 subprocess, ≈469, ≈506)
- Modify: `PROGRESS.md`

**Interfaces:**
- Consumes: `data.url`(일반 노드)·`data.spUrl`(subprocess), Task 6의 `LinkIcon` import 재사용.
- Produces: `UrlBadge({ url: string })` — 파일 내부 전용.

- [ ] **Step 1: 컴포넌트 추가 — UnresolvedCommentBadge 함수 아래**

```tsx
// URL 배지 — url 지정 노드 좌하단 표시 전용(반투명 액센트, 클릭 없음). 좌상단은 코멘트 배지,
// 우상·우하단은 경고/펼침이 사용 — 좌하단이 에디터·비교뷰 통틀어 유일하게 빈 모서리.
// 비교뷰는 노드 data에 url 미탑재(compare/page.tsx buildNodes)라 자동 미표시. (batch2 ⑧)
function UrlBadge({ url }: { url: string }) {
  return (
    <span
      data-id="node-url-badge"
      className="absolute -bottom-2 -left-2 rounded-xs border border-accent-tint-border bg-accent-tint/80 p-0.5 text-accent opacity-70"
      title={url}
    >
      <LinkIcon size={12} strokeWidth={1.5} />
    </span>
  );
}
```

- [ ] **Step 2: 3개 사이트에 마운트 — 각 사이트의 `{commentCount > 0 && <UnresolvedCommentBadge count={commentCount} />}` 줄 바로 아래에 추가**

subprocess 분기(≈426):

```tsx
        {data.spUrl && <UrlBadge url={data.spUrl} />}
```

나머지 두 분기(≈469·≈506):

```tsx
        {data.url && <UrlBadge url={data.url} />}
```

- [ ] **Step 3: 검증** — `npx vitest run && npm run lint`. Expected: 147 passed, 0 errors.

- [ ] **Step 4: 스펙 정정 반영 — `docs/superpowers/specs/2026-07-09-ui-batch2-design.md` ⑧ 절: "좌상단 `-left-2 -top-2`" → "좌하단 `-bottom-2 -left-2`(좌상단은 미해결 코멘트 배지 사용 중 — 사용자 재확인 완료)", 비교뷰 문장은 "비교 노드 data에 url 미탑재로 자동 미표시"로 교체.**

- [ ] **Step 5: PROGRESS 불릿 + 커밋**

`- ⑧ 노드 URL 배지 좌하단 표시 전용(액센트 틴트·툴팁=URL) — 좌상단은 코멘트 배지와 충돌해 위치 정정, 비교뷰 자동 미표시.`

```bash
git add frontend/src/components/process-node.tsx docs/superpowers/specs/2026-07-09-ui-batch2-design.md PROGRESS.md
git commit -m "feat(canvas): translucent url badge at node bottom-left — 노드 URL 배지 좌하단"
```

---

### Task 8: 통합 시각 검증 — :3002 + 스크린샷 + 사용자 게이트

**Files:**
- Create: 스크린샷/시드 스크립트는 scratchpad 전용 — 저장소에 추가 금지.

**Interfaces:** Consumes: Task 1-7 커밋된 워킹 트리.

- [ ] **Step 1: 백엔드 준비 (워크트리 backend/)**

```bash
cd backend
uv venv .venv && uv pip install --python .venv/bin/python -r requirements-dev.txt
.venv/bin/python -m scripts.reset_db
```

- [ ] **Step 2: 검증 데이터 시드 (sqlite, 데모 전용)**

```bash
# 노드 URL 2종 — 라벨 있는 노드·없는 노드 (맵 2 Employee Onboarding의 노드 2개)
sqlite3 dev.db "UPDATE nodes SET url='https://example.com/sop', url_label='Onboarding SOP' WHERE id=(SELECT id FROM nodes WHERE version_id=(SELECT id FROM map_versions WHERE map_id=2 AND status='published') AND node_type='process' LIMIT 1);"
sqlite3 dev.db "UPDATE nodes SET url='https://example.com/raw' WHERE id=(SELECT id FROM nodes WHERE version_id=(SELECT id FROM map_versions WHERE map_id=2 AND status='published') AND node_type='process' AND url='' LIMIT 1);"
# 멤버 역할 클러스터 시연 — 맵 2에 editor 부서 권한 1건 추가(기존 dept는 전부 viewer)
sqlite3 dev.db "INSERT INTO map_permissions (map_id, principal_type, principal_id, role, granted_by, granted_at) VALUES (2,'department','Growth Center/Strategy Office','editor','admin.sys',datetime('now'));"
```

(nodes 테이블 컬럼명은 `.schema nodes`로 먼저 확인 — version_id/node_type 컬럼명이 다르면 맞춰 수정.)

- [ ] **Step 3: 서버 기동 — `lsof -nP -iTCP:8000 -iTCP:3002 -sTCP:LISTEN`으로 포트 확인(점유 시 중단·보고) 후 backend `.venv/bin/uvicorn app.main:app --port 8000`(bg), frontend `npm run dev -- -p 3002`(bg). `curl`로 양쪽 200 확인.**

- [ ] **Step 4: Playwright 스크린샷 5장 (scratchpad 스크립트, `bpm.devUser=admin.sys`·`bpm.lang=en`)**

1. 새맵 모달 — viewport 1280×620(작은 높이)에서 홈 "New map" 버튼 클릭 → 모달 전체가 화면 안(위아래 잘림 없음), 내부 스크롤 동작. 큰 뷰포트(1440×900)에서도 1장(현행 동일 확인).
2. 홈 좌측 목록 — 1440×900 브라우즈 모드: 역할 경계 간격 + 가로 스크롤 없음(`document.querySelector('ul').scrollWidth <= clientWidth` 콘솔 확인).
3. 멤버 카드(에디터 /maps/2 Map 탭) — Teams 그룹에 editor 클러스터·간격.
4. 에디터 캔버스(/maps/2) — url 라벨 노드("Onboarding SOP" 줄)·무라벨 노드("LINK" 줄)·좌하단 배지 2개. 표시 필드 토글에서 URL 켠 상태(토글 UI 클릭 또는 localStorage `bpm.nodeDisplayFields=["assignee","url"]` 주입).
5. Map 탭 서브프로세스 섹션 — 한 줄 노트.
6. 비교뷰(/maps/2/compare?...) 1장 — URL 배지 미표시 확인(가능하면; 비교 진입 파라미터가 번거로우면 생략하고 보고에 명시).

- [ ] **Step 5: 스크린샷 SendUserFile 전송 + :3002 안내 — 사용자 OK까지 머지 진행 금지.**

---

## 완료 후

superpowers:finishing-a-development-branch — main 머지·푸시(ui-improvement-2·3·4 선례), ExitWorktree(remove)·브랜치 삭제. 머지 시 create-map-dialog.tsx가 worktree-ui-improvement-3과 양쪽 수정 상태임을 인지(영역 상이 — 충돌 시 양쪽 보존).
