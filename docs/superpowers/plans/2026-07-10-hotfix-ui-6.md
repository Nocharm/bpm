# Hotfix UI 6 — 구현 계획

설계: `docs/superpowers/specs/2026-07-10-hotfix-ui-6-design.md` · 브랜치 `worktree-hotfix-ui-6`

태스크당 1커밋. 각 커밋에 `PROGRESS.md` 한 줄 + 본 계획의 체크박스 갱신을 함께 담는다 (`rules/common/git.md`).

기준 게이트 (매 커밋 전): `npm run lint` 0에러 · `npx vitest run` 초록. 백엔드 태스크는 `pytest tests/ -q` 추가.

---

## T1 — Back to editor 버튼 ①

- [x] `frontend/src/app/maps/[mapId]/settings/page.tsx`
  - `lucide-react`에서 `ArrowLeft` import 추가
  - 274-279행 링크를 테두리 컴팩트 버튼으로 교체 (`self-start`, `data-id="settings-back-to-editor"`)
  - `←` 문자 제거 → 아이콘 16px/1.5
- 227행 no-access 링크는 **손대지 않음**

**verify** `npm run lint` · 스크린샷은 T7 통합 검증에서

---

## T2 — 피커 드롭다운 portal + fixed ②(a)

- [x] `frontend/src/components/permissions/principal-picker.tsx`
  - `scrollIntoView` `useEffect` 제거 (line 100-103)
  - 입력 래퍼에 `inputBoxRef` 부착
  - 배치 계산은 `src/lib/dropdown-placement.ts`의 순수 함수 `computeDropdownPlacement`로 분리
  - 드롭다운을 `createPortal(…, document.body)` + `position:fixed` + `z-[1250]`
  - `open` 동안 `resize`·`scroll`(capture) 리스너로 좌표 재계산, 언마운트 시 해제
  - `onMouseDown preventDefault` 유지 (blur 방지)
- [x] 단위 테스트 신설 — `src/lib/dropdown-placement.test.ts`
  - 아래 공간 충분 → `below`
  - 아래 부족 + 오른쪽 여유 → `right`
  - 아래 부족 + 오른쪽 부족 + 왼쪽 여유 → `left`
  - 삼면 부족 → `below` + 축소된 maxHeight
  - 옆 배치 시 `top` 뷰포트 clamp

**설계 제약** 위로 flip 금지 — 선택된 pills가 피커 바로 위에 있음

**verify** `npx vitest run src/lib/dropdown-placement.test.ts` 초록 (8케이스)

---

## T3 — 생성 모달 높이 ②(b)

- [x] `frontend/src/components/permissions/create-map-dialog.tsx`
  - `max-h-[calc(100dvh-2rem)]` → `max-h-[calc(100dvh-13rem)]` + 근거 주석 한 줄

**verify** T7에서 1280×580 실측 — 결재자 드롭다운이 `below`로 열리고 잘리지 않음

---

## T4 — 커스텀 breakpoint + 맵 탭 ③(1/2)

- [ ] `frontend/src/app/globals.css` `@theme`에 `--breakpoint-split: 61.25rem;` (980px) + 주석
- [ ] `frontend/src/app/page.tsx` — `xl:hidden`→`split:hidden`, `xl:flex`→`split:flex` (2곳)

**verify** `npm run build` 통과(Tailwind가 `split:` variant 생성) · 940/1100px 스크린샷은 T7

---

## T5 — 공지·인박스 탭 반응형 ③(2/2)

- [ ] `frontend/src/app/notices/page.tsx`
  - 우측 상세 마크업 → `NoticeDetail` 컴포넌트 추출 (같은 파일 하단)
  - 목록 `<li>`에 아코디언 래퍼 추가 (`split:hidden`, `grid-rows-[0fr↔1fr]`)
  - 우측 패널에 `hidden split:flex` 적용
- [ ] `frontend/src/app/inbox/page.tsx`
  - 알림 상세 → `NotificationDetail` 컴포넌트 추출 (승인은 기존 `ApprovalDetail` 재사용)
  - 알림·승인 두 탭의 `<li>`에 아코디언 래퍼
  - 우측 패널에 `hidden split:flex`

**주의** 아코디언 내부 클릭 `stopPropagation` 유지 — 배경 클릭=선택 해제 규약 (batch2 ⑩)

**verify** `npm run lint` · `npm run build`

---

## T6 — 부서 tree JSON 임포트 ④

### T6a — 프론트 파서

- [ ] `frontend/src/lib/dept-info-import.ts` 전면 교체
  - 루트 `{tree: [...]}`, `flat` 무시
  - `enDeptNm`/`deptNm`/`dheadUserId` 추출, `children` 재귀
  - `DEPT_INFO_EXAMPLE`을 tree 포맷으로 갱신
- [ ] `frontend/src/lib/dept-info-import.test.ts` 재작성
  - 중첩 3단(본부→실→팀) 전부 수집
  - `enDeptNm` 없는 래퍼 노드는 건너뛰되 자식은 순회
  - `deptNm`·`dheadUserId` 둘 다 빈 항목 제외 / `enDeptNm` 빈 항목 제외
  - 중복 `enDeptNm` 마지막 승리
  - `deptCd`가 숫자여도 무해 (읽지 않음)
  - 잘못된 루트·`children` 비배열·비문자열 필드 → 에러
  - `DEPT_INFO_EXAMPLE` 자체가 파싱됨
- [ ] `frontend/src/components/admin/dept-info-modal.tsx` 상단 주석 갱신

### T6b — 백엔드 known 확장

- [ ] `backend/app/routers/admin.py` `import_dept_info` — `known`을 `org_l1~org_l5 ∪ department` distinct로
- [ ] `backend/tests/test_dept_info.py` — 상위 레벨(본부/실) 임포트가 `updated`에 잡히고 `unknown`이 비는 케이스 추가

### T6c — 부서장 이름 검색

- [ ] `frontend/src/components/permissions/create-map-dialog.tsx` — `pickerDepts` 빌드 시 `dirUsers`로 `manager`(login_id) → `name`·`korean_name` 해석해 검색 텍스트에 합침
- [ ] 회귀 확인: `principal-picker.tsx:130`의 `manager` 필드가 이미 검색 대상

**verify** `npx vitest run` · `.venv/bin/python -m pytest tests/test_dept_info.py -q`

---

## T7 — 통합 시각 검증

- [ ] 백엔드 `:8000` + 프론트 dev 기동 (좀비 `next dev` 전수 `pkill` 먼저 — `docs/lessons/browser-verification.md`)
- [ ] Playwright 1280×580 — 새 맵 모달 결재자 피커 개방
  - 드롭다운 미클리핑
  - 개방 전후 모달 본문 `scrollTop` 동일 (밀림 0px)
  - 배치 `below` 확인
- [ ] Playwright 940px / 1100px — 홈·공지·인박스 3탭
  - 940 = 아코디언 · 1100 = 좌우 2단
- [ ] 맵 설정 좌측 레일 스크린샷 — 버튼 렌더 확인
- [ ] 스크린샷을 사용자에게 전달

---

## T8 — main 머지

- [ ] 전 게이트 재실행 (lint · vitest · pytest · build)
- [ ] `git merge --no-ff` → main
- [ ] 워크트리·브랜치 정리
- [ ] 메모리 갱신
