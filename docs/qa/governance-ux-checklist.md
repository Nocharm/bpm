# 거버넌스 UX 확장 검증 체크리스트 (feat/governance-ux)

> 설계: [`docs/design/2026-08-08-governance-ux-design.md`](../design/2026-08-08-governance-ux-design.md) · 4페이즈(P0 라이프사이클 대칭화 → C 승인 탭 통합 → B 카드 멤버 편집 → A 게시 동봉) + R4 가시성 UX + R5 Remove 필 폴리시 + R6 인스펙터 재정비.
> 자동 게이트: BE pytest 1042·ruff 0 / FE vitest 620·lint 1 warning·tsc 0·build OK — 이 문서는 **사용자 실사용 검증**용. 항목당 1~2분.
> **검증 이력: 2026-08-14 로컬 전수 브라우저 검증 완료** — Playwright+시스템 Chrome, 결정적 QA 시드(O/E/V 3역할·맵 4종), 전 항목 통과. 이 과정에서 결함 4건 발견·픽스(하단 이슈 기록). UI 자동화가 어려운 일부 백엔드 계약(P0-6·A-3/4/8/10 연쇄)은 API 실호출로 검증 — 항목 비고 참조. 9910 서버 실배포 확인은 별도.

## 준비

- 검증 서버(9910 dev 또는 로컬 네이티브)에서 유저 3역할 준비: **오너 O**(맵 소유), **에디터 E**(editor 그랜트), **승인자 V**(맵 지정 승인자, 비오너). sysadmin(admin.sys)은 오너+승인자 겸용으로 대체 가능하나, 6·10번은 역할 분리가 있어야 의미 있음.
- 비공개(private) 맵 1개(승인자 지정 완료) + 승인자 없는 맵 1개.
- 유저 전환: 우상단 Dev 스위처(로컬) 또는 계정 전환.

```powershell
# 로컬 네이티브(권한 검증 ON) — backend\ 에서
$env:DEV_ENFORCE_PERMISSIONS="true"; $env:BPM_SYSADMINS="admin.sys"; .venv\Scripts\uvicorn app.main:app --port 8000
# frontend\ 에서
npm run dev
```

```bash
# bash 동일 (backend/ 에서)
DEV_ENFORCE_PERMISSIONS=true BPM_SYSADMINS=admin.sys .venv/bin/uvicorn app.main:app --port 8000
```

## P0 — 승인 요청 라이프사이클

- [x] **P0-1 가시성 요청 + 새로고침 복원**: O로 설정→Visibility에서 Public 선택→Apply → "Approval pending" 마커. **새로고침** → 마커·Withdraw 버튼 유지(신규 기능).
- [x] **P0-2 철회 후 재요청**: 마커 옆 **Withdraw request** → 마커 사라짐 → 다시 Apply 가능(409 없음).
- [x] **P0-3 중복 요청 409**: pending 상태에서 다른 탭/유저(O)로 같은 맵 Apply → "already pending" 토스트, 행이 쌓이지 않음.
- [x] **P0-4 승인자 0명 409**: 승인자 없는 맵에서 Apply → "map has no approvers — assign approvers first" 토스트, pending 미생성.
- [x] **P0-5 강등 중복 가드**: E가 협업자 패널에서 다른 editor를 viewer로 강등(승인 대기 배지) → 같은 행 재시도 → "already pending" 토스트.
- [x] **P0-6 직접 적용 supersede**: 위 pending 상태에서 O가 같은 행을 직접 강등 → 즉시 적용 + E에게 supersede 알림(벨) + 결재 대기 목록에서 해당 요청 사라짐. *(검증 비고: 협업자 패널 UI는 pending 행의 select를 잠가(오너 포함) 직접 강등 진입점이 없음 — 계약은 API PATCH로 검증(즉시 적용·superseded·pending 소멸·requester 알림 전부 확인). UI 진입점 부재는 이슈 기록 ⑤ 관찰.)*
- [x] **P0-7 소프트삭제 스윕**: pending(가시성 or 강등) 있는 맵을 휴지통으로 → V 인박스·설정 결재 대기·관리자 콘솔 큐에서 해당 항목 소멸.

## C — 승인 탭 통합 + 배지

- [x] **C-1 4종 통합 목록**: 한 맵에 ①rename 요청(E가 이름 변경 요청) ②SP 등록 요청 ③강등 요청 ④가시성 요청을 만들고 설정→결재 대기 탭에서 4종이 모두 보이는지.
- [x] **C-2 행별 결정권(오너)**: **비승인자 오너 O**로 접속 → 결재 대기 탭이 보이고(신규), rename·SP 행에만 Approve/Reject 버튼, 강등·가시성 행은 "Approvers decide" 읽기전용.
- [x] **C-3 행별 결정권(승인자)**: V로 접속 → 강등·가시성 행에 버튼, rename·SP 행은 "Owner decides".
- [x] **C-4 레일 카운트 배지**: 좌측 레일 "결재 대기" 라벨에 pending 수 배지 → 하나 결정하면 즉시 감소.
- [x] **C-5 top-nav 인박스 배지**: 내 결정 대기 항목이 있으면 상단 Inbox 탭에 숫자 배지, 처리 후 15초 내 갱신/소멸.

## B — 맵 카드 멤버 편집

- [x] **B-1 카드에서 추가**: 홈 맵 상세 카드(또는 에디터 Map 탭)의 멤버 목록 하단 추가 UI(피커+역할)로 유저/부서/그룹 추가 → 즉시 목록 반영. 퍼블릭 맵이면 역할이 editor 고정.
- [x] **B-2 카드에서 제거(즉시/승인)**: 행 호버 시 X 버튼 — viewer 제거는 즉시, **비오너 E가 editor 제거 시** 행 유지 + 역할 배지에 승인 대기 표시 + 결재 대기 탭에 항목 생성.
- [x] **B-3 보호 행**: owner 행·오우닝 부서(잠금) 행에는 X 버튼이 없고, 유저 행 X 클릭이 행 펼침을 유발하지 않는지.
- [x] **B-4 viewer에겐 편집 UI 없음**: viewer(또는 무권한 공개맵 열람자)로 카드 열람 → 추가 UI·X 버튼 미노출.

## A — 게시 전이에 가시성 동봉

- [x] **A-1 에디터 모달 동봉(오너 전용)**: **O**가 체크아웃→편집→승인요청 → 모달에 "Also request visibility change to Public" 체크박스(현재 가시성의 반대 표시) → 체크 후 제출. 결재 대기 탭에 가시성 행이 **"Decided with version approval"** 읽기전용으로 표시되고, V 인박스에는 버전 항목만 뜸.
- [x] **A-1b 비오너에겐 체크박스 없음**: **E**(에디터, 체크아웃 보유)가 승인요청 모달을 열면 동봉 체크박스가 안 보임(가시성 변경은 오너 전용 — 최종 리뷰 픽스).
- [x] **A-2 게시 시 적용**: V(전원) 승인 → 제출자 게시 → 맵이 Public으로 전환(설정 Visibility 확인), private 시절 viewer 그랜트 자동 제거, 요청자에게 적용 알림.
- [x] **A-3 반려 연쇄**: 동봉 제출 → V 반려(사유) → 가시성 요청도 반려(맵 가시성 불변, 결재 대기에서 소멸), 요청자 알림.
- [x] **A-4 회수 연쇄**: 동봉 제출 → 제출자 회수(withdraw) → 가시성 요청도 철회(맵 불변).
- [x] **A-5 직접 결정/철회 차단**: 동봉 pending 상태에서 V의 결재 대기 행은 읽기전용(직접 승인 UI 없음, API 409), 설정 Visibility의 pending 마커/Withdraw에도 동봉 행이 노출되지 않음(버전 회수로만 철회), sysadmin 관리자 큐에도 동봉 행 미노출.
- [x] **A-6 셀프 게시 동봉**: 승인자=본인 1인 맵에서 승인요청 클릭 → 팝오버에 동봉 체크박스 → Yes → 제출·승인·게시 한 번에 + 가시성 적용. **직후 같은 세션에서 재시도 시 체크박스 대상이 갱신된 가시성 기준인지 확인**(stale 픽스 검증).
- [x] **A-7 설정 패널 동봉**: 설정→Versions의 Request approval → 확인 모달+체크박스 동작(에디터 모달과 동일). *(동봉 드롭다운 UI는 R4-2e에서, 제출 체인은 R-1(무동봉 제출→승인→게시)에서 실검증 — 동일 컴포넌트.)*
- [x] **A-8 스테이징 충돌 supersede**: O가 Visibility 스테이징으로 standalone 요청을 걸어둔 상태에서 동봉 제출 → standalone은 superseded + O에게 알림, 동봉 행만 pending.
- [x] **A-9 (엣지) 로드 직후 모달**: 페이지 로드 직후 곧바로 승인요청 모달을 열어도 체크박스는 역할 로드 전엔 숨김이고, 로드 후 대상 가시성이 올바른지(퍼블릭 맵에서 "to Private"). *(로드 직후 열기 실측: 픽커 표시 후 Current=Public·대상 Private 확인. 역할 로드 전 숨김은 canBundle=isOwner 게이트로 보장 — A-1b에서 비오너 미노출 실검증.)*
- [x] **A-10 (엣지) approved 버전 삭제**: 동봉 제출→전원 승인(approved) 상태에서 버전 삭제 → 동봉 가시성 요청이 자동 철회되어, 이후 새 가시성 요청이 정상 동작(중복 409 아님).

## R2 (QA 피드백 반영)

- [x] **R2-1 pending 태그 타 유저 가시성**: E가 다른 editor 강등 요청(pending) → **다른 유저(V 또는 O)**로 협업자 패널·맵 카드 열람 → 해당 행에 승인 대기 태그가 보이고 요청자가 아니어도 구분 가능한지.
- [x] **R2-2 가시성 동봉 pill**: ~~pill 버튼~~ → **R4-2에서 드롭다운으로 대체됨**. 현행 사양(라벨+우측 드롭다운·Current 필·재선택=해제)은 R4-2 a~f에서 3표면 전부 검증.
- [x] **R2-3 승인 모달에 동봉 내용 표시**: 동봉 제출 후 V가 Approve 클릭 → 승인 확인 모달에 동봉 가시성 변경(from→to·요청자)이 표시되는지.
- [x] **R2-4 토스트 문구 인간화**: 중복 요청(409)·승인자 0명 등 에러 시 토스트가 백엔드 detail 원문이 아닌 읽기 쉬운 문구(EN/KO)로 나오는지.
- [x] **R2-5 409 복구 경로**: "already pending" 상태에서 화면에 pending 태그·Withdraw 등 **되돌아갈 UI가 남아 있는지**(에러 후 막다른 화면 없음, 목록 자동 재조회).
- [x] **R2-6 상호 배제(정방향)**: E가 체크아웃 보유 중(또는 pending 제출자) → O가 E의 권한 변경 시도 → 409 + 읽기 쉬운 토스트. 오너 즉시 적용 경로도 동일 차단.
- [x] **R2-7 상호 배제(역방향)**: E에게 pending 강등이 걸린 상태 → E가 체크아웃 시도 → 409. 이미 체크아웃한 드래프트의 승인요청 제출도 409. (승인/반려 액션은 영향 없음.) *(경고는 토스트가 아닌 에디터 상태 라인에 1회 표시 후 폴 중지 — R3-5 설계 그대로. 제출 409는 서버 가드(_find_own_pending_downgrade)로 pytest 커버 — 정방향 배제가 '체크아웃 보유 중 강등 pending 생성' 자체를 막아 UI로는 재현 불가.)*
- [x] **R2-8 스택 저장/취소**: 협업자 패널에서 추가·역할변경·제거를 연달아 조작 → 즉시 적용되지 않고 고스트/취소선 행으로 쌓임 → **Save** 클릭 시 일괄 적용(부분 실패 시 실패 항목만 잔류), **Cancel** 시 전부 원복. 맵 카드에서도 동일.
- [x] **R2-9 맵 탭 기본 펼침**: 에디터 우측 Map 탭 → 협업자 목록이 처음부터 펼쳐져 있는지.
- [x] **R2-10 오너 정렬**: 맵 카드 멤버 목록 — 오우닝 부서 행 바로 아래 **Owner 라벨 섹션**에 오너 개인 태그, 그 아래 나머지 개인/부서/그룹 순인지. 오너 행엔 X 버튼 없음.
- [x] **R2-11 픽커 role 팝오버**: 협업자 추가 — 목록에서 이름 클릭 → 클릭 위치 근처에 [Viewer][Editor] 팝오버(뷰포트 안, 이름 안 가림), 클릭된 행에 하이라이트. 퍼블릭 맵이면 Editor만 보여도 2-step 유지. Esc/바깥 클릭=취소.
- [x] **R2-12 추가 플래시**: 팝오버에서 role 선택 → 방금 적립된 고스트 행이 스크롤로 드러나며 플래시 효과로 표시되는지(페이지 이탈 없음).
- [x] **R2-13 에디터 승인 탭 결재 섹션**: 에디터 > Map > 승인 탭 접이식 "결재 대기" 섹션(기본 접힘, R6에서 최상단으로 이동) — pending 수 배지, 펼치면 설정 화면과 동일한 4종 목록·행별 결정 버튼, 결정 시 워크플로 상태 갱신.
- [x] **R2-14 SP ⓘ 툴팁 + 접힘 영속**: 우측 사이드바 SP 카드 — 안내문이 본문에서 사라지고 제목 옆 ⓘ 호버 툴팁으로 표시. 카드 본문 기본 접힘, 펼침/접힘이 탭 전환·다른 탭에서도 유지(세션 내), 새 세션에선 다시 접힘. Designated 배지는 접힘에도 보임.
- [x] **R2-15 게시본 가기**: 드래프트/구버전을 연 상태에서 SP 카드 펼침 → "게시된 버전을 연 상태~" reason 우측에 게시본 가기 버튼 → 클릭 시 게시본으로 전환. 게시본이 없으면 버튼 미노출.
- [x] **R2-16 등록 요청(팬딩 툴팁)**: 비오너 E로 미등록 맵 SP 카드 → "오너와 관리자만~" reason 우측 등록 요청 버튼 → 클릭 시 요청 생성·버튼이 "등록 요청 중" disabled로 전환. 호버 시 누가·언제 요청했는지 툴팁. 이미 pending이면 처음부터 disabled.

## R3 (후속 정비)

- [x] **R3-1 새 맵 모달 협업자 role 팝오버**: 홈 "New map" → 협업자 추가 섹션에서 이름 클릭 → 클릭 위치 근처에 [Viewer][Editor] 팝오버(뷰포트 안, 이름 안 가림), 선택한 행에 하이라이트 유지. 퍼블릭 지정이면 Editor만 보이되 2-step 유지(이름 클릭 시 Editor 단일 버튼이라도 팝오버가 뜬다 — 원클릭 직행 아님, Editor 클릭 시 행 추가+팝오버 닫힘). Esc/바깥 클릭=취소.
- [x] **R3-2 SP ⓘ 클릭이 접힘 안 바꿈**: 우측 SP 카드 제목 옆 ⓘ 아이콘 클릭 → 접힘 상태가 바뀌지 않고 툴팁만 열렸다 닫힘(접힘·펼침 토글 아님).
- [x] **R3-3 role 원복 시 스택 태그 소거**: 협업자 패널에서 editor → viewer로 변경(태그 추가) → 즉시 viewer → editor로 원복 선택 → 고스트 태그가 행에서 소거되고, Save 클릭 시 해당 행이 결과에서 빠짐(no-op 미집계).
- [x] **R3-4 SP 등록 요청 중복 409**: 비오너 E로 미등록 맵 SP 카드 등록 요청 → pending 상태 → 다시 요청 시도 또는 새로고침 후 재시도 → "already pending" 또는 유사 읽기 쉬운 문구(백엔드 detail 원문 아님), 화면에 pending 상태·Withdraw UI 남음.
- [x] **R3-5 본인 강등 대기 중 드래프트 진입 + 동봉 미집계**: 본인(E)의 강등 요청이 pending 중일 때, 같은 맵 체크아웃 시도 → 경고 안내 메시지 1회 표시 후 진입 허용(반복 에러 없음). 또한 동봉 요청(가시성)의 pending 행이 결재 대기 배지 카운트에 **포함되지 않음**(오너만 동봉 가능하므로 결재자의 작업 아님).

## R4 (가시성 UX)

- [x] **R4-1 승인자 모달 우측 위 가시성 배지**: 에디터 승인 탭에서 승인자 관리 모달 열기 → 헤더 타이틀 옆 우측 위에 현재 맵 가시성(Globe/Lock 아이콘 + "Public"/"Private") 배지 표시. (설정 화면 승인자 카드는 별도 표면 — 배지 없음이 정상.)
- [x] **R4-2 동봉 UI 드롭다운(3표면)**: 에디터 승인요청 모달·설정 Request approval 모달·셀프 게시 팝오버의 동봉 UI → 라벨 "공개 범위"(EN "Visibility") + 우측 드롭다운(Globe/Lock 아이콘+현재값 라벨+ChevronDown) → 클릭 시 메뉴(Private/Public 옵션, 현재 값 옆에 "Current" 필 리터럴) → 옵션 선택 시 적용·메뉴 닫힘. 재클릭 또는 현재값 재선택 시 해제 null.
- [x] **R4-3 인스펙터 맵 탭 가시성 3:1 + 승인자 정보 모달**: 우측 사이드바 Map 탭 가시성 섹션 → Private/Public 버튼 비율 3:1(현재 활성 버튼이 3배 넓음) → O만 비활성 버튼 클릭 가능(E는 정적) → pending 있으면 필 표시 → O가 비활성 버튼 클릭 → ConfirmDialog(타이틀 "공개 범위 변경"·현재→대상 화살표 표시·승인자 이름 필 목록·안내 문구) → 승인자 0명이면 안내 경고 + confirm 비활성 → 확인하면 `requestVisibilityChange` → 성공 시 pending 갱신·모달 닫힘.
- [x] **R4-4 멤버 행 제거 hover 페이드 전환**: 맵 상세 카드(또는 에디터 Map 탭)의 협업자 멤버 행 → 기본 권한 배지(Viewer/Editor) → **행 hover 시** 같은 자리·같은 크기에서 X 아이콘 없이 색·문구만 빨간 Remove 필로 페이드 전환 → 클릭하면 제거 예정 스택 태그가 적립되고(즉시 적용 아님) **Save** 시 반영(오너는 즉시 적용, 에디터의 editor 제거는 승인 대기 배지). 오우닝 부서·오너 행에는 Remove 스왑 없음.
- [x] **R4-5 협업자 패널 X 정렬 교정**: 설정 협업자 패널의 제거 X 버튼 → hover 시만 표시로 전환(공간 비차지 — `absolute opacity-0 group-hover:opacity-100`) → 행 select/badge 우측 정렬이 제거·편집 모두 일치(우측 패딩 공통 부여).

## R5 (Remove 필 폴리시)

- [x] **R5-1 스왑 전후 필 크기·위치 완전 동일**: 맵 상세 카드(또는 인스펙터 Map 탭)의 removable 멤버 행에서 hover/focus로 역할 배지 ↔ Remove 필 전환 시 좌우상하 어느 방향으로도 크기·위치가 변하지 않고(비호버 필과 같은 박스 — 승인 대기 배지 행은 그 배지 폭 그대로) 페이드로만 바뀐다.
- [x] **R5-2 인스펙터 행 단위 hover 스코프**: 에디터 우측 인스펙터 Map 탭(허용 인원 아코디언, `<details className="group">`로 감싼 표면)에서 한 행에만 hover → **그 행만** Remove 필로 전환되고 나머지 행은 그대로(전 행 동시 스왑 없음).
- [x] **R5-3 비호버 시 필 우측 정렬 일치**: 비호버 상태에서 Owner/Editor/Viewer 필이 모두 같은 고정폭으로 우측 정렬이 일치(오너 행만 삐져나오는 등의 어긋남 없음. 오우닝 부서 행의 Editor 필은 별도 잠금 행이라 예외).
- [x] **R5-4 제거 예정 태그의 소속 줄 재배치**: 멤버를 제거 예정으로 적립하면 권한 필은 1행에 그대로 유지되고, 2행(좌측 소속/부서 줄과 같은 높이 대역)에 같은 크기의 빨간 staged 필이 노출된다. 행 hover/focus 시에만 그 우측 취소 X가 페이드 인하며(기본은 공간만 예약), 나타날 때 옆 필이나 레이아웃이 밀리지 않는다.

## R6 (인스펙터 재정비)

- [x] **R6-1 노드 디스플레이 기본 접힘 + 항목 아이콘**: 에디터 우측 Map 탭 → 노드 디스플레이 섹션이 **기본 접힘** 상태로 표시 → 헤더 클릭 시 ChevronRight가 회전하며 accordion 애니로 펼쳐짐 → 각 토글(assignee/department/system/url/params)의 왼쪽에 아이콘(사람/건물/서버/링크/슬라이더, 14px) 표시. (O/E)
- [x] **R6-2 엣지 스타일 기본 접힘 + 아코디언**: 에디터 우측 Map 탭 → 엣지 스타일 섹션이 **기본 접힘** 상태로 표시 → 헤더 클릭 시 ChevronRight 회전+accordion으로 펼침. (O/E)
- [x] **R6-3 버전 선택 행이 맵 탭 최상단**: 에디터 우측 탭 → **Map 탭** 상단에 버전 선택 행(VersionPill + 우측 아이콘 클러스터) 표시 → **Approval 탭**에는 없음. (O/E)
- [x] **R6-4 승인 탭 순서 + 드래프트 CTA 두 상태**: 에디터 우측 Approval 탭 → 순서가 ① 결재 대기 섹션(최상단, 기본 접힘) ② **드래프트 CTA 행** ③ 워크플로(기본 펼침) ④ SP ⑤ 타임라인 순 → **드래프트 有일 때** CTA는 "승인을 위해 드래프트로 전환"(`PencilLine` 아이콘), **無일 때** "승인을 위해 드래프트 생성"(`Plus` 아이콘) → **현재가 draft 상태**이면 CTA 미노출. (O/E)
- [x] **R6-5 체크아웃 UI draft 전용**: 워크플로 섹션 → Checkout 버튼이 **draft 상태에서만 표시**, rejected 상태에는 미표시. (O/E)
- [x] **R6-6 협업자 개인 목록 3.3행 클램프 + 전체 펼치기**: 맵 상세 카드(또는 인스펙터 Map 탭) → 멤버 목록의 **개인(user) 그룹**이 **3.3행으로 클램프**, 4명 이상일 때 하단에 "전체 펼치기 (N)" 버튼 노출 → 클릭 시 클램프 해제 + accordion 확장 → 버튼이 "접기"로 토글 → 재클릭 시 원상복귀(부서/그룹 그룹·오너 섹션은 클램프 적용 안 함). (O/E/V)
- [x] **R6-7 SP 카드 버튼 행 재배치 + 아이콘**: 우측 SP 카드 → 액션 버튼이 **한 행**에 통합, 좌측에 "Designate as subprocess"(`Workflow` 아이콘, 14px), 우측 정렬으로 ① kind==="needPublished"면 "게시본 가기"(`ArrowRight` 아이콘) 또는 ② kind==="ownerOnly"&&!designated이면 "등록 요청"(`BadgeCheck` 아이콘) → canManage=false인 경우에도 좌측 지정(또는 Edit/Undesignate) 버튼이 disabled 상태로 항상 렌더(행 자체가 앵커) → 그 **아래에는 reason 노트만 표시**(버튼 없음). (O/E/V)

## 회귀 스팟 (기존 기능 무손상 확인)

- [x] **R-1**: 동봉 없이 일반 승인요청→승인→게시가 종전과 동일하게 동작.
- [x] **R-2**: 협업자 패널(설정)의 추가/강등/제거·승인 대기 배지가 종전과 동일. *(P0-5·R2-8·R3-3·R4-5 개별 검증으로 커버.)*
- [x] **R-3**: rename 요청/철회·SP 등록 요청 플로우 종전과 동일(전용 철회 경로 유지).
- [x] **R-4**: 관리자 콘솔 Approval Queue(sysadmin)가 정상 표시(소프트삭제 맵 제외).

## 이슈 기록

| 번호 | 항목 | 증상 | 비고 |
|------|------|------|------|
| ① | R5-1 | hover 스왑 시 Remove 필이 역할 필보다 상하 6px 큼 — 오버레이 `inset-0`이 래퍼 라인박스(24px)를 통째로 덮음 | **픽스 완료** — 버튼은 투명 히트영역, 실제 필은 배지 동일 지오메트리 내부 span (`map-detail-card.tsx`). 실측 60×18 일치 |
| ② | C-3 | 설정 결재 대기 탭이 **실제 승인자(비오너)에게 미노출** — 게이트가 mock permState의 `isApprover`를 참조(실서버 승인자 미인지) | **픽스 완료** — `listApprovers` 실서버 목록으로 게이트 교체 (`settings/page.tsx`) |
| ③ | C-5 | 인박스 배지 첫 폴링이 X-Dev-User 미주입(기본 유저 명의)으로 나가 최초 15초간 미표시/오표시 | **픽스 완료** — `api.ts` devUser 모듈 변수를 localStorage에서 부트 시드(폴러 레이스 제거) |
| ④ | R2-13/R6 | 비권한자(일반 에디터·뷰어)가 에디터 승인 탭을 열면 `approval-requests` 403 **무한 재요청 루프** + 원문 detail 토스트(초당 수십 회) | **픽스 완료** — `pending-approvals-panel.tsx` 조회를 오너/승인자로 게이트 + 초기 로드 실패 무토스트(벨 폴링 선례) |
| ⑤ | P0-6 | (관찰) 협업자 패널이 pending 행 select를 잠가 오너 '직접 강등' UI 진입점이 없음 — 백엔드 supersede 계약은 유효 | 후속 판단 대기 — 상호 배제 정비(R2) 이후 의도된 잠금일 수 있음. 필요 시 카드 Remove(제거) 경로가 대안 |
