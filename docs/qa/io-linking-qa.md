# IO 연결(불러오기) 브라우저 QA 검수 — 2026-08-21

`feat/io-linking` 전면 브라우저 검수. 실행 환경: 로컬 네이티브(backend :8000 + frontend :3000) + Playwright/시스템 Chrome, `admin.sys`. 스펙: `docs/superpowers/specs/2026-08-21-io-linking-design.md`.

표기: ✅ 통과 · ❌ 결함(하단 이슈 절에 기록) · ➖ 해당 없음/차단됨(사유 기록) · (스모크) = pw-smoke-io-links.mjs 26체크가 이미 자동 검증한 항목(재확인은 시각 위주).

## A. 에드 버튼 · 메뉴

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| A1 | 인풋/아웃풋 섹션 비호버 시 + 버튼 숨김, 섹션(헤더·목록) 호버 시 표시 | | |
| A2 | + 클릭 → 2항목 메뉴(Add new / Import from node…), 바깥 mousedown·Esc로 닫힘 | | |
| A3 | Add new → 기존처럼 빈 행 추가·편집 정상 | | |
| A4 | 카드 draft dirty 상태에서 Import 항목 비활성 + 사유 노출(툴팁 신뢰성 — 최종 리뷰 지적) | | |
| A5 | 읽기전용(뷰어/비체크아웃)에선 + 버튼 자체 미노출 | | |

## B. 불러오기 모달

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| B1 | (스모크) 인풋 쪽: 업스트림 노드의 아웃풋 항목이 노드별 그룹핑으로 표시, 파일타입 필 동반 | | |
| B2 | 아웃풋 쪽: 다운스트림 인풋 항목 표시 | | |
| B3 | 기본 2홉 표시 → 3홉+ 후보 존재 시 Show more 행 → 클릭 시 흐름 전체 확장 | | |
| B4 | 텍스트 필터: 항목 텍스트·노드명 부분일치, 필터가 심층 후보만 남기면 Show more 유지(막다른 상태 없음) | | |
| B5 | (스모크) 행 호버 → 대상 노드 링(io-node-highlight) + 경로 엣지 하이라이트, 이탈 시 해제 | | |
| B6 | 이미 링크된 항목에 Link 배지, SP 항목에 SP 배지 | | |
| B7 | 자기 노드·이미 연결한 그룹·id 없는 레거시 SP 항목은 후보에서 제외 | | |
| B8 | 후보 0개면 빈 상태 문구, Esc/백드롭/X/Cancel 닫힘 + 하이라이트 잔존 없음 | | |
| B9 | 뷰포트 가장자리에서 모달 클램프(화면 밖 미이탈) | | |

## C. 불러오기 실행 — 4시나리오 + SP

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| C1 | (스모크) 미러 생성: 인풋이 일반 아웃풋 불러오기 → 미러 행 생성, 원본에 id 부여, 토스트, 자동저장·리로드 영속 | | |
| C2 | 소유권 인수: 아웃풋이 일반 인풋 불러오기 → 아웃풋 원본 행 추가, 대상 인풋이 미러로 전환(텍스트 불변), 토스트 | | |
| C3 | 원본 승계: 상류 아웃풋이 미러 인풋 불러오기 → 편집점 이동(구 원본 미러 강등·기존 미러 유지), 토스트 | | |
| C4 | 그룹 합류: 병렬 아웃풋이 미러 인풋 불러오기 → 미러로 합류, 기존 편집점 유지, 토스트 | | |
| C5 | SP: 지정된 SP의 아웃풋을 인풋으로 불러오기 → 미러 생성, SP 쪽 영구 원본(양쪽 수정 불가) | | |
| C6 | 순환(A⇄B) 구성에서 승계 없이 합류로 처리 | | |

## D. 미러 행 표시·해제

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| D1 | (스모크) 미러 행: 번호 대신 Link 아이콘, 텍스트 잠금, 폼 피커 대신 정적 폼 표시 | | |
| D2 | 아이콘 행 호버 시 Link→Unlink 스왑 + 툴팁 | | |
| D3 | (스모크) Unlink 클릭 → 마우스 근처 확인 팝오버(클램프) → Disconnect 시 일반 행 전환(텍스트·폼 복사 유지) | | |
| D4 | (스모크) 해제 후 Save 전 노드 재선택 → 링크 원복(draft 취소) | | |
| D5 | (스모크) 해제 + Save → 리로드 후에도 해제 영속, 원본은 배지 소멸 | | |
| D6 | 팝오버 Cancel/Esc → 링크 유지 | | |
| D7 | 미러 행 × 삭제 → 항목 제거, 같은 그룹 다른 미러·원본 무영향 | | |
| D8 | 미러 텍스트 클릭 → 원본 노드로 센터링+선택+인스펙터 포커스 | | |

## E. 전파 · 정합화

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| E1 | (스모크) 원본 텍스트 수정+Save → 모든 미러(복수 노드·인풋/아웃풋 혼재) 즉시 갱신 | | |
| E2 | 원본 파일타입 변경 → 미러 폼 동반 갱신 | | |
| E3 | 원본 항목 삭제+Save → 미러 자동 해제(복사본 전환, 텍스트 유지) | | |
| E4 | 원본 노드 삭제 → 같은 세션에서 미러 즉시 일반 행 전환(최종 픽스 검증) | | |
| E5 | 노드 편집 모달 경유 원본 수정도 전파(patchNode 경로) | | |
| E6 | 일괄편집: IO append는 링크 보존, 교체/비우기는 해당 측 링크 소거(미러 오염 없음) | | |
| E7 | 드리프트 시드(콘솔/DB로 미러 텍스트 어긋남 주입) 후 리로드 → 정합화 치유 + 저장 | | |

## F. 인스펙터 읽기모드 · 하이라이트

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| F1 | 읽기모드 연결 항목(원본·미러 양쪽)에 Link 아이콘 | | |
| F2 | 미러 항목 호버 → 원본 노드+경로 엣지 하이라이트 / 원본 항목 호버 → 모든 미러 노드 하이라이트 | | |
| F3 | 엣지 삭제된 링크: 호버 시 노드만 하이라이트, 링크·전파는 유지 | | |
| F4 | 하이라이트가 dev 서버에서 실표시(io-node-highlight raw style 주입 — Turbopack purge 회귀 없음) | | |
| F5 | 호버 이탈·노드 전환 시 하이라이트 잔존 없음(이연 항목 ⑩ 실측) | | |

## G. 필수/선택 플래그

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| G1 | 인풋 행 필 표시: 기본 Required(액센트 틴트), 토글 → Optional(뮤트) | | |
| G2 | 미러 인풋에서도 플래그 토글 가능(로컬), 원본 수정에 안 덮임 | | |
| G3 | 읽기모드: Optional만 `· Optional` 접미 표시, Required는 무접미 | | |
| G4 | 아웃풋 행·SP 카드엔 플래그 미노출 | | |

## H. SP · 지정 연동

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| H1 | SP 카드(읽기모드) 연결 항목에 Link 아이콘 + 호버 하이라이트 | | |
| H2 | SP 지정 저장 → sp 항목 id 부여, 텍스트 유지 재저장 시 id 보존(소비 맵 미러 유지) | | |
| H3 | SP 지정 항목 개명 후 소비 맵 리로드 → 해당 미러 보수적 해산(복사본 전환) | | |
| H4 | 미지정 SP는 후보 제외 | | |

## I. 엣지케이스 · 회귀

| # | 항목 | 결과 | 비고 |
|---|---|---|---|
| I1 | 원본 노드 Ctrl+드래그 복사/클립보드 붙여넣기 → 사본 output_ids 소거(그룹 무오염), 미러 복사는 미러 유지 | | |
| I2 | CSV 임포트로 원본 텍스트 변경 → 리로드 시 그룹 보수적 해산·텍스트 보존 | | |
| I3 | 링크 있는 맵 버전 생성(clone) → 새 버전에서 그룹 관계 유지 | | |
| I4 | 링크 없는 기존 맵·SP 지정 모달 등 기존 IO 편집 회귀 없음(MVI 공용 표면) | | |
| I5 | 뷰어(권한 없음)로 드리프트 맵 열람 → 자동 PUT 미발사(403·에러 배너 없음), 아이콘·호버만 동작 | | |
| I6 | 콘솔 에러 0 (전 시나리오 통과 중) | | |

## 이슈 로그

(발견 시 기록: 번호 · 항목 · 증상 · 심각도 · 조치)

## 결과 요약

(검수 완료 후 기입)
