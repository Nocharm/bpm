# height-shift 워스트 케이스 브라우저 QA (feat/node-spacing, HEAD f42fd8fb)

M3 겹침 재발 방지를 목적으로, 표시 높이로 커진 노드가 아래 행을 밀어내는 height-shift를 워스트 케이스 맵(긴 라벨·조건 2종·담당 3인·시스템·7파라미터·Input 10줄/Output 6줄)에서 실기동 검수했다.
환경: 격리 sqlite DB + backend 8000(`AI_ENABLED=false DEV_ENFORCE_PERMISSIONS=false`) + frontend 3000 네이티브, 시스템 Chrome(playwright-core, 1800×2200), devUser=admin.sys, 노드 표시 토글 7종 전부 ON. **결과 29/29 PASS.**

시드: `W(0,0)` 워스트 노드 · `N(0,280)` · `P(400,280)`(N과 같은 행) · `Q(0,560)` · `S(800,0)` 하위프로세스(게시+지정된 별도 맵 링크) · `Start(-300,-200)`, 엣지 `W→N`·`N→Q`.

| # | 항목 | 측정치 | 판정 | 증적 |
|---|------|--------|------|------|
| T1 | 캡 상태 겹침 0 | W 표시높이 451px(y=0, bottom=451) · N 표시 y=679(저장 280 + 밀림 399) · 간격 228px | ✅ | `T01-worst-capped.png` |
| T2 | 전체 펼침 겹침 0 | W 451→613px(+162) · 밀림 델타 N/P/Q = 162.0/162.0/162.0 · W bottom 613 < N top 841(간격 228) | ✅ 행 보존(N=P), Q ≥ 동일 델타 | `T02-worst-expanded.png` |
| T3 | 트윈 | N translateY 3시점 731.9 → 782.7 → 794.2(679→796 구간, 엄격 증가·역행 프레임 0) · `W→N` 엣지 끝점 − N y = 19/19/19(편차 0) | ✅ | `T03-tween-mid.png` (캡처 시점 y=767.5, 정착값 796) |
| T4 | 그룹 오버레이 | 그룹 박스 y=-50 h=953 → bottom 903 ≥ 밀려난 N bottom 879 | ✅ 두 멤버 모두 포함 | `T04-group-overlay.png` |
| T5 | PNG 내보내기 | 표시 bounds 1280×1359 → PNG 2760×2918(pixelRatio 2). 저장좌표 기준이었다면 2760×**1796**(하단 잘림) | ✅ Q 포함, 잘린 노드 없음 | `T05-export.png` |
| T6 | 드래그/스왑 무오염 | 드래그: 표시 y 936 − 활성 오프셋 561 = 저장 375(GET 375, 누출 시그니처 936 아님). 스왑: N(400,**280**)·P(0,**375**)로 저장값만 교환(누출 시그니처 841 아님). 새로고침 2회 드리프트 0 | ✅ | — |
| T7 | 인라인 펼침 상호 배제 | W 펼친 상태 N y=841 → S 인라인 펼침 중 N y=**280**(밀림 해제, steps 없음, 자식 3노드 합성 렌더) → 접으면 841 복귀 | ✅ | `T07-inline-expanded.png` |
| T8 | 콘솔 0 · 잔류 0 | 페이즈별(T1~T7) console error 0, 런 전체 0. teardown: 소프트삭제 + `_purge-test-map.py` 하드퍼지(맵 2건) + map_id 스코프 알림 3건 정리 → 전 테이블 행수 baseline 일치 | ✅ | — |

증적 스크린샷은 세션 스크래치패드(`…/scratchpad/qa-shots/`)에 보관 — 저장소에는 커밋하지 않는다.

## 이슈 로그

| # | 내용 | 판단 |
|---|------|------|
| 1 | 드래그 +100 flow px 지시에 저장값이 375(=+95)로 착지 | **하네스 아티팩트**. React Flow 드래그 임계값을 넘기려 넣는 선이동 3px(≈5 flow px @zoom 0.6)만큼 델타가 줄어든 것. 불변식(저장 = 표시 − 오프셋)은 정확히 성립하므로 제품 이슈 아님 — 검증도 절대값 대신 이 불변식으로 판정하도록 바꿔 측정했다. |
| 2 | 인스펙터 PNG 버튼은 Map 탭에서만 렌더 | 검수 절차 메모(노드 선택 해제 + Map 탭 전환 필요). 제품 동작상 정상. |

---

# U라운드 — 엣지 장애물 회피 (4c6ce621)

꺾은선(smoothstep) 엣지가 제3 노드의 표시 bbox(팽창 12px)를 관통할 때 빈 회랑으로 우회하는 `lib/edge-detour.ts`를 실기동 검수했다.
환경: 격리 sqlite DB + backend 8000(`AI_ENABLED=false DEV_ENFORCE_PERMISSIONS=false`) + frontend 3000 네이티브, 시스템 Chrome(playwright-core, 1700×1400, zoom 0.75), devUser=admin.sys, 노드 표시 토글 assignee/params/input/output. **결과 13/13 PASS (U1~U6 전항 ✅).**

시드: `A(0,0)` · `W(0,220)`(Input 10줄) · `B(0,760)` · `D(500,0)` · `Start(-700,-400)`, 엣지 `A→B`·`A→D` 둘 다 `line_style=smoothstep`(s-right → t-left).
측정은 전부 **flow(표시) 좌표계** — 노드는 `.react-flow__node` transform + `offsetWidth/Height`, 엣지는 path의 `d` 파싱(라운드 코너의 `Q` 제어점 = 원래 직각 꼭짓점) + `getPointAtLength` 61점 샘플링. `getBoundingClientRect`는 줌 함정이라 미사용.

| # | 항목 | 측정치 | 판정 | 증적 |
|---|------|--------|------|------|
| U1 | 우회 발생 | W 표시 rect `x[0..150] y[220..366]`. `A→B` = 수직 런 **1개**, 회랑 x=**163** = W.right(150)+MARGIN 12+1 → 팽창 rect 밖. 경로 61점 중 W bbox 내부 **0점**. 꼭짓점 `(154.5,19)→(163,19)→(163,873)→(-4.5,873)` | ✅ | `U01-detour.png` |
| U2 | 무관 엣지 불변 | `A→D` d=`M154.5 19 L174.5 19 L325 19 L325 19 L475.5 19 L495.5 19` — RF 기본 smoothstep 6점 구조 그대로(전부 y=19로 퇴화). x[154.5..495.5] ⊂ 직결 박스[144..506], y[19..19] ⊂ [13..25]. 이탈 웨이포인트 0 | ✅ | — |
| U3 | 성장 추종 | W Show more → 높이 146→**263**(+117), B 표시 y 854→971. 재계산된 `A→B` 회랑 여전히 x=163(수직 런 1개, len 854→**971**로 동행 신장), 커진 W rect 내부 점 **0** | ✅ | `U03-detour-expanded.png` |
| U4 | 라이브 재계산 | W를 +300 드래그(x 0→296) → 리로드 없이 수직 런 **1→2개** `[174.5, -24.5]` = RF 기본 서명(sourceX+20 / targetX−20) + 중간 y=446 수평 런 199px → **우회 해제**. 되돌리면(x=−8) 다시 수직 런 1개·회랑 x=**155**(=−8+150+12+1)·내부 점 0으로 **우회 복귀** | ✅ | — |
| U5 | 폴백 무회귀 | 인스펙터 Line style → 직선 전환. d=`M 154.5,19 L -4.5,873`(2점 직선, 웨이포인트 0) — 61점 중 **10점이 W 내부**를 그대로 통과. 직선 경로에 회피 미적용 확인 | ✅ | `U05-straight-unchanged.png` |
| U6 | 콘솔 0 · 잔류 0 | 런 전체 console error/pageerror **0**. teardown: 소프트삭제 + `_purge-test-map.py` 하드퍼지 → 전 테이블 행수 baseline 일치(`login_records` 제외) | ✅ | — |

증적 스크린샷은 세션 스크래치패드(`…/scratchpad/qa-shots/`)에 보관 — 저장소에는 커밋하지 않는다.

## 관찰 로그 (U라운드)

| # | 내용 | 판단 |
|---|------|------|
| 1 | U1의 좌/우 회랑 후보가 **정확히 동률**(mid0=75, 좌 −13 / 우 163 모두 편차 88) | 설계된 결정적 타이브레이크(오른쪽/아래 우선)가 실렌더에서 그대로 관측됨 — 우측 163 선택. 트윈 중 좌우 플립 없음(U3에서도 동일 회랑 유지). |
| 2 | 회랑이 W 테두리에서 13px, 소스 핸들에서 8.5px 떨어진 좁은 스텁을 만든다 | 최소 이탈 규칙의 정상 결과. 노드 A·B·W가 같은 x=0 컬럼에 폭 150으로 겹쳐 있어 여백이 좁을 뿐, 관통은 없다. |
| 3 | U4 되돌리기 후 W가 x=0이 아닌 **−8**에 착지 | T라운드 이슈 #1과 동일한 **하네스 아티팩트**(드래그 임계용 선이동 3px). 회랑도 155로 함께 이동해 `W.right+13` 계약을 유지하므로 제품 이슈 아님. |
| 4 | U4 초안 판정 기준("회랑이 직결 midpoint로 복귀")은 **오판 기준**이었다 | 타깃이 소스 왼쪽인 right→left 핸들에서 RF 기본 smoothstep은 midpoint 단일 회랑이 아니라 `sourceX+20 / targetX−20` 2회랑 + 중간 y 수평의 Z 형상이다. 판정을 이 실제 서명 대조로 교체해 재측정(제품 무수정). |

---

# W라운드 — 라벨 회피·재핏·제목 핸들 (b0ba8665 → 재핏 수정 19921742)

U라운드 우회 배선에 얹은 후속 3종을 실기동 검수했다 — ① 우회 라벨이 장애물에 가리지 않는 최장 구간으로 이동, ② 성장 반영 후 1회 재-fit(마운트 1.5s 창 한정), ③ 프로세스 좌/우 핸들의 제목 라인(18px) 고정.
환경: 격리 sqlite(`qa-w.db`, dev.db 사본) + backend 8000(`AI_ENABLED=false DEV_ENFORCE_PERMISSIONS=false BPM_SYSADMINS=admin.sys`) + frontend 3000 네이티브, 시스템 Chrome(playwright-core, 1600×1100 → 캔버스 pane 1016×964), devUser=admin.sys, 노드 표시 토글 assignee/params/input/output. **1차 결과 5/6 PASS(W3 재핏 FAIL) → 수정 `19921742`(재핏 디바운스) 후 W3 재검증 8/8 PASS로 최종 6/6 PASS.**

시드(저장 좌표): `Start(-260,-120)` · `P1(0,0)` · `W(390,140)`(Input 8줄) · `P2(900,0)` · `P4(800,426)` · `P5(0,900)` · `S(390,910)` · `P7(800,1400)` · `End(1200,1000, 4줄 라벨)`. 엣지 3개 모두 `line_style=smoothstep`(s-right→t-left): `P1→P4`(라벨 `dodge label`) · `P1→P2`(`top lane`) · `P5→P7`(`wide dodge`).
측정은 전부 **flow(표시) 좌표계** — 노드는 `.react-flow__node` transform + `offsetWidth/Height`, 엣지는 path `d`의 `Q` 제어점(=원래 직각 꼭짓점), 라벨은 `EdgeLabelRenderer` div transform(−50% 보정), 카메라는 `.react-flow__viewport` transform. `getBoundingClientRect`는 줌 함정이라 미사용.

| # | 항목 | 측정치 | 판정 | 증적 |
|---|------|--------|------|------|
| W1 | 라벨 회피 | W 표시 rect `x[390..540] y[140..306]`. `P1→P4` 꼭짓점 `(154.5,19)→(553,19)→(553,559)→(795.5,559)` — 회랑 x=**553**=W.right+13(밀착). 구간: ①H 398.5 mid(353.75,19) 무가림 · ②**V 540** mid(553,289) **W가 가림** · ③H 242.5 mid(674.25,559) 무가림. 최장은 ②지만 라벨은 **①의 중앙**에 착지 — 실측 `(353.52, 19.13)`, 기대 대비 편차 `(−0.23,+0.13)`. 라벨 박스 `(317.02,8.13)-(390.02,30.13)` ∩ W 박스 = **없음**. 반사실(구 동작=최장 구간 앵커 (553,289))이면 박스가 W와 **23.5×22px 겹침** | ✅ | `W01-label-clear.png` |
| W2 | 폴백 무회귀 | `P5→P7` 꼭짓점 `(154.5,1033)→(377,1033)→(377,1591)→(795.5,1591)` — 회랑 x=**377**=S.left−13. 구간 ①H 222.5 · ②**V 558** mid(377,1312) · ③H 418.5, 전 구간 무가림(S 세로 밴드 `y[1012..1074]`에서 mid가 238px 아래). 라벨은 최장 ②에 그대로 — 실측 `(377.21, 1312.12)`, 편차 `(+0.21,+0.12)`. 라벨 박스가 어떤 노드와도 교차 없음 | ✅ | — |
| W3 | 재핏 | **1차(b0ba8665) ❌**: 대조군(성장 커밋 1회)은 zoom 0.56354→**0.52512**·전 8노드 pane(1016×964) 안 **6/6회**, 키 큰 IO 노드 2개도 zoom 0.49160·전원 안 **5/5회**였으나, **혼합 성장(4줄 End + IO 노드) 맵은 재-fit 미발동 0/8회** — 카메라가 성장 전 fit(zoom 0.56354)에 머물러 `P7` 화면 bottom **1017.9 > 964**(54px 잘림). **재검증(19921742) ✅**: 동일 재현 시드로 신선 로드 **8/8회 재핏 발동 + 전 9노드 뷰 내부**. zoom 0.56354→**0.52329~0.52521**(회차별 미세 편차), 재핏 애니메이션 프레임 10~17개 관측, 최하단 `P7` bottom 948.3~951.6(여유 **12.4~15.7px**), 최우측 `End` right 922.2~923.7(여유 92.3~93.8px). console error 0 | ✅ | `W03-refit-fixed.png`(수정 후) · `W03-refit-clipped.png`(1차 FAIL) · `W03-refit-all-visible.png`(1차 대조군) |
| W4 | 카메라 불가침 | 정착 2.5s 후 transform `matrix(0.563543,0,0,0.563543,208.477,110.625)` 기록 → W의 Input 박스 호버 → Show more 클릭(높이 **166→247**, +81) → 1.2s 대기. `P4 540→621`·`P5 1014→1095`·`S 1024→1105`·`P7 1572→1653`·`End 1114→1195`로 5개 노드가 밀렸음에도 transform **문자열 완전 동일**. **수정 후 재확인(19921742)**: 정착(>1.5s) 뒤 같은 조작에서 `matrix(0.523876,0,0,0.523876,229.56,105.865)` 전후 동일 — 디바운스가 카메라 이동을 되살리지 않음 | ✅ | `W04-camera-fixed-after-grow.png` |
| W5 | 제목 라인 핸들 | 프로세스 노드는 인라인 `top:18px` — `W`(h=**166**) `t-left`/`s-right` offsetTop **18**(50%라면 82), `P1`·`P2`·`P4`(h=38) 모두 18. 터미널은 인라인 스타일 **없음**(50% 유지) — `Start`(h=38) 18, **`End`(h=98, 4줄) 48**(=50%)로 18과 수치 분리. `P1→P2` 경로 `M154.5 19L174.5 19L525 19L525 19L875.5 19L895.5 19` — **y값 종류 1개(19)** = 두 노드 top(0)+18의 수평 단선 | ✅ | `W05-title-handles.png` |
| W6 | 콘솔 0 · 잔류 0 | 전 페이즈(본런 + 반복 8/6/5회 + 타임라인 2회 + 스크린샷런) console error/pageerror **0**. teardown: 테스트 맵 3건 `_purge-test-map.py` 하드퍼지 후 **전 테이블 행수 baseline 완전 일치**. 실 `dev.db`는 사본(`qa-w.db`)만 사용해 무접촉(mtime 불변) | ✅ | — |

보조 근거: `npx vitest run src/lib/edge-detour.test.ts` **11/11 통과** — 브라우저 시드로는 도달하지 않는 "전 구간 가림 → 최장 구간 폴백" 분기를 단위 테스트가 덮는다.
증적 스크린샷은 세션 스크래치패드(`…/scratchpad/qa-shots/`)에 보관 — 저장소에는 커밋하지 않는다.

## 이슈 로그 (W라운드)

| # | 내용 | 판단 |
|---|------|------|
| 1 | **W3 재핏이 "성장 오프셋이 2커밋에 나눠 도착하는 맵"에서 영구 유실** (0/8회) | **제품 이슈 → `19921742`에서 수정·재검증 완료(8/8)**. 수정은 소모 플래그(`didGrowthFitRef`)를 타이머 콜백 안에서 세우는 것 — 이제 뒤따르는 성장 커밋이 취소 대신 **재예약**하고 마지막 커밋 기준 80ms 디바운스로 1회만 발사한다(창 밖 도착은 발사 없이 소모 표시). 이하 1차 진단 기록: 근거는 최하단 노드 `P7`의 표시 y 궤적. 혼합 맵: `1400 → 1460`(t=538, 여러 줄 **터미널**의 텍스트 전용 높이는 IO 박스보다 한 커밋 먼저 확정) → t=563부터 `1460→1572` 트윈(IO 성장) = **yOffsets 변경 2회, 간격 25ms**. 대조군: `1400 → 1628` **단일 스텝**(t=560). 재핏 이펙트는 `return () => clearTimeout(timer)`를 달고 deps가 `[yOffsets, reactFlow]`이므로, **두 번째 yOffsets 변경이 cleanup으로 80ms 타이머를 취소**하고 `didGrowthFitRef.current`는 이미 `true`라 **재예약되지 않는다**. 터미널은 재현 트리거의 하나일 뿐, 성장 신호가 두 번 오는 맵이면 동일. |
| 2 | 1.5s 마운트 창은 W3 실패 원인이 **아니다** | 타임라인 실측: `.react-flow` 마운트 264ms · W 성장 513ms · 최초 fit 534ms · P4 밀림 578ms — 전부 창 안. 창 가드는 무고하고 위 취소 경로가 진범이다. |
| 3 | `isLabelClear`의 **가로 ±80 기준은 선택된 회랑에서 사실상 항상 실패**한다 | 회랑 후보가 구조적으로 `장애물 모서리 ± 13`(MARGIN 12 + 1)이라 가로 여백이 80을 넘을 수 없다. 즉 실제 무가림 판정은 전부 **세로 ±12 밴드**가 낸다(W2가 그 사례 — mid가 S 아래 238px). 의도된 동작이면 무해하나, 가로 기준이 코드상 죽은 조건에 가깝다는 점은 기록해 둔다. |
| 4 | 캔버스는 `selectionOnDrag` + `panOnDrag={[1]}` + `zoomOnScroll={false}` | 검수 절차 메모 — 좌드래그는 선택 박스가 생기고 휠은 줌이 아니라 팬이다. 확대는 우하단 줌 pill의 `+`, 이동은 **가운데 버튼 드래그**로만 해야 스크린샷이 오염되지 않는다(첫 시도에서 선택 박스·노드 선택으로 재촬영). |
| 5 | 수정 후 재핏 zoom이 회차마다 **0.52329~0.52521**로 흔들린다(편차 0.4%) | **정상 범위**. 디바운스가 마지막 오프셋 커밋 기준 80ms에 발사되는데 그 시점엔 Y 트윈이 아직 진행 중이라, fitView가 트윈 중간 좌표로 bounds를 잡는다. 잔여 트윈만큼 노드가 더 내려가 아래쪽 여유가 `padding:0.1`이 주는 값보다 줄지만, 8회 전부 최하단 `P7` bottom 여유 **12.4~15.7px**로 뷰 안에 남았다. 여유가 더 필요해지면 트윈 종료 후 발사로 바꾸는 선택지가 있으나 현 시드에선 회귀 아님. |
