# Progress

프로젝트 진행 현황 로그. 커밋 직전 갱신 (`rules/common/git.md`). 한 줄 요약만 — 상세는 git 이력·`docs/superpowers/specs/`·`docs/spec.md` 참조.

- **하위프로세스 권한 마스킹 — Task 3 양방향 검증 완료 + compare 403 기록 (mask Task 3, feat/expand-sync, 검증 전용·코드 무변경)** — 3중첩 픽스처(L1·L2 owner user.lee → L3 owner user.choi, `DEV_ENFORCE_PERMISSIONS=true`)로 차단/허용 양방향 스모크. **차단**: user.lee→L3 및 user.choi→L1/L2 = `200+{locked:true,nodes:[],edges:[]}`(데이터 미유출), UI 봉인(Lock 뱃지·expand 버튼 0·아웃라인 chevron 0·expand-all이 잠긴 L3 미펼침). **허용**: user.choi→L3(소유자, 직접 열기+API `200 locked=false nodes>0`)·sysadmin(admin.kim)→L1→L2→L3 풀 통과 → 잠금은 구조 아닌 **권한 조건부**이며 과하지 않음. 관측 매트릭스=기대 일치, pageerror 0. 회귀: depth-2 드릴·일반맵·포커스모드·expand/collapse-all 무회귀. **기록(이 기능 무관, 별도 저우선)**: `/maps/{id}/compare` 가 초기 `GET /maps/{id}` 에서 dev-auth 타이밍 레이스로 403(`X-Dev-User` 헤더가 요청 뒤에 세팅→`local-dev` 폴백→빈 캔버스). **일반 맵에서도 재현**되고 마스킹 diff는 compare·dev-auth 미접촉 → 이 기능의 회귀 아님(compare는 resolved 미호출이라 마스킹 영향 0). dev 전용 추정(운영 Keycloak 영향 미상). 별도 dev-login 헤더 타이밍 수정으로 분리 권장. `.git/sdd/mask-task3-report.md`. **마스킹 Task 0~3 완료**(각 implementer+reviewer 통과, whole-branch 최종리뷰는 사용자 결정으로 생략).
- **잠긴 하위프로세스 마스킹 렌더 — 프론트 게이트 전환 (mask Task 2, feat/expand-sync)** — Task 1 서버가 viewer 미만 호출자에게 반환하는 `200 + {locked:true, nodes:[], edges:[]}` 를 UI가 존중하게 함: 잠긴 링크맵 호스트는 Lock 뱃지를 달고 펼침/드릴 불가(캔버스+아웃라인), 단 호스트 노드와 입력/대표출력 엣지는 유지(Option 1 봉인 박스). **잠금은 status 아닌 응답 body(`g.locked`)로 판정** — eager loader `.then(g)`가 `g.locked`면 `lockedKeys`(state)에 키 기록(resolvedCache엔 미저장 → getEmbed null → buildCompositeTree가 자식 없는 봉인 호스트 유지), `.catch`는 실제 네트워크/5xx만. 새 `lockedKeys: Set<string>` state + `lockedKeysRef` 미러. 게이트 3곳 잠금 인지화: `canExpand`(no-op→fullGraphRef 룩업+linkKey∉lockedKeysRef, eslint-disable 제거), `isDrillableHost`(구조검사+권한검사), `injectSubEnds`(잠금 키면 `data.locked:true` — 모든 subprocess 렌더 경로[childNodes materialize·composite kid·displayNodes]가 통과하는 **단일 전파점**, 어서션 #3 확인됨). 아웃라인은 별도 경로라 `OutlineNode.locked`를 outline useMemo 양 브랜치(라이브 `node.data.locked`·임베드 `linkKey(flat)∈lockedKeys`)에서 채우고 `buildOutline`의 `isSubprocessExpandable(type, locked)`가 잠김이면 펼침 화살표 억제. `process-node.tsx`에 `LockedBadge`(lucide `Lock` 16px sw1.5, `border-hairline bg-surface text-ink-secondary shadow-sm`, tooltip `subprocess.locked`) — 잠김이면 `ExpandToggleButton` 대신 표시, `SubprocessHandles`는 불변(엣지 유지). i18n `subprocess.locked`(EN "No access"/KO "권한 없음"). `api.ts` `Graph.locked?: boolean`, `canvas.ts` `NodeData.locked`·`OutlineNode.locked`. **부수**: `canExpand`가 ref를 읽게 되며 `toggleInlineExpand`를 직접 호출하던 SummaryPanel `onOpenChild` 인라인 화살표가 새 `react-hooks/refs` 룰("Cannot access refs during render")을 건드림 → `handleSummaryOpenChild` useCallback으로 추출(파일 기존 `handleToggleExpand`·`handleSummaryPatch` 패턴과 동일, useCallback 내부 ref 접근은 룰 허용). 포커스모드 effect·펼침 애니메이션 미접촉. 검증: lint 0err(기존 무관 warning 1)·tsc·build clean. 렌더 스모크(시스템 Chrome, `AUTH_ENABLED=false DEV_ENFORCE_PERMISSIONS=true` + reset_db seed_nesting_demo, **user.lee**[L3 비소유]로 L1 열고 L2 인라인 펼침): L3(map9) resolved=**200 locked=true nodes=0**(데이터 미유출)·L2(map10)=locked=false nodes=3, L3 호스트 Lock 뱃지 1·expand 버튼 0·in/out 핸들 1/1(봉인), 더블클릭 드릴 미발생(6→6), 아웃라인 L3행 chevron 0·L2행 chevron 1(비잠금 정상), pageerror 0. dev.db 복원·서버 정지. `.git/sdd/mask-task2-report.md`.
- **resolved 권한 마스킹 서버 차단 (mask Task 1, feat/expand-sync)** — `GET /api/library/processes/{map_id}/resolved` 에서 viewer 미만 호출자를 완전 잠금. `GraphOut`에 `locked: bool = False` 추가(하위 호환 기본값). `resolved_graph`에 `user: str = Depends(get_current_user)` 주입, 권한 체크를 `_load_graph` 호출 전 최초 수행: `role_rank(get_effective_role(...)) < role_rank("viewer")` → `GraphOut(nodes=[], edges=[], locked=True)` 즉시 반환(그래프 미빌드). None(맵 없음·권한 없음) = rank 0 → 자동 잠금. `imports`: `get_effective_role`, `role_rank` 추가. 수정 파일: `app/routers/library.py`, `app/schemas.py`. 신규 테스트 4개(`tests/test_library_mask.py`): below-viewer→200+locked+빈nodes/edges, viewer→200+unlocked+실그래프, owner→200+실그래프, sysadmin→200+실그래프. `test_graph.py` 2개 exact-dict 단언에 `locked: False` 포함(의미 약화 없음). 최종: ruff clean, **266 passed** (262+4). `.git/sdd/mask-task1-report.md`.
- **딥드릴 합성 id — L2→L3 더블클릭 드릴 수정 (mask Task 0, 마스킹 전제)** — 근본원인은 합성 id 미해석이 아니라 **이벤트 라우팅**: 캔버스 캡처-phase `dblclick` 핸들러가 "현재 프레임 노드는 전부 RF에 위임"했는데, 딥뷰(읽기전용, `data.scopeId!=null`) 프레임 노드에는 React Flow가 `onNodeDoubleClick`을 발화하지 않아(루트 편집 노드만 발화) L2→L3 드릴이 죽었음(브라우저 실측: deep-view 노드는 onNodeClick은 발화·onNodeDoubleClick 미발화, 캡처 핸들러는 inFrame=true로 조기 return). 합성 id(`n1-sub/n2-sub`)는 `fullGraph`에 존재·`node_type`/`linked_map_id` 보존되어 3개 룩업(dblclick 카메라계산·isDrillableHost·buildScopesTo)은 모두 정상 — 도달만 못했음. 수정: 캡처 핸들러 가드를 `scopeId`로 분기 — 루트 편집 프레임(`scopeId==null`)만 RF에 위임(드릴+타이틀 이름편집 보존), 딥뷰 읽기전용 프레임은 여기서 직접 `drillIntoSubprocess` → 합성 id가 그대로 흘러 정상 해석. **확인 #1**: 인라인 펼침은 노드 객체에서 `linked_map_id`를 직접 읽어 합성 id 무관(영향 없음), 드릴은 id 문자열 재룩업이라 이벤트 미도달 시 죽음 — 확인됨. 다른 드릴 진입점은 dblclick→isDrillableHost 1개(+다른스코프 클릭 onNodeClick은 single-click). 검증(시스템 Chrome, DEV_ENFORCE OFF, seed_nesting_demo L1=11/L2=10/L3=9): L2→L3 더블클릭=L3 진입(Receive/Handle/Done 프레임·브레드크럼 L1›L2›L3·카메라 무점프), L1→L2 드릴·루트 타이틀 이름편집·일반맵(주문처리 4노드) 더블클릭·아웃라인 depth-3(L3 leaf) 무회귀, pageerror 0. lint(신규 0)·tsc·build clean. dev.db 복원·서버 정지. `.git/sdd/mask-task0-report.md`.
- **마스킹 게이트 자리(no-op) + 3중첩 데모 픽스처 (expand-sync Task 2)** — 펼침/드릴이 수렴하는 3개 지점을 미래 마스킹 권한이 뒤집을 수 있는 "게이트 자리"로 만들되 동작은 불변(현재 항상 허용): ① `canExpand(nodeId)=true` no-op + `toggleInlineExpand` 본문 최상단 `if(!canExpand) return`(펼침·컨텍스트 open child·아웃라인 subprocess 모두 수렴) ② `isDrillableHost` 주석만(구조 검사 불변, 링크맵 권한 검사 자리 표시) ③ `canvas.ts` buildOutline의 `hasChildren` 하드코딩을 `isSubprocessExpandable(type)` 로컬로 추출(여전히 subprocess면 true). 데모 시드 ADDITIVE: 3중첩 A→B→C 체인 `seed_nesting_demo(session)`(`scripts/seed_nesting_demo.py`) — "Nesting L3 — Leaf"(owner **user.choi**, published, start→process→end) ← "Nesting L2 — embeds L3"(owner user.lee, published, start→[subprocess→L3]→end) ← "Nesting L1 — embeds L2"(owner user.lee, draft, start→[subprocess→L2]→end). 링크맵에 실제 내부 노드+중첩 subprocess가 있어 깊이-3 드릴인 실현(기존 픽스처는 링크맵에 중첩 없어 depth-3 불가). L3=choi 는 다음 마스킹 작업의 비대칭 사전 마련(권한 코드 없음). 기존 맵/seed_local_employees/seed_reference_demo 불변. 검증: ruff clean·pytest 262 pass(reset_db 미사용→무영향)·lint(신규 0)·tsc·build clean, `python -m scripts.reset_db` 성공(L1.sub→L2·L2.sub→L3·L3 owner=user.choi 확인 print), **depth-3 SYNC 런타임 스모크**(시스템 Chrome, DEV_ENFORCE OFF): 3단 인라인 임베드(L1→L2→L3 캔버스 9노드) → 중간 호스트 L2 접기=L2·L3 둘다 닫힘(decision ②, Task-1 R2 갭 실측), 드릴 L1→L2 후 브레드크럼 중간복귀=scope 체인 PRESERVED(focusScope, windowed-scope 모델)·재포커스 동작, 게이트 동작 불변, pageerror 0. dev.db 복원·미스테이징. **드릴-L3 합성 id 한계 발견**(딥 스코프 호스트 노드 data-id가 복합 `n1-sub/n2-sub` → isDrillableHost 미해석, 캔버스 더블클릭 드릴은 첫 진입서 미발화 — 인라인 펼침/아웃라인 경로는 정상, 기존 동작·내 변경 무관). `.git/sdd/expand-sync-task2-report.md`.
- **아웃라인 접기 드릴인 모드 인지 (expand-sync Task 1)** — 드릴인(scopes)으로 펼친 하위프로세스를 아웃라인 ←/chevron/F로 접으면 캔버스가 안 접히던 버그 수정. 원인: 포커스모드 scope-load effect가 빠져나온 드릴 경로를 다시 인라인 펼침(re-add). 픽스: `collapseIntentRef`(명시 접기 host 가드) + mode-aware `collapseSubprocessRow`(드릴인=scope pop+가드 표시·인라인=기존 토글), effect는 가드를 캡처→즉시 클리어(1사이클)→keep-filter·re-add 양쪽서 제외하는 가드만 추가, 접기 방향 분기 3곳(`handleOutlineCollapse`·`handleToggleExpand`·`handleOutlineFold`) 배선(→ `handleOutlineExpand` 불변). page.tsx만 수정(51/9). 검증: lint 0err·tsc·build clean, 런타임 스모크(map4 host 결제) (a)드릴→접기=캔버스접힘·(b)인라인 펼침/접힘 불변·(c)R2 스코프nav 단독 미접힘·(d)R1 재드릴 동작(가드 1사이클 클리어)·(e)이중상태 둘다제거, pageerror 0. dev.db 복원·미스테이징. **데이터 한계: 3-deep A→B→C 체인 없음→2-scope로 검증.** `.git/sdd/expand-sync-task1-report.md`.
- **권한 워크플로 데모 시드 + EN/KO 가이드 (Task 5)** — `reset_db` 가 호출하는 ADDITIVE `seed_permission_demo(session)` 신설(`scripts/seed_permission_demo.py`). 데모 엔터티: 가시성 대비 맵 2(Public/Private), "Roles & Principals Demo" 맵(owner lee + park editor·choi viewer·Procurement Office dept editor·active 그룹 editor = 3종 principal, pending 2건[downgrade·visibility_change], 승인자 jung[active]+user.former[inactive 신규 직원]), 그룹 2(active "Approved Cross-Team Group"[approved_by admin.kim, 멤버 choi+dept, 상속] · pending "Proposed Review Group"[큐 표시]), "Version Workflow Demo" 맵(published v1 + pending v2[submitted_by lee, approver jung, tally 0/1]). LOCAL_USERS/seed_reference_demo 불변. 검증: ruff clean·pytest 262 pass(테스트는 reset_db 미사용→무영향), `python -m scripts.reset_db` 성공(employees=6·perms=8·requests=2·groups=2·approvers=3·pending_ver=1), 런타임 스모크(시스템 Chrome, `DEV_ENFORCE_PERMISSIONS=true BPM_SYSADMINS=admin.kim`): park 가 Public 보이고 Private 안 보임·협업자 3 principal·승인자 active+inactive·결재 대기 2·admin 그룹 큐 g_pending·v2 pending, pageerror 0. dev.db 복원·미스테이징. 가이드 `docs/permission-demo-walkthrough.md`(EN/KO 8단계). `.git/sdd/perm-demo-seed-report.md`.
- **권한 whole-branch 리뷰 픽스(I-1·M-2)** — `create-map-dialog.tsx` Promise.all에 `.catch` 추가(console.warn + 빈배열 폴백, 주석과 실제 동작 일치). `collaborators-panel.tsx`는 이미 try/catch 있음→무변경. `reassign-approver-modal.tsx` 삭제(importer 0, 브랜치가 직접 생성한 고아). `permissions-store.ts`의 `setApprovers` 함수·주석·`MapApprover` import 제거. 검증: lint 0err·tsc·build clean, pytest 262 pass. `.git/sdd/perm-whole-review.md` "Fixes applied" 섹션 추가.
- **mock-store 정리(dead code 제거)** — Layer 1-4 후 외부 참조 0인 심볼 제거. `VersionPublishPayload`(types) + 8 logic 함수(`roleRank`·`getGroupMembership`·`getEffectiveRole`·`isVisibleToUser`·`canComment`·`isDowngrade`·`getActiveApprovers`·`hasActiveApprover`) + 18 store 함수(`resetPermissions`·`addCollaborator`·`changeRole`·`removeCollaborator`·`requestVisibilityChange`·`transferOwner`·`createMapPermission`·version-flow 4종·`requestGroup`·`decideGroup`/`addGroupMember`/`removeGroupMember`/`setGroupManagers`(실 API로 이전)·`toggleUserActive`·`deleteMapMock`). 유지: `decideRequest`+`usePermissions`(approval-queue MAP 미리보기), `setApprovers`(reassign-approver-modal mock 잔존), `useCurrentMockUser`(서버 신원 훅), 피커 어댑터 타입, 각각 EN/KO 주석 추가. cross-map 결재 큐 엔드포인트는 미생성(금지). 검증: lint 0err·tsc clean·build clean, pytest 262 pass, 런타임 스모크(시스템 Chrome, home/settings/groups/admin) pageerror 0. dev.db 복원·미스테이징. `.git/sdd/perm-cleanup-report.md`.

- **권한 Layer 4 Task 4(마지막) — 유저그룹 화면 + 그룹 협업자 principal 실 API 배선**. Task-3b 그룹 API로 프론트 그룹 표면 전체를 mock→real 전환. (A) `api.ts`: `Group`/`GroupMember`/`GroupStatus`/`GroupMemberInput` 타입 + `listGroups`/`createGroup`/`getGroup`/`addGroupMember`/`removeGroupMember`(member PK)/`setGroupManagers`/`listPendingGroups`/`decideGroup` wrapper(라우터 응답 1:1·dept member_id=org_path 문자열). (B) `groups/page.tsx`: 목록 `listGroups`·생성 `createGroup`, 멤버 피커=실 `getDirectory`, **≥2-멤버 클라 검증**(submit disable + `minMembersHint`, 서버도 422), 생성 후 재조회; mock `requestGroup`/`state.groups` 폐기. (C) `groups/[groupId]/page.tsx`: 로드 `getGroup`·멤버 add/remove·관리자 PUT 실 API(갱신 그룹 응답을 state 반영, 낙관적 갱신無), 권한/상태 위반=서버 403/409/422→토스트, 404=not-found(가시성 은닉), 표시명=실 디렉터리. (D) `admin/approval-queue.tsx`: 그룹 큐=`listPendingGroups`+`decideGroup`(결정 후 재조회, 인라인 async+active 가드로 set-state-in-effect 회피); **MAP cross-map 큐는 라벨된 mock 미리보기 유지**(엔드포인트 부재). (E) collaborator picker(`collaborators-panel`·`create-map-dialog`): 그룹 옵션=실 active 그룹(`listGroups` 필터, principalId=문자열 그룹 id), **`perm.groupLayer4Note` 표시·키 삭제**(그룹 grant 실효). i18n 신규 `perm.group.minMembersHint`(en/ko). 잔존 mock(설계상): approval-queue의 MAP 미리보기(`usePermissions`/`decideRequest`), 피커 어댑터용 타입(`Department`/`MockUser`/`UserGroup`), `useCurrentMockUser`(서버 신원). 검증: lint 0err(내 파일)·tsc·build clean, pytest 262 pass(백엔드 무변경), 런타임 스모크(시스템 Chrome, `DEV_ENFORCE_PERMISSIONS=true BPM_SYSADMINS=admin.kim`): user.lee 2-멤버 생성→pending→admin.kim 승인 큐 approve→active→맵1 그룹 editor grant→user.lee `my_role=editor`, 1-멤버 422+클라 submit-disabled+hint, 와이어 화면 4개 pageerror 0. dev.db 복원·미스테이징. **Layer 4 그룹 표면 완료. 남은 것: mock-store 정리(별도)·cross-map 결재 큐 엔드포인트·AD·감사.** `.git/sdd/perm-task-4-l4-report.md`.
- **권한 Layer 4 Task 3b — 유저그룹 관리 API**. Task-3a 테이블/판정 위에 그룹 CRUD·승인 라우터를 올림. 신규 `app/routers/groups.py`(prefix `/api`, `Depends(get_current_user)`): `GET /groups`(가시성 필터 — sysadmin은 pending/rejected 포함 전부, 그 외는 active + 본인 생성 pending), `POST /groups`(status='pending'·created_by=user·**멤버 ≥2 검증 422**·생성자 자동 관리자+전달 관리자 dedupe), `GET /groups/{id}`(가시성 규칙, 미가시=404 존재 은닉), `GET /groups/pending`(**sysadmin only** 403 else, `/{group_id}` 앞에 선언해 int 경로캡처 회피), `POST /groups/{id}/members`·`DELETE /members/{member_pk}`(게이트=manager OR sysadmin·**active만 409**·중복 409), `PUT /groups/{id}/managers`(집합 교체·**≥1 유지 422**), `POST /groups/{id}/decide`(**sysadmin only**·approve→active+approved_by/at·reject→rejected·재결정 409 멱등가드). `deps.py`에 `is_group_manager`/`assert_group_manager_or_sysadmin` 추가. `schemas.py`에 `MemberIn/MemberOut/GroupCreate/ManagersIn/GroupDecisionIn/GroupOut`. `main.py` 라우터 등록. 신규 `tests/test_groups.py` 19개(enforce/act_as 패턴 + **end-to-end 판정**: 승인 그룹 editor grant→user-멤버 effective_role=editor / pending 그룹 grant→무역할). 검증: ruff clean, pytest 262 pass(243 기존 + 19 신규), `reset_db` exit 0, dev.db 복원·미스테이징. **Layer 4 남은 건 Task 4(프론트)만.** `.git/sdd/perm-task-3b-l4-report.md`.
- **권한 Layer 4 Task 3a — 유저그룹 스키마 + `effective_role` 그룹 principal 활성**. Layer 1-2가 저장만 하고 무시하던 `principal_type='group'` grant를 실제 판정에 반영. (A) `models.py` 3테이블 추가 — `UserGroup`(user_groups, status pending|active|rejected), `UserGroupMember`(user_group_members, member_type user|department; department member_id=org_path 문자열), `UserGroupManager`(user_group_managers). 마이그레이션 無 — `reset_db`의 `create_all`로 재생성, `db.py _ADDED_COLUMNS` 미변경. (B) `logic.effective_role`에 `user_group_ids: set[str]` 파라미터 추가(순수성 유지) — `'group'` 분기가 `principal_id ∈ user_group_ids`일 때만 grant 적용, 우선순위(highest-wins) 불변, docstring 갱신; `is_visible`도 동일 스레드. (C) `access.py` 신규 `get_user_active_group_ids(session, login_id, emp_org_path)` — `user_group_members`⨝`user_groups` status='active'만, user-멤버(member_id==login_id) 또는 dept-멤버(`belongs_to_department(emp_org_path, member_id)`)면 그룹 id(문자열) 수집; `get_effective_role`이 산정·전달. (D) 콜러 4곳 갱신: `access.get_effective_role`, `routers/maps.py list_maps`(루프 밖 1회 산정), `logic.is_visible`, 테스트 헬퍼. (E) `tests/test_permissions_logic.py` group-ignored 테스트를 새 계약으로 전환(멤버일 때 적용·미멤버일 때 무시, 삭제 아님), 신규 `tests/test_group_judgment.py`(enforce 픽스처로 sysadmin 차별화, user-멤버·dept-멤버·비멤버·pending·highest-wins). 검증: ruff clean, pytest 243 pass(236 기존 green 유지+신규), `reset_db`로 3테이블 생성·empty 확인, dev.db 복원·미스테이징. `.git/sdd/perm-task-3a-l4-report.md`.
- **권한 Layer 4 Task 0 — 협업자 피커 데이터소스 교체 (Korean mock → English 실 디렉터리)**. (A) Backend: `GET /api/directory` 신규 라우터(`app/routers/directory.py`) — `Depends(get_current_user)` 전용(require_admin 미사용), 전 직원 `{id:login_id, name, department}` + 각 직원 org-level의 "/" 구분 프리픽스 전부(l1, l1/l2, l1/l2/l3, …)를 `{id:org_path, name:리프세그먼트}`로 반환. `schemas.py`에 `DirectoryUserOut`·`DirectoryDeptOut`·`DirectoryOut` 추가. `main.py`에 라우터 등록. (B) Tests: `tests/test_directory.py` — non-admin 200, 영문 이름 확인, org-path 프리픽스 3레벨 모두 확인, admin 접근 가능 (총 2테스트, 전체 227 pass). (C) Frontend: `api.ts`에 `DirectoryUser`·`DirectoryDept`·`Directory` 타입 + `getDirectory()` wrapper 추가. `collaborators-panel.tsx`·`create-map-dialog.tsx` 양쪽에서 마운트 시 `getDirectory()` 호출, 실 users+departments를 피커 형식으로 변환해 `PrincipalPicker`에 전달; groups는 mock 유지(Task 3/4). 검증: ruff clean, pytest 227 pass, lint 0err, tsc clean, build clean. 런타임 스모크는 사용자 검증 단계(STOP). `.git/sdd/perm-task-0-l4-report.md`.

- **권한 Layer 3 Task 3(마지막) — 결재 승인·버전 게시 실 API 배선**. (A) 맵 결재-결정 큐: `pending-approvals-panel.tsx`를 `listApprovalRequests`(GET)·신규 `decideApprovalRequest`(POST `/approval-requests/{id}/decide`, `decision:"approve"|"reject"`)로 전환 — mock `decideRequest` 폐기, 서버 진실(approve 시 서버가 다운그레이드/가시성 적용→**요청 목록 재조회 + 호스트 `onDecided`로 맵 데이터 재조회**, 낙관적 갱신 금지), 결정중 버튼 disable, 403/409 토스트. settings page: `refreshMap` useCallback 추출(결재 후 역할/가시성 재조회), 패널 props 정리(`currentUserId` 제거·`onDecided` 추가). (B) 버전 워크플로: `versions-publish-panel.tsx`를 mock `versionFlow` 폐기·실 `getWorkflowState`(버전별)·`submit/approve/reject/publish/withdraw`로 전환, 액션 후 워크플로 재조회. §9-4 매핑 = **1:1 확인(상위집합)**: 상태명(draft/pending/approved/rejected/published)·전이 동일, 실 흐름이 만장일치 다중승인자(승인 tally `approvals/approvers` 표시)·반려사유필수(window.prompt)·`withdraw`(제출자, mock無)를 추가. 게이팅은 워크플로 상태+(approvers/submitted_by↔currentUserId) 파생, 서버가 최종 게이트(403/409). (C) admin/approval-queue: **cross-map 결재 목록 엔드포인트 부재** → 전역 큐 MAP 부분은 mock 미리보기 유지(엔드포인트 발명 금지·보고), 실 MAP 결재는 맵별 설정 화면에서. 그룹생성 큐는 mock(Layer 4). i18n 신규 5키(en/ko): `perm.version.withdraw`·`approvedByYou`·`rejectReasonPrompt`·`rejectReasonRequired`·`perm.sysadmin.mapMockNote`. 유지(Layer-4 group): `decideGroup`·그룹 액션·`usePermissions`(admin queue). 잔존 dead(내 변경으로 미사용된 store 함수, 미삭제·보고): `requestVersionPublish`/`approveVersionPublish`/`rejectVersionPublish`/`publishVersionFlow`/`getActiveApprovers`/`VersionFlowEntry`. 검증: lint 0err(내 파일)·tsc·build clean, pytest 225 pass(백엔드 무변경), 런타임 스모크(시스템 Chrome, enforcement OFF): map1에 editor→viewer 다운그레이드 pending 시드→"결재 대기" Approve 클릭→**서버 적용 확인(user.lee role=viewer·request applied·목록서 제거)·pageerror 0**, 버전 happy-path API 스모크(draft→submit→pending→approve(만장일치)→approved→publish→published). dev.db 복원·미스테이징. **Layer 3 완료 — MAP 권한+버전 표면 전부 실 API. 남은 Layer 4: 그룹 생성/판정·cross-map 결재 큐·AD·감사.** `.git/sdd/perm-task-3-l3-report.md`.
- **권한 Layer 3 Task 2 — 맵 권한 화면 실 API 배선(mock 액션 폐기)**. 6개 화면을 Layer-2 엔드포인트로 전환, 원칙 = 서버 진실(낙관적 갱신 금지·변경 후 재조회 또는 응답 반영). `api.ts`: `listMapPermissions`/`addMapPermission`/`changeMapPermission`(pending 봉투)/`removeMapPermission`(pending 봉투)/`transferMapOwner`/`requestVisibilityChange`/`listApprovalRequests` + 타입(`MapPermission`/`ApprovalRequest`/`PermissionMutationResult`), `MapSummary.visibility` 추가. 컴포넌트: collaborators-panel(GET 목록·POST 추가·PATCH/DELETE — 다운그레이드/에디터제거는 mutation 응답 `pending:true`로 "승인 대기" 표시·역할 미변경, group은 저장+Layer4 노트), approvers-panel(GET/PUT, active-toggle은 백엔드 없어 비활성+Layer4 노트, "0명" 경고=목록 비어있음), visibility-control(POST visibility-request→pending 표시, 현재값은 settings의 getMap.visibility), danger-zone(POST transfer-owner→`/maps/{id}` 이동·클라 불변식 폐기·확인모달만, DELETE map→홈), create-map-dialog(POST maps→협업자별 POST permissions→PUT approvers, mock 오버레이 제거, 가시성은 생성시 private). settings page: getMap.visibility 캡처(mock getMapMeta 폐기)·ApproversPanel/VisibilityControl props 갱신·mock 강제 재지정 모달(`getActiveApprovers`) 제거. 백엔드 최소(정당): `MapOut.visibility` 읽기전용 노출(없으면 가시성 서버진실 표시 불가) — pytest 225 그대로 pass(`my_role` 멤버십 단언이라 무영향). i18n 신규 2키(en/ko): `perm.groupLayer4Note`·`perm.approversToggleLayer4`, delete 문구는 실삭제로 정정. 유지(미배선): pending-approvals/admin approval-queue=Task 3, 그룹 화면=Layer 4(`isApprover`/usePermissions 등 심볼 유지). 검증: lint 0err(내 파일)·tsc·build clean, pytest 225 pass, 런타임 스모크(시스템 Chrome, enforcement 기본 OFF): 맵 생성=owner·private, 협업자 추가 재조회 노출, editor→viewer 다운그레이드 "승인 대기"·서버 역할 editor 유지·pending 요청 1건, pageerror 0. dev.db 복원·미스테이징. `.git/sdd/perm-task-2-l3-report.md`.
- **권한 Layer 3 Task 1 — 역할 컨텍스트 서버 단일화 + 시드 영문화(AD 정렬)**. (A) `app/ad/service.py LOCAL_USERS` name/title/org 영문화(login_id 불변, 3패턴 유지: lee==park / choi 같은 Procurement Office 다른 팀 / jung l3-null 상위prefix / kim 다른 office). 테스트 영문 동기화·약화 없음(경계가드 `…/Procurement`≠`…/Procurement Office/…` 유지): `test_permissions_logic.py`(PATH_*·dept matrix·effective_role), `test_permission_gates.py`(dept-grant), `test_employees.py`(dept assert). `reset_db.py`는 service.py와 어긋난 한글 시드(다른 부서·org_l*無)였고 seed_local_employees를 호출하지 않았음 → `seed_local_employees` 재사용으로 단일 소스화. (B) 가법 추가: `MeOut.is_sysadmin`(/api/me, `logic.is_sysadmin`), `MapOut.my_role`(GET /maps/{id}=`access.get_effective_role`, GET /maps=가시성필터의 effective_role 재사용·이중계산 회피, create=owner). `test_maps.py` 3개 추가. (C) 프론트 게이팅 서버 단일화: `CurrentUser.isSysadmin`(providers.publishMe가 /api/me에서 채움), `useCurrentMockUser` 신원을 서버 currentUser에서(seed 조회 폐기), api.ts wire types에 my_role/is_sysadmin 스레드, home은 `m.my_role!==null` 필터(isVisibleToUser 폐기), top-nav sysadmin 링크=`user.isSysadmin`, settings effectiveRole=서버 my_role(getEffectiveRole 폐기·roleLoaded 가드)·dev스위처는 /api/me 재경유. mock STORE은 Task-2 액션용 유지. dev-auth LOCAL_USERS 표시명 영문 정렬. 검증: ruff clean·pytest 225 pass, frontend lint 0err·tsc·build clean, dev.db clean. `.git/sdd/perm-task-1-l3-report.md`.
- **권한 백엔드 Task 4 — 권한 관리 엔드포인트(Layer 2 마지막)**. 프론트 mock-store 액션이 매핑되는 API 추가. 신규 `app/routers/permissions.py`(prefix `/api`): collaborators GET/POST/PATCH/DELETE(`/maps/{id}/permissions`, gate editor+) — 추가/업그레이드 즉시, **다운그레이드(editor→viewer)·editor 제거는 `permission_downgrade` ApprovalRequest pending 생성·미적용**, viewer 제거 즉시, owner grant 변경·제거는 409(이양 경로로), 중복 grant 409, group principal 저장 허용(effective_role은 무시 — Layer 4); owner 이양 `POST /transfer-owner`(gate owner, 즉시) — new_owner editor+ 필수(아니면 409)·구 owner→editor·신규→owner·`map.owner_id` 갱신·**owner grant 정확히 1개 불변식**(전 owner행 일괄 강등); 가시성 변경 `POST /visibility-request`(gate owner) `visibility_change` pending; 승인요청 `GET /approval-requests`(gate approver-or-sysadmin) + `POST /approval-requests/{id}/decide`(런타임 gate, approve→payload 적용 후 applied[downgrade=role 변경/제거, visibility=flip], reject→rejected·무변경, idempotent). `deps.py`에 `is_map_approver`/`assert_approver_or_sysadmin`/`require_approver_or_sysadmin` 추가. 기존 `PUT /approvers`에 `assigned_by=current`만 추가(재게이트는 DEFER). 신규 `test_permission_endpoints.py` 22개(enforce/act_as 패턴). 검증: ruff clean, pytest 216 pass(194 기존 + 22 신규), dev.db clean. **Layer 1+2 완료** — 스키마·판정·게이트·관리 API 전부 green. `.git/sdd/perm-task-4-report.md`.
- **권한 백엔드 Task 3 — 맵 엔드포인트 권한 게이트 강제(Layer 2, HIGH 위험)**. Task 2 순수 logic을 FastAPI 게이트로 배선. 신규 `app/permissions/access.py`(DB-aware `get_effective_role`·`assert_map_role` — 맵 부재=404, 권한 부족=403), `deps.py`(`require_map_role`/`require_version_map_role`/`require_comment_map_role` 의존성 팩토리). 게이트: GET /maps 가시성 필터(권한/승인자/직원 1회 로드 후 메모리 판정 — N+1 회피), POST /maps는 생성자에 owner MapPermission 행 + owner_id + visibility=private 세팅(본인 맵 잠금 방지), GET/{id}=viewer, PATCH=editor, DELETE=owner, PUT /graph=editor **+ 체크아웃 보유 강제**(권한과 별개 동시편집 규칙, sysadmin도 미보유 시 409), PATCH 버전 rename=editor, 댓글 create/resolve=viewer+. sysadmin은 effective_role에서 owner로 해석돼 게이트 통과(체크아웃 규칙 제외). 핵심 불변식: 기존 175 테스트는 auth OFF(전원 sysadmin)라 게이트 통과 — 단 PUT /graph 체크아웃 보유 요구로 깨진 시드들을 checkout-first로 수정(test_graph·test_subprocess·test_versions·test_collab 헬퍼). 신규 `test_permission_gates.py` 19개(auth_enabled=True + get_current_user 오버라이드로 is_sysadmin 차별화). 검증: ruff clean, pytest 194 pass(175 기존 + 19 신규), dev.db clean. `.git/sdd/perm-task-3-report.md`.
- **캔버스 회귀 픽스 ② — 아웃라인이 접힌 하위프로세스를 펼치기 가능으로 표시**. 아웃라인의 has-children은 "다른 노드가 이 노드를 parent로 가짐"인데, 접힌 하위프로세스는 `fullGraph`에 자식이 없어(임베드는 `hostsToEmbed`에만) childless로 보여 chevron 미표시 → 노드 우상단 버튼으로만 펼침 가능했다. 픽스: `buildOutline`에서 `nodeType==="subprocess"`를 항상 펼치기 대상으로(`hasChildren = parentsWithChildren.has(id) || nodeType==="subprocess"`). 아웃라인 행 펼침/접힘은 하위프로세스면 `toggleInlineExpandRef.current?.(id)`로 캔버스 인라인 임베드 토글(자식 로드) — `handleToggleExpand`(chevron)·`handleOutlineExpand/Collapse/Fold`(키보드) 모두, `isSubprocessRow` 판정. outline `effectiveExpanded`에 `expandedInline` 추가 → 임베드된 하위프로세스가 아웃라인에서 펼친 것으로·자식 표시. 일반 노드 동작 불변. 검증: pw-smoke-issue2-outline RED(어포던스無·임베드0)→GREEN(c-order/c-deliver chevron·클릭시 행펼침+자식4 임베드), task9·task7-drag 회귀 PASS, lint·tsc·build clean.
- **캔버스 회귀 픽스 ① — 펼침 영역 가로지른 루트 드래그 드롭 좌표 보정**. `buildScope`는 루트 노드의 표시 x를 "그 노드 왼쪽의 펼침 앵커들 footprint 합"만큼 민다(위치 의존 오프셋). `finalizeRootDrag`가 드래그 **시작** 오프셋(`dragStartOffsetRef`)으로 표시→저장 환산해, 드롭이 앵커 경계를 가로지르면 시작·드롭 오프셋이 달라 정확히 footprint(수백 flow)만큼 빗나갔다. 픽스: `inlineComposition`이 루트 스코프(depth 1) 앵커별 `rootShiftSteps={저장x,footprint}` 노출(x는 미쉬프트 원본). 순수헬퍼 `offsetAtX(savedX,steps)=Σfootprint(s.x<savedX)`. `finalizeRootDrag` 유효드롭에서 x를 `dropDisplay.x − offsetAtX(sx)` 고정점 풀이로 환산(단조 계단 ⇒ 앵커수 이내 수렴). y는 무관(`dropDisplay.y − offset.y` 유지), 무효드롭 취소 동일. 검증: pw-smoke-issue1-crossdrag RED(miss 165px·persist −916)→GREEN(miss 27px·persist −8), task7-drag 회귀 PASS(비가로지름 followOk·무효취소·영속), lint·tsc clean.
- **캔버스 회귀 픽스 ③ — obsolete "child" 드롭존 제거 + 하위프로세스 엣지 핸들 보정**. 참조 모델에선 하위프로세스 생성이 라이브러리 드래그 전용이라 드롭 링의 "child"(하위로 넣기) 타일은 dead(handleZoneDrop에 분기 없음) → `DropZone` 타입·`pickDropZone` 타일·page.tsx 오버레이 타일·`childAllowed`/`disabled` 로직·`CornerDownRight` import·`dropzone.child` i18n(en/ko) 전부 제거. 엣지 재배선: `applyFlowEdges`(insert)와 `swapNodes`가 변 기본 핸들(`s-right`/`t-left`)만 써 하위프로세스 끝점에 안 붙던 버그 → 순수헬퍼 `withSubprocessHandles(edge, isSubprocess)` 신설(target=subprocess→`in`, source=subprocess→`__primary__`, 끝점이 일반으로 바뀌면 stale 전용핸들 변기본값 리셋)을 두 경로에 적용. `onConnect`(수동 핸들드래그)·decision 분기라벨 source는 미변경. `buildGraph`/`toAppEdges` 핸들 라운드트립 확인. 검증: pw-smoke-issue3-dropzone RED→GREEN((i)child타일無 (ii)insert sh=__primary__ (iii)swap 위치+핸들 불변식), task9 회귀 PASS, lint·tsc clean.
- **하위프로세스 참조 모델 — 프론트엔드 Task 7(읽기전용 딥뷰 드릴인) + 펼침중 루트편집 버그 픽스 동시 완료**. (A) 딥뷰: `Scope`를 태그드 유니온(`root`|`sub{hostId,mapId,pinned,followLatest}`)으로, `currentParentId=sub?hostId:null`(렌더머신 단일 앵커 유지). `hostsToEmbed=expandedInline∪드릴경로 호스트` → 합성트리에 namespaced 자식 보장. 스코프 로드 분기: 루트=`getGraph`(편집), 딥뷰=**합성트리에서 host 자식 필터**(getResolvedGraph 재로드 금지 — 원본 id는 ancestorContext 체인에 앵커 못함)·읽기전용 마킹. `drillIntoSubprocess`(더블클릭, 임베드자식·루트 하위프로세스 양쪽)·`saveCurrentScope` 읽기전용 no-op(ref미러)·`navigateTo` 떠날 때 저장 스킵. `fullGraph` dep + `loadedScopeKeyRef` 가드로 딥뷰 자식 늦은 로드만 채우고 루트 재로딩 회귀 방지. (B) 버그: ① `nodesDraggable={!readOnly}`(펼침 게이트 제거) ② `rootOffsets`(표시−저장) 노출 + `unshiftRootPosition`이 루트 드래그 position 변경에서 빼 이중쉬프트 방지 ③ 선택은 표준 리듀서가 `nodes` state로 전달·displayNodes가 carry-through. (C) dead `isActive`/`activeScopeId`·ref·리셋 effect 제거. 검증: lint 0err·tsc·build clean. Playwright: `pw-repro-root-edit.mjs`가 "ROOT EDITABLE WHEN EXPANDED(no bug)"로 플립, `pw-smoke-task7.mjs` PASS(드릴인 검토노드 읽기전용·c-done 조상 dim 0.4·카메라 점프 없음·브레드크럼 복귀 편집재개·펼침유지·err 0), task5/6b/9 회귀 PASS. ⚠️ repro 스크립트도 수정(펼침 후 c-done이 화면밖→ctrl휠 줌아웃으로 도달, 통과조건 불변). net 단일파일. 보고서 `.git/sdd/task-7-report.md`.
- **🚩 체크포인트 — 하위프로세스 참조 모델 Plan 2: 9태스크 중 8 완료, Task 7만 남음(새 세션).** 완료(서브에이전트 TDD, 전부 review-clean·build clean·Playwright 실측 PASS): 백엔드 라이브러리 보강 / API·타입 / subprocess-embed 순수모듈 / 노드 렌더+동적핸들 / **합성트리 스왑(핵심: fullGraph 파생, 링크맵 resolved를 네임스페이스 parent_node_id로 임베드)** / 하위 편집경로 제거 / 뷰어 클릭선택 UX / 라이브러리 드래그 생성 / 다중출구 핸들+버전 배지·follow-latest. dev.db는 새 평면 픽스처(3맵). **남음: Task 7(읽기전용 딥뷰 드릴인 — 최고위험, 사용자가 새 세션으로 지정). 설계보정: 드릴인 nodes를 합성트리에서 파생(네임스페이스 id)해야 ancestorContext 앵커링 일치 — getResolvedGraph를 원본 id로 nodes에 직접 로드 금지.** 이후 최종 전체리뷰 + Plan 3(데모 재시드) → 머지. SDD 레저(`.git/sdd/progress.md`)에 resume 지점 기록. 25커밋, ⚠️ 미머지.
- **하위프로세스 참조 모델 — 프론트엔드 Task 9(다중출구 핸들 퍼시스트 + 버전 업데이트 배지/액션 + follow-latest 토글) 구현 완료**. `onConnect`는 기존 `...connection` 스프레드가 `sourceHandle`/`targetHandle`를 그대로 전달하므로 추가 변경 없음. 추가: (1) `listLibraryProcesses` 임포트·마운트 1회 fetch→`libraryList` state, (2) `libByMap` useMemo(map_id→LibraryProcess), (3) `injectSubEnds`에 `updateAvailable` 계산 주입(핀된 버전보다 최신 발행본 있으면 true), (4) `handleUpdateSubprocess` 콜백(최신 버전id 갱신→resolved 재fetch→subEnds 재파생→끊어진 보조출구 엣지 감지+toast→setNodes+autosave), (5) 인스펙터에 subprocess 전용 블록(follow-latest 체크박스 + "Update to latest" 버튼, updateAvailable 시 표시), (6) i18n en/ko 3키(`subprocess.update`·`subprocess.followLatest`·`subprocess.endRebindWarn`). 검증: lint 0err, tsc clean, build clean. Playwright: pw-smoke-task9 PASS(nodeFound·checkboxFound·checkboxToggled·roundtripOk·consoleErrors 0), pw-smoke-task5 regression PASS. 업데이트 배지는 seed 최신발행본=핀버전이라 기본 비표시 — 더 새 버전 발행 시 노출 확인 가능. `scripts/pw-smoke-task9.mjs`.
- **하위프로세스 참조 모델 — 프론트엔드 Task 6(하위 편집 경로 제거) 구현 완료**. 편집은 루트 전용, 임베드 자식은 읽기전용. 제거 항목: `runCreateSubprocess`/`createSubprocess`/`createChildEdge`/`addChildNode`/`saveChildScopeAfterDelete`/`saveChildScopeDragBatch`/`flushChildScopeSaves`/`patchChildNode`/`renameChildNode`/`handleCreateEndForSubprocess`/`handleSubprocessPick`/`confirmDeleteSubprocess` 12콜백, 상태(`draggingChildIds`/`subprocessPrompt`/`pendingSubprocessPick`/`deleteInvariantPrompt`/`childEdits`) 5개, refs(`childEditsRef`/`dirtyChildScopesRef`/`childSaveTimerRef`) 3개, 모달 3개. `handleNodesDelete`에서 자식 스코프 save 브랜치 제거. `onConnect`에서 자식 간 엣지 생성 브랜치 제거. `displayNodes` 자식 노드는 항상 `draggable:false, deletable:false, connectable:false, selectable:true`. `inlineComposition` kidApp에서 `childEdits` 오버레이 제거(`data: app.data`). `onBeforeDelete` 불변식 가드 제거. `api.ts`: `getGraph`/`saveGraph`의 `parentId` 옵션 파라미터 제거, `aiChat`의 `parent` 파라미터 제거 → `ai-chat-panel.tsx` 콜사이트 동기화. `inline-expand.ts`: `checkScopeInvariant`/`splitByScope` 제거. 검증: tsc·lint·build clean. Playwright(task5 embed regression: 펼침→접수·검토 등장 PASS, task6: 루트 draggable·자식 selectable-not-draggable PASS). `scripts/pw-smoke-task6.mjs`. net -751줄.
- **하위프로세스 참조 모델 — 프론트엔드 Task 5(합성 트리 스왑) 구현 완료**. `fullGraph`를 state→**파생(useMemo)**으로 전환: 루트 평면 그래프(`rootGraph`=getFullGraph, parent_node_id null) + 링크맵 resolved 캐시(`resolvedCache`, linkKey별)를 `buildCompositeTree`로 합성 → 기존 모든 소비자(materialize·inlineComposition·ancestorContext·펼침한도) 무변경. 선로드 effect가 보이는 subprocess의 resolved를 eager fetch(캐시/in-flight 가드, 중첩 수렴)해 펼치기 전 `subEnds`/핸들을 채움. `injectSubEnds`(deriveSubEnds)를 3곳(materialize·kidApp·displayNodes) 주입 — Task4 펼침토글/끝핸들 게이트 활성. 구 `setFullGraph` 8곳은 `setRootGraph`로 리네임(자식편집 경로는 다음 태스크서 제거, 현재는 컴파일·무해). 검증: lint·tsc·build clean + Playwright(map 3 "주문 처리" 펼침 → 임베드 자식 접수·검토 등장, subEnds 핸들 source 2개 선로드, console err 0) PASS. `scripts/pw-smoke-task5.mjs`.
- **하위프로세스 참조 모델 — 프론트엔드(Plan 2/3) 구현계획 작성**. 핵심 아키텍처: **합성 트리(compositeTree)** — 기존 렌더 폴리시(buildScope·ancestorContext·게이트웨이)가 전부 `parent_node_id` 트리를 읽으므로, 링크맵 resolved 그래프를 네임스페이스 id + 합성 `parent_node_id`로 끼워 구조 동일한 트리를 만들어 먹임 → 레이아웃 수학 무변경, 데이터 소스만 교체. 9태스크: 라이브러리 list 보강(발행본·refs)·API/타입·subprocess-embed 순수모듈·노드 렌더+동적 끝핸들·합성트리 스왑·하위편집 제거·읽기전용 딥뷰(조상감싸기/카메라 재활용)·라이브러리 드래그·다중출구 핸들+버전갱신 배지. 결정(이번 세션): 읽기전용 딥뷰 유지(ancestorContextNodes/focusCamRef 삭제 아닌 읽기전용 재활용). `docs/superpowers/plans/2026-06-21-subprocess-reference-frontend.md`. 프론트 테스트 러너 없음 → 순수로직 tsx 새너티, 배선 lint+tsc+build+Playwright. ⚠️ Plan 2 완료까지 미머지(현 프론트 비호환).
- **하위프로세스 참조 모델 — 백엔드(Plan 1/3) 구현 완료**(서브에이전트 주도 TDD). 노드 평면화(`parent_node_id` 제거)·subprocess 참조/대표끝/엣지핸들 필드·그래프 라우터 평면화·프로세스 검증(시작1·끝이름유니크·대표끝1)·순환 참조 차단(직접+간접 BFS)·프로세스 라이브러리/링크 해석 API. 발견·수정한 실버그: 노드 update 경로가 subprocess 필드 미저장(insert만). 10커밋, 12파일 +438/-208, **120 tests pass·ruff clean**, 최종 전체리뷰 READY. 다음: 프론트 Plan 2(읽기전용 인라인 임베드·렌더 폴리시 유지·편집 제거·라이브러리 드래그)·Plan 3(데모 재시드). ⚠️ 백엔드만 바뀌어 현 프론트와 비호환 — Plan 2 전엔 미머지.
- **하위프로세스 참조 모델(Call Activity) 설계문서 작성**(방향 전환). 인라인 계층 편집(버그 원천) 제거 → 루트에서만 편집, 하위는 다른 프로세스를 링크해 읽기전용 펼침. 결정: 클린 브레이크·버전 고정+알림+자동추종 토글·대표끝(안정)+나머지끝(best-effort)·라이브러리 드래그 생성·인라인 읽기전용(렌더 폴리시 유지). `docs/superpowers/specs/2026-06-20-subprocess-reference-model-design.md`. 백엔드 구현계획 작성됨(`docs/superpowers/plans/2026-06-20-subprocess-reference-backend.md`, Plan 1/3 — 스키마 평면화·검증·순환·라이브러리/해석 API, TDD 6태스크). 다음: 실행 → 프론트 Plan 2·3.
- **포커스 시 조상이 활성 영역을 "감싸도록" — 편집 뷰 = 모두 펼친 뷰**(사용자 /goal, 선택: Path 2 유지). 근본 원인: 깊은 스코프로 드릴인하면 `ancestorContextNodes`가 조상을 **compact(좌측 누적)**로 재배치 → 펼친 뷰의 "감싸기"(im-be→[백엔드]→im-end)가 무너져 노드가 왼쪽으로 몰림. **수정:** ancestorContextNodes를 재귀 감싸기로 재작성 — 부모 스코프의 포커스 노드 자리를 활성 영역으로 치환(앞 형제=왼쪽, 뒤 형제=오른쪽), 그 범위를 다음 상위 조상의 활성 영역으로. `ANCESTOR_CONTEXT_GAP` 제거. Playwright(구현 포커스 깊이2): 조상 개발 dv-integ(1196)·dv-end(1278) **>** 활성 im-end(1140)=오른쪽 감쌈, dv-impl(799) **<** im-start(881)=왼쪽 → WRAP PASS. tsc/eslint green.
- **권한 관리 Phase 3 전체 완료 (Task 3.0~3.3) + 언어 기본값 ko.** 3.0 `DEFAULT_LANG ko`(독립 커밋). 3.1 유저그룹(`/groups`·`/groups/[groupId]` — 생성요청 누구나/pending, 상세 멤버·관리자 편집=관리자·sysadmin·active만, top-nav 링크). 3.2 sysadmin 콘솔(`/admin/permissions` — isSysadmin 게이트, 통합 승인 큐[그룹생성+권한하향+가시성, owner이양·version 제외], 부서 테이블 org 디버그 토글[가변 깊이], 사용자 목록 read-only). 3.3 per-map 결재 surface(settings "결재 대기" 탭 — 맵 승인자/sysadmin이 ③④ decideRequest+toast) + mock 알림은 ToastStack/배지로 한정(실제 NotificationBell은 백엔드라 무변경). SDD Task별 implementer+reviewer, Important/제약위반 fix+재리뷰. lint/tsc/build green(10 routes). **STOP — Phase 3 종료, whole-branch 최종 리뷰 진행 여부 확인.** 미해결(최종 리뷰 트리아지): 브라우저 런타임 재검증·일부 컴포넌트 소소한 중복/타입 폭·create-form key(type:id).
- **권한 관리 Phase 3 Task 3.2 완료 — sysadmin 콘솔 (§7.4)** — 신규 라우트 `/admin/permissions`(기존 `/admin` 무변경). 게이트: `useCurrentMockUser().isSysadmin` false → "권한 없음(시스템 관리자 전용)" 노티스. 3탭(승인 큐·부서·사용자). 승인 큐: 그룹 생성 대기(`state.groups` status=pending) + 권한·가시성 요청(`state.requests` status=pending, `version_publish` 제외 — versionFlow 전용) 통합, 각 행 승인/반려 버튼→`decideGroup`/`decideRequest`+toast. 부서 테이블: 기본 부서명·코드, 디버그 토글 시 `orgLevels` 최대 깊이 **동적 계산**(`Math.max`로 max 런타임 산출 — 절대 하드코딩 없음) 후 Org1…OrgN 컬럼 자동 생성. 사용자 목록: 읽기전용, `isSysadmin` → 태그(env-managed 표기, 토글 없음). top-nav 드롭다운에 `mockUser.isSysadmin` 조건부 "시스템 관리" 링크 추가. i18n en+ko `perm.sysadmin.*` 32개 키. tsc/lint/build green(새 라우트 `/admin/permissions` 빌드 출력 확인).
- **Pre-Phase3 S5 완료 — 승인자 암묵 viewer 판정 (설계 §5)** — `permissions-logic.ts`에 `isApprover` 헬퍼 추가(활성 상태 무관 승인자 목록 확인); `getEffectiveRole` 마지막 단계에 §5 floor 추가(null일 때만 viewer 올림, 상위 역할 하향 없음); `isVisibleToUser`는 기존 `getEffectiveRole !== null` 유지(승인자는 floor 경유로 커버). sanity 10개 all pass(신규: approver-only user.jung→viewer; 회귀: user.jung base null, user.park viewer, admin.kim owner, user.lee editor not downgraded). tsc/lint/build green.
- **권한 관리 Phase 2 전체 완료 (Task 2.1~2.4) — 맵 권한 UI 일습.** 권한 탭(`/maps/[mapId]/settings`: 협업자·결재자·공개범위·위험구역·버전), 생성 다이얼로그(visibility·초기 협업자·필수 승인자, 실제 createMap+mock 오버레이, 홈 isVisibleToUser 필터), 버전게시 mock 상태기계, 승인자 재지정 강제 모달. SDD Task별 implementer+reviewer, Important/제약위반은 fix+재리뷰 통과. lint/tsc/build green(7 routes). **STOP — Phase 3(그룹·sysadmin) 사용자 확인 대기.** 미해결: 브라우저 런타임 검증(사용자 단계 보류)·설계 §5 "승인자 암묵 viewer"는 getEffectiveRole 미구현(기록)·최종 whole-branch 리뷰는 브랜치 완료 시. 세부는 아래 Task별 항목 참조.
- **권한 관리 Phase 2 Task 2.4 완료 — 승인자 재지정 강제 모달** — (1) `reassign-approver-modal.tsx` 신규: 소유자가 활성 승인자 0명일 때 닫기 불가 오버레이 모달. `ModalBackdrop onClose={() => {}}` no-op으로 백드롭 클릭 비닫기, X 버튼 없음. 픽커는 활성 유저 중 미지정자만 표시 → 확인 시 `setApprovers(mapId, [...기존, newId], by)`. 부모 파생 조건이 false가 되면 자동 언마운트(강제 닫기). (2) `settings/page.tsx`: `getActiveApprovers` import 추가, `showForcedReassign = isOwner && currentMockUser !== null && getActiveApprovers(permState, mapIdStr).length === 0` 조건으로 탭 무관 페이지 레벨 렌더. 비소유자 미표시. (3) i18n `perm.reassign.*` 4개 키 en+ko 추가. tsc/lint/build green.
- **권한 관리 Phase 2 Task 2.3 완료 — 버전 게시 mock 탭 + versionFlow 승인/반려/게시 전환** — (1) `permissions-store.ts`: `VersionFlowEntry`에 `approvedBy?`·`publishedBy?` 선택 필드 추가; `approveVersionPublish(vid, by)` pending→approved, `rejectVersionPublish(vid, by)` pending→rejected, `publishVersionFlow(vid, by)` approved→published 세 액션 추가(전이 guard 포함). (2) `components/permissions/versions-publish-panel.tsx` 신규: 버전별 mock 상태 표시(`versionFlow[vid]?.status ?? 'draft'`) + 역할·상태별 버튼 — editor+: draft/rejected→[승인 요청]; 승인자: pending→[승인]/[반려]; 요청자만: approved→[게시]; viewer: 읽기전용. 실제 api.ts submitVersion/approveVersion/publishVersion 미호출. (3) `settings/page.tsx`: TabId·Tab.labelKey·TABS에 `versions` 탭 추가, VersionsPublishPanel 렌더. (4) i18n en+ko `perm.tabVersions`·`perm.version.*` 8개 키 추가. tsc/lint/build green. 단일 승인자 mock 단순화(설계 §4①은 만장일치지만 mock은 첫 승인자 단독 처리). 실제 VersionSummary.status 무시 — mock versionFlow 우선.
- **권한 관리(Permission Management) UI-first 설계 확정**(`feat/permission-management-ui`, 워크트리·`origin/main`이 아닌 캔버스 브랜치 기준). 코드 대조로 스코프 확정 — 버전게시 승인(흐름①)은 기존 실제 구현이나 미사용 → mock 신규(§7.5는 에디터 대신 settings에 배치), 맵 승인자 mock 통일, 실제 맵 리스트 + mapId-keyed mock 권한 오버레이, i18n는 기존 en+ko 따름. 백엔드·에디터(`maps/[mapId]/page.tsx`) 무변경. 3단계(Phase1 mock 기반→Phase2 맵 권한 UI→Phase3 그룹·sysadmin). 설계 `docs/superpowers/specs/2026-06-20-permission-management-design.md`, 구현 계획 `docs/superpowers/plans/2026-06-20-permission-management-ui.md`(Phase1 종료 시 stop-and-confirm).
- **권한 관리 Phase 2 Task 2.1a 완료** — (1) `permissions-store.ts` changeRole/removeCollaborator not-found guard 추가(존재하지 않는 권한 변경·제거 no-op 리턴, Phase 1 리뷰 minor 해소). (2) `maps/[mapId]/settings/page.tsx` 신규 라우트: 탭 셸(협업자·결재자·공개범위·위험구역), 맵 이름(getMap), 역할 게이팅(editor+ 편집/viewer 읽기전용/null 권한없음), Dev 유저 전환 드롭다운. (3) `components/permissions/` 3파일 신규: `role-badge.tsx`(역할·승인대기 pill), `principal-picker.tsx`(초성 포함 사용자/부서/그룹 검색), `collaborators-panel.tsx`(목록·역할변경·제거·추가, gated→승인대기 badge+toast). (4) i18n 26개 `perm.*` 키 en+ko 모두 추가. tsc/lint/build green. 새 라우트 `/maps/[mapId]/settings` 빌드 출력에 확인.
- **권한 관리 Phase 2 Task 2.1b 완료 — 권한 탭 전체 완성** — settings 탭의 나머지 3섹션 + 신규 store 액션 `deleteMapMock`. (1) `approvers-panel.tsx`: 결재자 목록·추가(사용자만)·제거(`setApprovers`), 비활성 표시, **활성 0 경고 배너**(`getActiveApprovers`), 비활성 토글 affordance. (2) `visibility-control.tsx`: owner 전용 public/private 토글 → `requestVisibilityChange`(즉시 미적용, pending+「승인 대기」), public이면 collaborators의 viewer grant 비활성(`viewerGrantDisabled` prop). (3) `danger-zone.tsx`: 오너 이양(editor+ 선택→확인 모달 1회→`transferOwner`, 원 owner editor 강등·owner 1인 불변식)·맵 삭제(**mock 전용 `deleteMapMock`, 실제 백엔드 deleteMap 미호출**→ 홈 리다이렉트). Approvers/Visibility/Danger는 owner(=sysadmin 포함) 전용, 비소유자 읽기전용/숨김. i18n en+ko. tsc/lint/build green. **STOP — 사용자 브라우저 검증 대기**(npm run dev). 한계: deleteMapMock 후에도 sysadmin은 owner-override로 실제 맵을 계속 봄(mock 한계, 의도).
- **권한 관리 Phase 1(mock 레이어) 구현 완료** — `frontend/src/lib/mock/`에 타입(`permissions-types`)·시드(`permissions-seed` `buildSeed`)·순수 판정함수(`permissions-logic` getEffectiveRole/isVisibleToUser/requiresDowngradeApproval 등, 전이적 그룹·부서 상속)·in-memory store(`permissions-store` 불변갱신+pub-sub+resetPermissions, 액션 다수)·배럴(`permissions`)·현재유저 매핑(`current-mock-user`) 추가. dev-auth user.choi 부서를 seed와 정합(프로세스혁신팀). 화면 없음. 검증: 순수함수 새너티(npx tsx, addCollaborator/changeRole 게이트/decideRequest/transferOwner) + lint·tsc·build green. SDD(서브에이전트) 방식, Task별 리뷰 통과. Phase 2(맵 권한 UI)는 사용자 확인 후 진행.

- **복잡한 깊이4 테스트 맵에 그룹·속성·디스크립션·분기 추가**(사용자 요청 후속). `scripts/seed_complex_demo.py` 확장 — 전 노드 description + 속성(담당자/부서/시스템/소요시간), 그룹 2개(루트 "핵심 개발"=기획·개발 / 개발 스코프 "엔지니어링"=설계·구현, `group_ids` JSON), 검증 스코프에 **decision 분기(품질 판정 마름모) + 엣지 라벨(통과/결함/재검증) + 핸들 변(source/target side) + 결함 수정 루프백**(y=420 분기 배치), 정돈된 좌표. 33노드/27엣지/2그룹. Playwright(map 2): 그룹 라벨·담당자 카드·description·decision·rework·엣지라벨 전부 렌더 확인.

## 2026-06-20 (이전)
- **복잡한 깊이4 테스트 맵 추가**(사용자 요청). `scripts/seed_complex_demo.py` — "제품 개발 프로세스"(map 2). 루트에 하위프로세스 보유 노드 3개(기획·개발·검증), **개발→구현→백엔드→DB설계로 깊이 4까지** 중첩. 기존 데모 맵(map 1)은 보존(같은 이름만 멱등 재생성). Playwright(map 2): 루트 하위보유 3개 확인, 깊이4 사슬 펼침(db-schema까지) 확인. dev.db 갱신 커밋.
- **자식 클릭 포커스 시 펼쳐진 레이아웃이 왼쪽으로 붕괴하던 버그 수정**(사용자 피드백 — "좌우로 찢어져야할 노드가 왼쪽 정렬"). 중첩 펼침(심사 안에 승인 펼침) 상태에서 심사를 클릭 포커스하면 `navigateTo`의 스코프 로드 효과가 `setExpandedInline(new Set())`로 승인 펼침을 리셋 → 심사 종료가 far-right(2635)에서 compact(1594)로 붕괴. **수정:** 자식 클릭 포커스(`focusCamRef` 설정됨)일 땐 **새 스코프 하위의 펼침을 유지**(부모 체인이 currentParentId를 포함하는 펼침만 필터해 보존). 브레드크럼/검색(focusCamRef 없음)은 기존대로 전체 리셋. childNodes는 expandedInline→materialize 효과가 자동 동기화. Playwright: 중첩펼침→심사포커스 시 s-end **2635 유지**(spread 1699 유지), s-doc op 0.4→1(포커스됨), a-first 존재(승인 유지), breadcrumb 심사. tsc/eslint green.
- **포커스 중 인라인 펼침 시 레인·조상 컨텍스트 깨지던 버그 수정**(사용자 피드백, Image 8). 깊이1 포커스 상태에서 하위(승인)를 펼치면 ① 깊이1 레인이 펼친 끝(밀려난 심사 종료·자식)을 못 따라감 ② 깊이0(루트) 컨텍스트가 통째로 사라짐 ③ 깊이2 틴트가 깊이1 레인 밖이라 안 겹침. **근본 원인:** (a) `focusScopeLanes` 현재 스코프 레인이 raw `nodes`(펼침 전) 기준 → 표시(`inlineComposition.nodes`) 기준으로 변경, (b) `ancestorContextNodes`가 `inlineComposition` 있으면 즉시 `[]` 반환 → 가드 제거 + 앵커를 표시 위치 기준으로. Playwright(depth1+승인펼침): 심사 레인 band r=1707→**2748**(s-end 2717 포함), 루트 r-* 5개 op=0.4로 복귀, 승인 영역(1574-2371)이 심사 레인(910-2748) 안에 중첩. tsc/eslint green. 좌상단 고정 제거는 사용자 만족(유지).
- **캔버스 좌상단 고정 효과 제거**(사용자 요청 — 아웃라인 클릭 네비게이션이 있어 불필요). `contentExtent`의 비대칭(좌상단 바짝 + 우하단 minZoom 확장 → 줌아웃해도 centering 방지)을 **대칭 여백(EXTENT_MARGIN 사방 동일)**으로 교체 → 줌아웃 시 기본 centering 허용. Playwright: 21% 줌아웃 시 콘텐츠 중심 cx=752(뷰포트 중앙≈760)로 가운데 수축(좌상단 아님), focus-in 0px 유지(회귀 없음). 프레이밍 헬퍼(fitScopeTopLeft·frameScopeTopLeftKeepZoom)는 그대로 — 이상하면 후속. tsc/eslint green.
- **드래그 시 브라우저 텍스트 선택 UI 오류 제거 + 포커스 레인 틴트 통일**(사용자 피드백). ① Issue 1: 박스선택 드래그가 노드 라벨·아웃라인 텍스트를 파랗게 선택(매우 거슬림)했음 — 원인은 `user-select`가 캔버스(pane)에서 "auto"라 드래그가 텍스트 선택을 유발. 캔버스 컨테이너(`canvasContainerRef`)에 `select-none` + globals.css에 입력창(`input/textarea/contenteditable`) 예외 추가. Playwright: 드래그 중 `getSelection()`="" · pane userSelect=none 확인. ② Issue 2: 깊이별로 같은 펼침이 다르게 보이던 것 — InlineRegionBands(인라인 펼침)는 틴트 5%인데 FocusScopeBands(포커스)는 depth×6%였음 → FocusScopeBands도 **flat 5%로 통일**. **셰브론도 통일**(사용자 선택: 활성=또렷 유지 + 셰브론 통일): FocusScopeBands에 "›×절대깊이 + 스코프이름" 라벨 추가(InlineRegionBands와 동일 위치·스타일, 비상호작용), 양쪽 셰브론을 **절대깊이(루트 기준)**로 맞춤(InlineRegionBands에 `baseDepth=currentScopeDepth` 전달 → `›×(baseDepth+box.depth)`). Playwright: 깊이0 인라인·깊이2 포커스 셰브론 둘 다 [››,›], 깊이1 포커스 "› 심사" 렌더 확인. 불투명도(활성 밝음/비활성 0.4)는 포커스 지표라 의도 유지. **후속(사용자 피드백): 셰브론 라벨이 첫 노드와 겹치던 것 수정** — FocusScopeBands 라벨 y를 `top+4`(노드 높이) → `top-26`(노드 위)로. Playwright: "›심사" t=468 < s-start t=492(겹침 없음) 확인. tsc/eslint green.
- **포커스 exit 제자리(통합 카메라 보정) + 깊이별 중첩 레인**(사용자 피드백). ① exit 갭: 클릭한 노드의 "현재 표시 위치 − 저장(스코프상대) 위치"를 카메라 shift로 써 **focus-in·exit 둘 다 그 노드가 제자리에 남게** 통합(onNodeClick/dblclick). focus-in은 0px 검증, exit는 동일 공식. ② 깊이2에서 깊이1 틴트가 사라지던 버그: `focusScopeBounds`(단일)→`focusScopeLanes`(배열)로 — **현재 스코프 + 보이는 조상 스코프(깊이≥1) 각각** 레인+깊이 틴트 렌더(루트=틴트 없음). Playwright: 깊이1 레인 1개·깊이2 레인 2개(심사+승인) 확인. **검증 함정 기록:** 이전 세션 좀비 프론트가 포트 3000 점유→테스트가 구버전/오염 dev.db에 붙어 거짓 500·실패. `pkill -9 -f 'next dev|uvicorn app.main'` 전수 정리 + 클린 1쌍 재기동 필수. tsc/eslint green.
- **포커스 모드 Path 2 — 활성 자식을 "진짜 nodes"로(제자리·무카메라, 풀 편집 공짜)** (사용자 선택). 자식 클릭 시 `navigateTo(buildScopesTo(scopeId))`로 그 스코프를 실제 `nodes`化(currentParentId 전환)하되, **카메라를 `scopeOffsets×zoom`만큼 한 번 이동**(`focusCamRef` → 스코프 로드 효과에서 `setViewport`)해 자식이 레인 자리에 그대로 나타나게(시각적 무이동). 자식이 `nodes`라 **그룹·다중선택·정렬·드롭존·드래그가 전부 네이티브**(연산별 포팅 불필요). `onNodeClick`/raw-dblclick을 navigateTo+카메라보정으로, `focusScopeBounds`/lane은 currentParentId 기준으로 복원, `buildScopesTo` 재추가. Playwright: 자식 클릭 시 s-doc 화면 이동 **0px**·프레임 선택 가능·박스선택 2·그룹(untitled) 생성 PASS. **함정 기록:** 좀비 프론트(이전 세션 PID가 3000 점유→새 프론트 3001로 떠 테스트가 구버전에 붙음)로 거짓 500/실패가 났었음 — `pkill -f 'next dev'`로 정리 후 정상 확인. 남은 갭: 루트 복귀(exit) 카메라 역보정은 후속. tsc/eslint green.
- **자식 다중 선택 드래그 시 1개만 이동하던 버그 수정**(사용자 보고). 인라인 자식 드래그가 잡은 1개만 `draggingChildIds`에 넣어 나머지 선택 자식은 표시상 고정·드롭 후 원복됐음. `onNodeDragStart`에서 같은 스코프의 **선택된 자식 전부**를 draggingChildIds에 추가·파생위치 동기화, `onNodeDragStop`에서 **전부를 한 번의 getGraph→PUT로 배치 저장**(`saveChildScopeDragBatch`, 같은 스코프 다중 PUT 레이스 방지). Playwright(상대좌표): 자식 2개 박스선택 드래그 시 둘 다 이동 PASS(수정 전엔 1개만). 진단: 세션 read-only(체크아웃 락)는 별개 — 커밋 dev.db는 락 없음. tsc/eslint green.
- **포커스 모드 A안 — 제자리 토글(navigateTo/카메라 제거)** (사용자 "줌·네비 없이 노드 위치 고정, 활성↔비활성 구역만 토글"). 클릭 시 navigateTo(캔버스 교체)로 위치/카메라가 바뀌던 것을, **`activeScopeId` 상태 토글**로 전환 — 인라인 펼침 합성은 그대로(위치 고정), 클릭한 스코프만 활성(불투명·편집)·나머지 dim. displayNodes가 `node.data.scopeId === activeScopeId`로 활성/비활성 게이팅(자식·프레임 공통), onNodeClick/raw-dblclick은 navigateTo 대신 `setActiveScopeId`. 활성 스코프 진입 시 카메라/위치 불변(scope-load 효과 미발화). `focusScopeBounds`를 activeScopeId 기준으로(레인+깊이 틴트), 접히면 현재 스코프 복귀 효과. `buildScopesTo` 제거(미사용). Playwright: 클릭 시 viewport 불변(-25,184,0.914)·s-doc 위치 불변(1155,511)·opacity 토글(0.4↔1)·활성 자식 드래그 PASS. (편집은 기존 인라인 scope-split — 드롭존/그룹/정렬은 후속 B) tsc/eslint green.

## 2026-06-19
- **포커스 레인 틴트로 변경**(사용자 "세로선 바깥은 깊이0 바탕") — 깊이 틴트를 캔버스 전체 배경에서 **세로선 사이(활성 스코프 레인)만**으로 한정. `FocusScopeBands`가 좌우 세로선 + 그 사이만 `color-mix(accent, depth×6%, transparent)` 틴트, 바깥은 부모(깊이0) `bg-canvas`. 전체 배경 div 틴트 제거. Playwright 스크린샷: 레인 안 틴트·오른쪽 바깥 무틴트 확인. tsc/eslint green. (다음: 사용자 요청 "줌·네비게이션 없이 노드 위치 고정, 활성↔비활성만 토글" = 제자리 포커스 B안 논의.)
- **포커스 스코프 세로 경계선 추가**(사용자 "세로선 경계와 함께") — 포커스(navigateTo) 스코프를 "무한 캔버스 속 레인"으로 보이게 활성 스코프 좌우에 세로 경계선 2개(전체 높이). `FocusScopeBands`(신규, `useViewport` 구독 — 인라인 펼침 `InlineRegionBands`와 동일 언어, accent 35% 1.5px) + `focusScopeBounds` memo(currentParentId≠null일 때 nodes bbox±REGION_PAD). 배경 깊이 틴트와 함께 표시. Playwright 스크린샷: 심사 스코프 좌우 세로선+틴트 확인. tsc/eslint green.
- **포커스 전환·뷰포트 재정비(사용자 피드백)** — ① 전환 시 줌이 커졌다작았다 하던 것 제거: 스코프 전환을 `fitView`(줌 변경)에서 `frameScopeTopLeftKeepZoom`(**현재 줌 유지** + 콘텐츠 좌상단 정렬)로 교체. ② **좌상단 고정 복원**: translateExtent를 대칭에서 다시 비대칭(좌상단 tight `EXTENT_TOPLEFT_MARGIN` + 우하단 pane/MIN_ZOOM 가드)으로. ③ **fit 버튼 스냅 제거**: 기본 fitView(가운데+스냅) 대신 좌상단 정렬 커스텀(`fitScopeTopLeft`, `ControlButton`+`showFitView={false}`) — 콘텐츠를 extent 좌상단 경계에 맞춰 setViewport라 비대칭 extent 안에 들어와 스냅 안 남. ④ **깊이별 배경 틴트 복원**(사용자 "처음처럼 유지"). Playwright: 전환 줌 0.914→0.914 불변·좌상단 정렬·틴트 root↔depth1 구분·fit 후 20px 팬 delta(20,0) 스냅없음 PASS. **남은 항목: 포커스(navigateTo) 스코프에 세로 경계선 — 현재는 인라인 펼침에만 있어 사용자 확인 중.** tsc/eslint green.
- **포커스 전환 부드럽게(A안) — 스코프 진입/이탈 시 카메라 애니메이션**(사용자 피드백 "화면이 갑자기 변하지 않게, 부드럽게 이동"). 기존엔 클릭 활성화 시 카메라 이동 없이 콘텐츠만 제자리 교체돼 시야가 길을 잃었음. 스코프 로드 효과에 `prevScopeRef`로 전환 감지(첫 로드/버전변경 제외) → 검색 점프가 아니면 새 스코프 노드로 `fitView({duration:600, maxZoom:1.2, padding:0.3})` 부드럽게. Playwright: 심사 진입 시 카메라 949px 이동·중간 프레임이 양끝과 달라 애니메이션 확인, 루트 이탈도 동일. (B안 "제자리 배치"는 후속 — 활성 스코프를 영역 자리에 렌더해 이동 거리 최소화) tsc/eslint green.
- **루트(깊이0) 복귀 버그 수정 + 깊이별 배경 틴트 제거**(사용자 피드백). ① 자식 스코프에서 루트 노드를 클릭해도 루트로 못 나가던 버그: 루트 노드는 `scopeId=null`인데 클릭 핸들러가 `scopeId!==null`일 때만 navigateTo해서 막혔음 → `buildScopesTo(null)`이 루트 체인 반환하도록 하고 onNodeClick·dblclick의 null 가드 제거(이제 루트 노드 클릭=루트 복귀). Playwright: 깊이1에서 r-intake 클릭→루트 복귀(dim 노드 0) PASS. ② 깊이별 배경 틴트는 "전체뷰 느낌 유지·배경 균일" 요구로 제거(무한 캔버스 느낌). 다음: 포커스 전환 시 활성 스코프가 제자리(영역 위치)에 나타나 시야가 점프 않도록 "제자리 포커스+부드러운 전환" 방향 논의 중(사용자에 현재 상태 스크린샷 공유). tsc/eslint green.
- **fit 버튼 후 살짝 팬하면 좌상단으로 순간 스냅하던 버그 수정**(사용자 피드백). 원인: translateExtent의 비대칭 좌상단 고정(`EXTENT_TOPLEFT_MARGIN` 120)이 fitView 가운데 정렬 위치를 좌상단 여백 밖에 두어, 팬 시 d3 constrain이 스냅(실측: 20px 팬에 y 276px 점프). 수정: translateExtent를 좌상단 고정 대신 **사방 대칭 여백 `max(EXTENT_MARGIN, pane/MIN_ZOOM)`**로(줌아웃 centering 가드는 유지 — 뷰포트≤extent). `EXTENT_TOPLEFT_MARGIN` 제거. Playwright: 수정 후 20px 팬→(20,10)만 이동, 스냅 없음. 무한 캔버스처럼 자유 패닝 느낌에도 부합. tsc/eslint green.
- **포커스 모드 Step 4(부분) — 자식 더블클릭을 스코프 활성화로 통일** (`feat/active-scope-focus-mode`). 비활성 스코프(인라인 자식·조상) 노드의 raw dblclick 캡처가 옛 요약 모달(`setSummaryNodeId`)을 열던 것을, 단일클릭(Step 3b)과 동일하게 `navigateTo(buildScopesTo(scopeId))`로 그 스코프를 활성화하도록 변경(deps에 navigateTo·buildScopesTo 추가). 현재-스코프 더블클릭 경로(RF `onNodeDoubleClick`)는 무변경 → 회귀 없음. Playwright: 자식 더블클릭 시 s-doc opacity 0.4→1 활성화·옛 모달 안 뜸 PASS. 잔여(인라인 자식 이동/삭제/연결/추가 라우팅)는 Step 1에서 이미 비활성(읽기전용)이라 inert — 대량 dead-code 제거는 회귀 위험 있어 보류(선택적 후속). tsc/eslint green.
- **편집 스코프 깊이별 배경 틴트** (사용자 피드백 "어느 단계든 배경이 늘 같아 지금 몇 단계 하위인지 인식이 안 됨"). 캔버스 콘텐츠 배경을 `activeIndex>0`이면 `color-mix(accent, min(depth×6,30)%, canvas)`로 — 루트=무틴트, 깊을수록 진해져 단계 구분(dot-grid 유지, 인라인 영역과 동일한 accent 깊이 틴트 언어, raw hex 없이 토큰만). Playwright 실측: 루트 rgb(246,246,248) → 깊이1 → 깊이2로 배경 색 단계별 구분 PASS. tsc/eslint green.
- 아웃라인 행 클릭 시 **캔버스가 매번 줌(1.3)·재중심으로 점프하던 것 개선**(사용자 피드백 "클릭하면 왼쪽위로 맞추며 끊김"). `handleOutlineSelect` 같은-스코프 분기의 `fitView(maxZoom:1.3)`를 `revealNodeIfOffscreen`로 교체 — 노드가 화면 안(여유 48px)에 보이면 **이동 없음**, 화면 밖/가장자리일 때만 **현재 줌 유지한 채 `setCenter`로 부드럽게**(줌 강제 변경 제거). Playwright 실측: 보이는 노드 5개 연속 클릭 시 viewport 불변(줌·x·y 그대로)·선택 정상, 가장자리 노드는 줌 유지한 채 팬됨. tsc/eslint green.
- **포커스 모드 Step 3b — dim 자식 클릭 → 스코프 활성화(navigateTo)** (`feat/active-scope-focus-mode`). 비활성 스코프(인라인 자식·조상 컨텍스트) 노드 클릭 시 그 스코프를 활성 `nodes`로 전환 → 드롭존·그룹·정렬 등 전부 네이티브 편집(plan 핵심 골). `buildScopesTo(nodeId)`(루트→…→노드 스코프 체인, 검색 내비와 동일 패턴) 추가, `onNodeClick`의 "비현재스코프=무시" 분기를 `scopeId≠null이면 navigateTo(buildScopesTo(scopeId))`로 교체. Playwright 실측(심사 펼침→서류검토 클릭): s-doc opacity 0.4→1(활성)·형제 s-start=1·루트 r-intake=0.4(조상 dim)·드릴 창 0·breadcrumb "…›심사" 확인. **onNodeClick이 selectable:false dim 노드에도 발화함 확인**(raw capture 불필요). 알려진 갭(Step 4): 자식 더블클릭 편집 모달이 단일클릭-활성화와 공존(정리 예정). tsc/eslint green.
- **포커스 모드 Step 3a — 드릴인 플로팅 창 억제(캔버스 항상 루트)** (`feat/active-scope-focus-mode`, 사용자 선택 "드릴 창 억제"). 기존엔 ReactFlow가 활성 스코프 `ScopeWindow`(index>0 플로팅 창) 안으로 이동 → 포커스 dim 캔버스를 가렸음. `scopes.map` 윈도 렌더에서 ①루트(index 0)는 항상 `active`(유일 캔버스 호스트, `!active` "click to edit" 오버레이 제거)·②`index!==0`은 렌더 안 함(드릴 창 억제). currentParentId가 activeIndex를 따라가므로 캔버스 내용은 그대로 활성 스코프. AI 채팅 창은 별도라 유지. 2줄 변경(ScopePreview 분기는 index 0 active=true라 자동으로 ReactFlow 렌더 → 대수술 불필요, dead 분기는 Step 4 정리). Playwright(s-approve 진입): 드릴 창 0개·활성 또렷·조상 dim·breadcrumb 복귀 PASS. (다음 3b: dim 자식 클릭→navigateTo 활성화) tsc/eslint green.
- **포커스 모드 Step 2 — 조상 스코프 dim 컨텍스트 렌더** (`feat/active-scope-focus-mode`). 활성 스코프가 자식(`currentParentId≠null`)일 때 조상 체인을 읽기전용 dim으로 메인 캔버스에 렌더. `ancestorContextNodes` memo: `fullGraph`에서 각 조상 스코프(형제 포함)를 `toAppNodes`로 만들어 measured 직접 주입(레슨: 미측정=visibility:hidden), 각 스코프 우변이 활성 스코프 좌측에 오도록 평행이동(상단 정렬)·깊이만큼 좌로 누적. `displayNodes`에 concat, `contentExtent`(패닝)에 포함해 좌측 조상까지 팬 가능. 아키텍처는 navigateTo 방식 확정(사용자). Playwright 실측(s-approve 진입): 활성 4종 opacity=1·조상 2단계(r-review 스코프 4종+루트 5종) 모두 0.4 PASS. **발견: navigateTo가 기존 `ScopeWindow` 드릴인 플로팅 창을 띄워 포커스 캔버스를 가림 → Step 3에서 클릭 활성화와 함께 드릴 창 억제 필요(라우팅 정리).** tsc/eslint green.
- **포커스 모드 Step 1 — 비활성(인라인 자식) 스코프 dim + 읽기전용** (`feat/active-scope-focus-mode`). 인라인 펼친 자식을 편집 가능(5종)에서 **읽기전용 dim**으로 전환: `displayNodes` 자식 분기를 `selectable/draggable/deletable/connectable=false` + `opacity 0.4`(`INACTIVE_SCOPE_OPACITY`), `styledEdges` childEdges도 `selectable:false`+dim. 전역 `nodesDraggable`(펼침 중 false)은 무변경 → 루트 기본 편집 무영향. `activeScopeId` 상태는 활성 스코프가 현재 스코프와 갈라지는 Step 3에서 도입(지금은 자식=비활성이 childById로 이미 구분돼 중복 상태 보류). 알려진 갭(Step 4 라우팅 정리): 자식 더블클릭 편집 모달은 아직 열림. Playwright(시스템 Chrome) 실측: r-review 펼침 시 자식 4종 opacity=0.4·루트=1.0·자식 클릭 비선택·루트 클릭 선택 모두 PASS. tsc/eslint green.
- 줌아웃 시 **캔버스 좌상단 고정이 풀리던 버그 수정** — `translateExtent`가 줌아웃 뷰포트(`pane/minZoom`)보다 작아지면 d3-zoom 기본 `constrain`(zoom.js: `dx1>dx0 ? (dx0+dx1)/2 …`)이 콘텐츠를 **가운데 정렬(centering)**해 좌상단 여백이 자랐음. `translateExtent` 우하단을 `pane/MIN_ZOOM` 이상으로 늘려 centering 원천 차단(`nodeExtent`는 콘텐츠 bbox 유지로 분리 — 노드를 빈 우하단으로 드래그하는 회귀 방지), `MIN_ZOOM` 상수화. Playwright(시스템 Chrome) A/B 실측: minZoom 좌/상 여백 295/334px(가운데 밀림)→24px(마진×스케일, 고정), 여백은 우/하단에만. 프론트 tsc/eslint green.
- 인라인 하위 프로세스 **편집 가능화 — 별도 `childNodes` state 방식**. (1차 "메인 nodes에 자식 합치기"는 아웃라인·모달 라우팅·자식 flush 저장 등 `nodes`=현재스코프 가정을 광범위 파손해 reset.) 자식을 RF-관리 **별도 state**에 두고 displayNodes 합성 + 커스텀 `onNodesChange`로 nodes/childNodes 분배 → RF 측정·노드 이벤트 발화하면서 **메인 nodes 무손상(회귀 0)**. 검증: 표시 픽셀 동일(단일·중첩)·자식 클릭 선택·모달 편집 저장·아웃라인 깨끗. 계획 `docs/superpowers/plans/2026-06-19-editable-inline-subprocess.md`.
- 레인 안 자식 편집 4종(각 브라우저 검증, root 스코프 무오염) — **삭제**(`deletable`+`saveChildScopeAfterDelete` 자식스코프 PUT+fullGraph 낙관적 제거), **이동**(buildScope가 dagre 재배치 대신 **저장 pos 사용**+`childTop` 상단정렬로 튐 제거, 드래그 절대→`childOffsets`로 스코프상대 변환 저장), **연결**(펼침 중 `nodesConnectable` 켜고 프레임은 `connectable:false`, `onConnect`→`createChildEdge` 자식스코프 PUT·즉시 렌더), **추가**(영역 우클릭→가장 깊은 region 탐지→`scopeOffsets`로 스코프상대 변환→`addChildNode` 낙관적+PUT). 인라인 이름편집은 자식 in-node 입력 커밋 불안정 → 모달 유지.
- **캔버스 좌상단 고정** — `contentExtent`(패닝/노드 범위)를 대칭 `EXTENT_MARGIN`(600)→비대칭(좌상단 `EXTENT_TOPLEFT_MARGIN`=120/우하단 600). 위·왼쪽 무한 패닝 방지, 아래·오른쪽 성장 여유.
- **포커스 모드 설계(다음 세션)** — 최종 골 "깊이 무관 풀 편집(드롭존·그룹화·정렬 포함)". 통찰: 스코프 네비(`navigateTo`)로 어느 스코프든 `nodes`化하면 모든 기능 네이티브. 영역 클릭→그 스코프 활성(편집)+나머지 깊이 dim 읽기전용. 4단계 계획 `docs/superpowers/plans/2026-06-19-active-scope-focus-mode.md`. (드롭존 자식 적용 진단: `screenRectOf`는 getNode로 해결되나 `getIntersectingNodes`가 자식엔 빈 결과 → 수동 형제 겹침 판정 필요.)
- ⚠️ 검증 함정(기록): dev.db는 테스트마다 오염 → 단계 검증 전 `git checkout dev.db`+백엔드 재시작(실행 중 checkout은 readonly 유발). 자식 RF 이벤트·onNodeDrag는 **깨끗한 db에서 정상**(0으로 보인 건 오염 탓). Playwright 연결 드롭은 매우 flaky.
- 🐞 알려진 이슈(다음 세션 수정): **그룹/다중 선택 후 드래그 시 1개 노드만 이동**(선택된 전체가 아님). 사용자 실측. 의심: 커스텀 `handleNodesChange`의 nodes/childNodes 분배 또는 펼침 중 `draggable` 오버라이드가 다중-드래그 position change를 일부만 적용. 또는 기존(비펼침) 다중-드래그 자체 문제일 수도 — 펼침/비펼침 양쪽에서 재현 범위부터 확인.
- 📚 레슨런 문서화 — `docs/lessons/`(canvas-react-flow·scope-save-and-coordinates·browser-verification·react-ts-patterns + README) 신설, CLAUDE.md에 "Lessons" 섹션으로 링크. CLAUDE.md 상태 줄 stale 수정(인증·배포 완료, 주력=캔버스 에디터 UX).

## 2026-06-18
- 드롭으로 하위 만들기도 **후속없음 모달**을 타도록(사용자 피드백 #4) — `moveToChild`를 `runMoveToChild`(실제)+wrapper로 분리, B에 후속(나가는 엣지) 없으면 우클릭 생성과 동일 모달. 모달/픽 상태를 `{nodeId|sourceId, proceed}` continuation으로 일반화해 생성·드롭 공용(`handleCreateEndForSubprocess`·`handleSubprocessPick`이 `prompt.proceed()` 호출). Playwright로 신규 노드 우클릭→모달 `[Cancel·Pick successor·Add End node]`→"Add End node"→"Subprocess created" 검증. tsc/lint/build green.
- 인라인 펼침/접힘 UX 3종(사용자 요청) — ① **ease-in-out 슬라이드 애니메이션**: 펼침/접힘 시 다운스트림 노드 transform에 350ms `cubic-bezier(0.65,0,0.35,1)` 전환. `commitExpanded`가 애니메이션 클래스를 먼저 칠하고(rAF×2 뒤 `expandedInline` 변경) 전환을 발동. 규칙은 globals.css 대신 **raw `<style>`로 주입**(Turbopack dev가 `.react-flow__node` 대상 규칙을 purge해서). ② **펼침/접힘 줌 자동변경 제거**: 펼침 fitView 삭제, 아웃라인 나브 포커싱도 현재 줌 유지(minZoom=maxZoom). ③ **휠=캔버스 상하이동(`panOnScroll`+`Vertical`+`zoomOnScroll:false`), Ctrl/Cmd+휠=줌(`zoomActivationKeyCode`)**. Playwright(시스템 Chrome)로 dev·prod 모두 검증: 휠 dy≠0·dz=0, Ctrl+휠 dz≠0, 펼침 줌 2.0→2.0 불변, r-notify computed-x 826→999→1713→2048→2134 슬라이드 확인. tsc/lint/build green.
- `/graph/all`(fullGraph)이 **`has_children`을 계산·반환하도록 수정** — `FlatNodeOut`에 `has_children` 필드 누락 + 엔드포인트가 미계산이라 항상 null이었음(단일 스코프 `/graph`는 계산). 어떤 노드의 parent로 등장하는 id 집합으로 채움. 이로써 **레인 안 자식의 중첩 펼침 셰브론**(자체 하위 보유 자식, 예: 승인)이 표시됨. ruff·pytest(111) 통과, API로 r-review/s-approve=True 확인.
- 아웃라인에서 하위 노드 이동 시 **드릴인 창 대신 인라인 펼침**(사용자 피드백) — `handleOutlineSelect`의 다른-스코프 분기가 `navigateTo`(창)를 호출하던 것을, 대상 노드의 **조상 체인을 `expandedInline`에 모두 추가**(중첩 레인으로 노출) + 대상 노드로 `fitView` 포커싱으로 교체. 클릭·Tab(첫 자식 진입) 양쪽 적용. Playwright로 Tab→창 안 뜨고(react-flow 1개 유지) 자식 인라인 표시·포커싱 확인. tsc/lint/build green.
- 펼친 자식 **더블클릭 편집 모달이 안 열리던 문제 수정** — React Flow가 prop-only 자식 노드를 이벤트 트리에 reconcile 안 해 `onNodeDoubleClick`·React `onDoubleClick`이 자식엔 발화 안 함(루트는 정상). 캔버스 컨테이너에 **raw `dblclick` 캡처 리스너**를 달아 `closest('.react-flow__node')` data-id가 현재 스코프에 없으면(=자식) `preventDefault`+`stopPropagation`(RF 줌 차단) 후 요약/편집 모달을 연다. Playwright로 자식 더블클릭→모달(Previous/Next steps도 fullGraph 기준 정확)→Assignee 편집→**백엔드 자식 스코프 저장**까지 E2E 검증. (실패했던 onOpenSummary/ProcessNode onDoubleClick 시도는 자식에 발화 안 해 폐기) tsc/lint/build green.
- **인라인 펼침 자식 노드가 안 보이던 근본 버그 수정**(브라우저 실측으로 진단) — 합성 자식 노드는 `nodes` state에 없어 React Flow가 측정(measure)을 못 하고, **미측정 노드를 `visibility:hidden`으로 숨겨** 인라인 펼침이 통째로 빈 영역으로 보였음. `buildScope`에서 자식에 `width/height/measured`(타입별 `nodeSizeOf`)를 직접 넣어 즉시 가시화. + 펼침 `fitView`를 전체 그래프(멀리 떨어진 노드까지 맞춰 과도 축소 22%) → **펼친 서브프로세스(부모+자식)만** 프레이밍(`reactFlow.getNodes()`로 앵커+자식 id만, padding 0.3·maxZoom 1.2)으로 변경. Playwright+시스템 Chrome 헤드리스로 실제 렌더 검증(자식 라벨 "서류 검토" 등 페인트 확인). tsc/lint/build green.
- 펼친 자식 노드 **더블클릭 → 편집 모달**(사용자 피드백 #2) — 보기전용이던 자식을 현재 스코프 노드와 동일하게 더블클릭하면 요약/편집 모달이 뜨고 모든 필드 편집. `childEdits` 오버레이를 `Map<id, Partial<NodeData>>`로 확장, `handleSummaryPatch`/`LabelCommit`이 자식이면 `patchChildNode`(속성, **700ms 디바운스 저장**)/`renameChildNode`(이름, 즉시 저장)로 분기. 저장은 `getGraph(scopeId)`로 권위 그래프(노드+엣지+**그룹**)를 받아 `patchGraphNode`로 노드 필드만 덮어 PUT(fullGraph엔 그룹 없어 재구성 불가). `flushChildScopeSaves`가 dirty 스코프별 일괄 저장, 오버레이는 `childEditsRef`로 미러. 모달 node 조회·선후행은 자식이면 fullGraph 기준. 스코프 전환 시 타이머·dirty 정리. tsc/lint/build green. (※ 자식 속성 편집 후 700ms 내 스코프 이동 시 그 편집 유실 — 이름은 즉시 저장이라 안전.)
- 인라인 펼침 영역을 **캔버스 전체 높이 세로 레인**으로(사용자 피드백 #1) — 콘텐츠 높이 박스 → 보이는 뷰포트 위아래를 가득 채우는 세로선 2개. `InlineRegionBands`(신규, `useViewport`+`useStore(height)` 구독해 줌/팬 시 이 부분만 리렌더) 분리, ViewportPortal flow 좌표로 `topFlow=-y/zoom`·`bandHeight=height/zoom` 변환. 깊이 틴트 겹침·› 라벨(콘텐츠 상단, 클릭=접기)은 유지. tsc/lint/build green.
- 우클릭 "하위 열기"도 **인라인 펼침**으로 전환(드릴인 창 제거 마무리) — 컨텍스트 메뉴 onSelect를 `toggleInlineExpand`(정의 순서상 위쪽 useMemo에선 `toggleInlineExpandRef`로 호출)로, 고아 `handleDrillIn` 제거. 이제 자식 여는 모든 경로(체브론·요약창·우클릭)가 인라인. tsc/lint/build green.
- 피드백 수정 2건 — ① 드롭으로 하위 넣기(`moveToChild`): 대상이 비어있으면 A를 **Start→A→End**로 감싸 제대로 된 하위 프로세스 생성(불변식 충족), 이미 하위 있으면 기존처럼 추가. ② 요약창 "하위 열기"(`onOpenChild`)를 드릴인 **창 대신 인라인 펼침**(`toggleInlineExpand`)으로 전환, 고아 `handleOpenSummaryChild`·`handleDrillById` 제거(컨텍스트 메뉴 "열기"는 아직 창 — 사용자 확인 후 별도). tsc/lint/build green. ⚠️ 캔버스 수동 검증 필요.
- 인라인 **레인 안 편집(Phase 3 최소 슬라이스)** — 펼친 자식 노드를 `selectable`로 풀고 **타이틀 더블클릭 인라인 리네임**을 자기 스코프에 저장(scope-split). `renameNode`가 현재 스코프에 없는 id면 `renameChildNode`로 분기: `getGraph(versionId, scopeId)`로 권위 그래프(노드+엣지+**그룹**) 받아 라벨만 바꿔 PUT(`VersionGraph`엔 그룹이 없어 fullGraph 재구성은 데이터 유실 → fetch 방식). 낙관적 `childEdits` 오버레이로 즉시 반영(실패 시 되돌림, 스코프 전환 시 초기화), `inlineComposition`이 오버레이 적용. 펼침/접기·현재 스코프 저장은 무영향(비-dirty 유지). 인스펙터/recolor/속성 편집·레인 내 삭제는 후속(편집 핸들러가 현재 스코프 `nodes`에 묶여 자식-인식 분기 필요). tsc/lint/build green. ⚠️ 캔버스 수동 검증 필요(브라우저 없음).
- 하위 프로세스 **삭제 불변식**(Task 5.2) — 자식 스코프(`currentParentId!==null`)에서 노드 삭제가 **유효→무효 전이**(Start/End/작업 각 1개 미달)일 때만 `onBeforeDelete`로 가로채(레거시 미충족 스코프는 비차단) `[취소]/[하위 프로세스 삭제]` 모달. 확정 시 `saveGraph(versionId, 빈그래프, scopeId)`로 하위 통째 cascade 삭제(백엔드 graph.py가 후손·엣지·코멘트·그룹까지 재귀 삭제) → `has_children`은 파생이라 부모 자동 일반화 → 인라인 접기 + 브레드크럼 pop으로 부모 스코프 복귀. `navigateTo`는 이동 전 자동 저장으로 방금 비운 스코프를 되살리므로 직접 스코프 pop. `checkScopeInvariant`(기존 정의) 활용. i18n 5키. tsc/lint/build green. ⚠️ 캔버스 수동 검증 필요.
- 하위 생성 **후속없음 모달**(Task 5.1 잔여) — `createSubprocess`가 후속(나가는 엣지) 유무를 검사: 있으면 즉시 생성, 없으면 `[취소]/[후속 노드 선택]/[종료 노드 추가]` 모달(`expand-invariant-modal.tsx` 공용 확인 다이얼로그 신설 — 5.2와 공용). "종료 노드 추가"=현재 스코프에 End+`P→End` 엣지 생성 후 진행, "후속 노드 선택"=선택 모드 진입(상단 힌트 배너·Esc/빈영역클릭 취소)→클릭 노드로 `P→picked` 연결 후 진행. 둘 다 `scheduleAutoSave`로 부모 스코프 영속(펼침 시 End→후속 게이트웨이 연결 보장). i18n 8키. tsc/lint/build green. ⚠️ 캔버스 수동 검증 필요.
- 하위 프로세스 생성(spec step 3 — 생성) — process 노드(하위 없음) 우클릭 **"하위 프로세스 생성"** → Start/작업/End + 직렬 엣지를 자식 스코프에 자동 생성(`saveGraph(versionId, graph, nodeId)`, 불변식 충족), 부모 `hasChildren` 반영 + `refreshFullGraph` + 인라인 자동 펼침 + 토스트. 하위 보유 노드는 기존 "열기"(창) 유지. i18n 6키(`ctx.createSubprocess`·`subprocess.*`). 페이로드는 기존 `buildGraph` 출력과 동형이라 스키마 일치. (후속: 후속없음 모달·삭제 불변식·레인 안 편집) tsc/eslint/build green.
- 확인용 더미 1개 + DB 초기화 — `backend/scripts/seed_inline_demo.py`(`--reset`: 기존 맵 전체 삭제 후 **Start/End 포함 3단계 중첩 하위** 1개 생성 — 심사 › 승인 › 1·2차승인). 개발단계·실데이터 없음이라 `backend/dev.db`를 추적(`*.db`에 `!backend/dev.db` 예외)해 원격 작업용으로 커밋. ruff 통과.
- 인라인 펼침 **모두 펼치기/접기 + 펼침 캡(Phase 4)** — 툴바에 하위 모두 펼치기(`UnfoldHorizontal`)/접기(`FoldHorizontal`) 버튼. 개별·모두 펼칠 때 노드수>300 또는 깊이>5면 확인 모달(`그래도 펼치기`/취소, `EXPANSION_LIMITS`·`checkExpansionLimits`). i18n 6키 추가. tsc/eslint/build green.
- 인라인 펼침 영역 렌더 경량화(사용자 제안) — 흰 캔버스+dot-grid div → **세로선 2개 + 반투명 accent 5% 틴트**(깊을수록 틴트가 겹쳐 자연히 진해짐, 현재 캔버스 점 배경 그대로 비침). 깊이는 영역 좌상단 `›`×depth + 이름(클릭=접기)로 표시. `ChevronDown` import 제거. tsc/eslint/build green.
- 인라인 펼침 **중첩(재귀) 지원** — 레인 안 자식 노드(`hasChildren`)의 토글로 다시 펼치면 그 안에 더 깊은 캔버스 레인이 삽입. `inlineComposition`을 `buildScope` 재귀로 재작성(루트=수동 배치, 자식 스코프=dagre 배치 후 재귀, depth>1은 원점 정규화 후 부모가 영역 안쪽으로 평행이동·세로 중앙). 깊이별 배경 틴트(accent (depth-1)×6%), `A→B`/게이트웨이/가로지르는 엣지/전체높이 레인/캡 계산은 깊이 무관 처리. tsc/eslint/build green.
- 인라인 펼침 영역 시각 변경(사용자 피드백) — 그룹식 깊이 틴트 박스 → **삽입된 새 캔버스 느낌의 세로 레인**. 영역 배경은 콘텐츠 **상하 전체 높이**(전 노드 Y 범위 + 여백)로 뻗고, 흰 `surface` + dot-grid + 좌우 `divider`로 "새 캔버스가 끼워진" 모양. 자식은 A 세로 중앙에 배치. tsc/eslint/build green.
- 인라인 펼침 **영역 컨테이너 모델로 전환**(사용자 피드백) — 평면 LR 병합은 연결 안 된 노드가 상하로 쌓여 영역 구분이 사라지는 문제. 펼친 A 오른쪽에 **깊이 틴트 배경의 하위 영역 박스**를 삽입하고 공간상 A보다 오른쪽 노드를 우측 이동(왼쪽·A의 수동 배치 보존, 전체 재배치 아님). 자식은 영역 안에서 로컬 LR 배치, `A→진입(Start 등)`·`진출(End 등)→후속` 게이트웨이 또렷이(opacity 0.55), `A→B` 숨김, 영역 가로지르는 엣지 반투명. 영역 제목 칩 클릭=접기. 다중 펼침은 왼→오 누적 시프트. MVP=루트 레벨(중첩·편집은 후속). tsc/eslint/build green.
- 인라인 펼침 레이아웃 수정 — 게이트웨이가 Start/End 타입에만 의존해 레거시 하위(Start/End 없음)에서 자식이 그래프와 끊겨 dagre가 분리 컴포넌트로 처리 → 다음 노드가 아래로 빠지고 자식이 안 보이던 문제. 진입점=Start(없으면 진입차수0 자식)·진출점=End(없으면 진출차수0 자식)로 추론해 항상 `P↔자식↔다음노드`를 연결(자식이 가운데로 삽입·다음노드 우측 이동). 펼침 시 `fitView`로 화면 맞춤. tsc/eslint/build green.
- 인라인 펼치기/접기 **Phase 2(렌더)** — 노드 우상단 토글(`DrillButton`→`ExpandToggleButton`, Chevron, `hasChildren`일 때만)로 하위를 같은 캔버스에 인라인 펼침/접기. 메모리 `fullGraph`에서 자식 수집 → 통합 `layoutWithDagre(LR)` 재배치(**파생 레이어** `inlineComposition`, raw state·저장 무오염), A→B(펼친 노드 출발 엣지)는 `hidden`·게이트웨이(P→Start/End→후속)는 흐리게, 중첩 펼침은 현재+자식 엣지 합쳐 처리. 펼침 중 드래그/연결 비활성(dagre 재배치 좌표 불일치 방지)·자식은 보기 전용(비선택)·pan extent는 합성 노드 기준·그룹박스는 펼침 중 숨김. 자식 편집은 당분간 기존 창(존속) 경로 유지(편집 인라인화는 후속). 프론트 tsc/eslint/build green. ⚠️ 캔버스 수동 검증 필요(이 환경 브라우저 구동 불가).
- 드롭·삽입으로 만든 엣지가 화면에서 좌-좌로 붙던 버그 수정 — `withEdge`(canvas.ts)가 신규 엣지에 `sourceHandle`/`targetHandle`를 미설정해, 노드 핸들 8개 중 첫 렌더 핸들(left)로 폴백됐음. 반면 메뉴·`toAppEdges`·`buildGraph`는 right/left 폴백을 가정 → "선택은 우/좌인데 화면은 좌/좌"로 어긋남(저장→리로드 시 right/left로 점프). 기본 핸들 `s-right`/`t-left`를 명시(`sourceHandleId/targetHandleId` 재사용). 프론트 tsc/eslint green(런타임 시각 확인 미실행).
- 아웃라인 키보드 내비게이션 — `↓`/`Tab`=다음, `↑`/`Shift+Tab`=이전, `→`=펼치기, `←`=하위프로세스 닫기, `F`=스마트 토글(접힘→펼치기·펼쳐짐→첫 자식·말단→부모 접고 이동). 기존 `Tab`의 자동 하위펼침-다이브인 제거(펼침은 →/F 담당). `editor-left-sidebar.tsx handleListKey` 키 디스패치 + `page.tsx` 아웃라인 핸들러(next/prev/expand/collapse/fold), 방어적 `stopPropagation`으로 캔버스 방향키 노드이동·전역 단축키와 분리(`Ctrl/Cmd+F`는 통과). 프론트 tsc/eslint green(런타임 미실행).
- 아웃라인 키 동작 정정 + 단축키 안내 — `F`는 펼쳐진 노드에서 첫 자식 진입이 아니라 **접기**(펼침↔접힘 토글로 정정), `←`도 닫을 게 없으면(말단·이미 접힘) **부모 접고 상위로**(F 말단 동작과 통일, 공통 `foldToParent`). 노드인포↔아웃라인 사이에 **고정 단축키 안내**(`↑↓→←F` 5행, 선택 노드 상태로 행별 활성/흐림, `ShortcutLegend` kbd 스타일 재사용, i18n `outlineNav.*` en/ko 추가). 프론트 tsc/eslint green(런타임 미실행).
- 사이드바 단축키 카드 다듬기 + 아웃라인 선택 자동 스크롤 — 안내에 `Tab`/`⇧Tab` 별칭 병기(`↓`/`↑` 행), 카드 접기/펼치기(노드인포와 동일), 두 카드 접힘 상태 `sessionStorage` 영속(새로고침 유지, i18n 복원 패턴으로 hydration 회피), 둘 다 접히면 `contents`↔`flex`로 좌우 분할 표시. 선택 노드 변경 시 아웃라인 행 `scrollIntoView`로 스크롤 영역 밖 이탈 방지(키보드 이동·캔버스 선택 모두). 프론트 tsc/eslint green(런타임 미실행).
- 사이드바 스크롤 UX — 스크롤바 기본 숨김(`scrollbar-hidden`) + 스크롤 중에만 커스텀 막대 페이드 인 → 멈추면 700ms 후 페이드 아웃(막대는 스크롤 컨테이너 내부 `absolute`, `top`에 `scrollTop` 보정해 시야 고정). 자동 스크롤을 `block:"center"`로 변경(선택 행을 경계가 아닌 중앙쪽; 최상단 선택은 0으로 clamp되어 맨 위). 프론트 tsc/eslint green(런타임 미실행).
- 선택행 자동 스크롤 부드럽게 — `scrollIntoView`에 `behavior:"smooth"` 추가(네이티브 ease-in-out, `prefers-reduced-motion` 자동 존중). 프론트 tsc/eslint green(런타임 미실행).

## 2026-06-17
- 인라인 하위 프로세스 펼치기/접기 — 드릴인 자유창(`ScopeWindow`)을 같은 캔버스 인라인 펼침으로 전환 착수. 설계 확정: 자식 스코프(`parent_node_id`) 유지+뷰 합성(백엔드 무변경), 메모리의 `fullGraph` 재사용(추가 fetch 0), 통합 `layoutWithDagre(LR)`, 펼침=순수 뷰(A→B는 hide·재배선 없음), scope-split 저장, AI 채팅용 ScopeWindow는 존속. **Phase 1 기반**: 캡 config(`expansion-config.ts` 노드300/깊이5), 순수 로직(`inline-expand.ts` — 자식수집·게이트웨이·캡·scope-split·불변식), `NodeData.scopeId` 태그(`toAppNodes` 주입). 프론트 tsc/eslint green. WIP. (스펙/계획: `docs/superpowers/specs|plans/2026-06-17-inline-subprocess-expand.md`)
- 마이너 버그 수정 묶음. ① 언그룹→실행취소 시 그룹 미복원 — `Snapshot`이 `groups` 누락해 undo가 nodes만 복원하던 게 원인, 스냅샷·undo·redo에 `groups` 포함(disband·create 양쪽 정상화). ② 그룹 생성 로직 — 멤버 2명 미만이면 자동 제거(`pruneSmallGroups`, leaveGroup·노드삭제 경로 한정, 로드 데이터 불변), 선택 노드가 모두 한 그룹이면 생성 차단+토스트, 단일 노드 Ctrl+G 차단+토스트, 생성 즉시 이름 편집모드 진입(`GroupTitleBar autoEdit`), 그룹 자동삭제 시 토스트. ③ 노드 타입 변경 기능 삭제 — 인스펙터·요약모달 select를 읽기전용 표시로 교체(생성 시 타입 선택은 우클릭 메뉴 유지). ④ 토스트 재설계 — 우상단(Nav 아래) 슬라이드 인/아웃+스택(`toast-stack.tsx`, `var(--ease-spring/smooth)`). ⑤ 아웃라인 노드 삭제 지연 — stale `fullGraph` 병합이 삭제 노드를 되살리던 빈틈, 라이브 로드 후 현재 스코프는 라이브가 권위(즉시 반영). 프론트 tsc/lint/build green.
- 엣지 핸들 변 커스텀 — 엣지마다 시작/끝이 붙는 노드 변(상/하/좌/우)을 엣지 우클릭 십자 패드 2개(Start/End)로 변경(클릭해도 메뉴 유지). `source_side`/`target_side` 엣지 컬럼 영속(기본 우/좌, Pydantic `Literal` 검증), 노드는 4변 source·target 핸들(8개) 렌더(평소 은은·hover 강조), 엣지는 `sourceHandle`/`targetHandle`(`s-{side}`/`t-{side}`)로 연결. diff는 엣지를 source→target 계보로만 비교해 변은 비교 제외(diff.ts 무변경). 스키마는 1회 drop+recreate 필요. 백엔드 pytest(라운드트립·기본값·invalid 422) 통과, 프론트 tsc/lint/build green. (스펙: docs/superpowers/specs/2026-06-17-edge-handle-side-customization-design.md, 계획: docs/superpowers/plans/2026-06-17-edge-handle-side-customization.md)
- 판단(decision) 분기 엣지 Yes/No 색상 구분 — 은은한 파스텔 블루(Yes `#6f93cc`)/레드(No `#cf7e84`) 토큰 신설, `styledEdges`에서 `branchKindOf(label)` 기준으로 stroke·화살표·라벨 알약(14% 틴트 배경+테두리) 색 적용. 기타 분기는 기본 톤 유지, 라벨 파생이라 영속 불필요. 선택 노드 강조(in=teal/out=orange)는 그대로 우선.
- AI 채팅 패널 세로 스크롤 수정 — 메시지 영역 `flex-1`에 `min-h-0` 누락으로 컨테이너 밖으로 늘어나 스크롤 불가였음. `min-h-0` 추가 + 새 메시지 시 자동 하단 스크롤. 스크롤바는 `scrollbar-hidden` 유틸(globals.css)로 기본 숨김(동작은 유지).
- 모달 외부클릭 닫기 버그 수정 — `onClick`(=mouseup)으로 닫아, 모달 내부 드래그를 바깥에서 떼면 백드롭 click이 발생해 잘못 닫혔음. 공용 `ModalBackdrop`(mousedown·click 모두 백드롭 자신일 때만 닫음)으로 5개 모달(dev-login·node-summary·group-bulk·approver-manager·edge-branch) 통일. edge-branch 기존 150ms `armed` 해킹은 이 가드로 대체·제거.
- 캔버스 좌측 줌 인디케이터(`canvas-zoom-scale.tsx`) — `useViewport`로 현재 줌을 세로 눈금 바에 실시간 표시(로그 스케일·반응형 높이·표시 전용 pointer-events-none).
- 노드·그룹 이름 캔버스 내 중복 금지 — 중복 시 `(2)`/`(3)` 자동 접미사(`canvas.ts makeUniqueLabel`). 노드끼리·그룹끼리 따로, 빈 이름 예외. 적용: 노드 인라인/요약모달(blur) 리네임·생성, 그룹 리네임(타이틀바·일괄)·생성(선택→/드롭→).

## 2026-06-16
- 편집화면 노드/엣지 생성 시 `crypto.randomUUID is not a function`(평문 HTTP=insecure context, secure context 전용 API) 수정. 실제 호출처는 에디터 `[mapId]/page.tsx` **6곳**(handleAddNode·createEdge·applyAiProposal·그룹 생성) — 직접 `crypto.randomUUID()`. 공용 헬퍼 `lib/id.ts`(`genId`, getRandomValues 폴백) 신설해 6곳 + `canvas.ts` 전부 교체. (앞선 canvas.ts-only 수정은 실제 경로를 못 짚었고, ugrep이 `[mapId]` 대괄호 경로를 건너뛰어 호출처를 놓쳤었음.) localhost는 secure context라 정상 → 서버(원격 HTTP) 전용 증상.
- AI 채팅 502 "AI returned invalid response" 진단 보강 — 검증 실패 시 원본 모델 출력을 서버 로그에 기록(`ai.py`), `_extract_json`으로 ```json 펜스·앞뒤 prose 제거 후 재파싱. (AI는 서버 전용 기능=AI_ENABLED, 로컬 기본 비활성이라 서버에서만 발현. 근본 원인은 로그의 raw 출력으로 확정 필요.)
- 로그인 직후 첫 `GET /maps` 401(missing bearer token) 수정 — 자식 페이지 fetch effect가 부모 AuthGate의 `setAuthToken` effect보다 먼저 실행되던 레이스(React effect 자식→부모 순서). 토큰을 effect 대신 AuthGate **렌더 단계에서 동기 반영**해 자식 mount 전에 채움. 전 페이지 적용.
- 앱 노출 포트 9787→3333 일괄 변경 — 서버 실제 배포 포트(3333)에 맞춤. `.env.example`·`docker-compose.yml` 기본값·README·CLAUDE.md·`docs/spec.md`·`docs/deploy.md`·`docs/deploy-auth-ad.md`·`nginx/default.conf` 주석 전수 스윕(과거 로그는 보존). ⚠️ Keycloak `bpm-frontend` Valid redirect URIs/Web origins도 `:3333`으로 갱신해야 로그인 redirect 정상.
- Keycloak 로그인 무반응(평문 HTTP) 수정 — 원격 IP HTTP는 secure context가 아니라 PKCE의 `crypto.subtle`이 브라우저에 차단됨 → `signinRedirect`가 조용히 throw. 프론트 OIDC 설정(`keycloak-login.ts`·`providers.tsx`)에 `disablePKCE: true`로 우회(사내망 한정·Keycloak도 동일 서버 개발용이라 수용한 트레이드오프, HTTPS 전환 시 복구). 배포 문서 트러블슈팅·보안 메모 추가.
- 서버(사내 71번) 배포 성공 반영 — 프론트 Dockerfile `node:22-alpine`→`node:20-alpine`(서버에서 node:22 이미지 풀 실패), `docker-compose.yml`에 `networks.default` 명시 서브넷(172.36.0.0/16, 사내망 대역 충돌 회피).
- 배포 절차 문서(`docs/deploy-auth-ad.md`) — Keycloak 로그인+AD 동기화 서버 배포(env·Keycloak federation·검증·트러블슈팅). + `docker-compose.yml` backend에 `LDAP_*`·`SYSTEM_ADMIN_LOGIN_IDS` 환경변수 배선(누락 갭 수정 — 컨테이너는 .env 직접 안 읽고 compose environment로만 주입받음).
- Keycloak 로그인 화면 + 사내 AD(LDAP) 동기화 + 로컬 임시 로그인 구현(spec/plan: `docs/superpowers/specs|plans/2026-06-16-keycloak-login-ad-sync*`). 백엔드: `employees` 테이블, `app/ad/`(DN 파싱·필터 순수함수+pytest, ldap3 클라이언트, `sync_one`/`sync_all`+5분 인메모리 가드), `X-Dev-User` 의존성·`require_admin`, `/api/me` 확장·`/api/employees`·`/api/employees/sync`(admin). 프론트: `/login` 와일드카드 게이트(AuthGate/DevGate), 임시 로그인 모달(5명 fixture, `X-Dev-User`), TopNav 유저 드롭다운(관리자 페이지/로그아웃), `/admin` 직원 테이블+동기화. 검증: 백엔드 108 pytest+ruff, 프론트 eslint+build green. (Keycloak 서버 설정·실연동은 서버 배포 시.)
- DB 초기화·더미 시드 문서화(`docs/db-seed.md`) + 시드 스크립트(`backend/scripts/seed_dummy.py`) 저장소 등록. `--reset`(전체삭제 후 3세트 재생성)·`--verify`(인접 버전 diff) 사용법, 대상 DB(로컬 sqlite/서버 postgres), 검증 절차 정리.
- 판단(마름모) 분기 모달 누락 수정. 엣지 생성 경로가 둘(핸들 드래그 `onConnect` / 노드 드롭 `applyFlowEdges`)인데 모달이 드래그에만 있어 드롭 연결 시 라벨 없는(=기타) 엣지가 바로 생성되던 버그 → 드롭 경로도 모달을 타도록 `branchPrompt`(connection/edge) 일반화. 중간 삽입 시 분기 라벨이 마름모를 source로 유지되도록 `insertNodeAfter`에 `bIsDecision` 분기 추가.
- 전역 버튼 인터랙션 base(`globals.css`) — 모든 `<button>` 포인터 커서 + 클릭 `scale(0.97)` 눌림(`prefers-reduced-motion` 가드), 컴포넌트는 hover 배경만. 설계 규칙(`rules/frontend/design.md` §4)에 명문화.
- 캔버스 노드 hover 모션에 대각선 이동(`-translate-x-0.5`) 추가 — 마름모·일반 노드.
- WorkflowDashboard 리디자인 — 하단 액션 버튼 전폭(flex-1)·흰 배경 호버 가시화, 안내/진행 메시지 좌측 배치, 승인자 관리 버튼 본문 이동, LifecycleStepper를 oh-my-zsh Powerline 셰브론 세그먼트 바 스타일로 재설계.
- decision/start/end는 하위 프로세스 생성 차단(moveToChild 가드+`err.childOnlyProcess`, 노드메뉴 "하위 열기"·DrillButton은 process/기존하위 노드에만 노출, 드롭존 child 타일은 숨기지 않고 비활성(흐리게) 표시). 분기 모달 즉시닫힘 방지(EdgeBranchModal `armed` 150ms 가드 — 연결 릴리스 후속 click 무시; onConnect 로직은 회귀 아님). 오토레이아웃 단축키 추가: 전역 Shift+L, 메뉴 가속기 A→A(정렬 날개 첫 항목 accel A).

## 2026-06-15
- 드롭다운 메뉴 단축키 힌트를 숏컷 레전드와 동일한 `kbd` 디자인(공용 `KBD_CLASS`)으로 + 라벨과 간격(gap-3) 분리, 패널 폭 w-44→w-48.
- 단축키 IME 무관(`event.code` 물리키 판정 — 한글 ㅁ/ㅊ 등도 인식) + 정렬 키를 왼손 전용·연관 철자로 재배치(좌 W=West, 가로가운데 C, 상단 T, 세로가운데 X, 가로분배 R=spRead, 세로분배 V). Alt 조합도 동일(Alt+W/C/T/X·R/V). 레전드 갱신.
- 메뉴 단축키 2계층화. ① **메뉴 가속기(단일 키, 우클릭 메뉴 떠 있을 때만)** — `ContextMenu`에 키보드 네비게이션(`accel` 필드, 하위 메뉴 진입: A→정렬 날개→T 등). 1~4 추가, E 정보수정, A 정렬, G 그룹생성. ② **전역 조합키(메뉴 없이)** — Alt+L/C/T/M 정렬·Alt+H/V 분배(event.code로 OS무관), Ctrl+G 그룹, Ctrl+⇧E PNG. 레전드 갱신.
- 정렬 메뉴 재구성. 가로 가운데(centerX)·세로 가운데(centerY) 정렬 추가(`alignSelected`), 오토레이아웃+정렬+분배를 단일 "정렬·레이아웃" 날개(submenu) 메뉴로 통합(pane/group/selection 공용 `alignItem`), 각 항목에 Lucide 정렬 아이콘으로 가로/세로 구분(`ContextMenuItem.icon` 지원 추가). bun 검증.
- dot-grid 가시성 향상(`--color-canvas-dot` 진하게 + size 1.2→1.8) + 루트(최외곽) 캔버스 포커스 시 드릴인 창 자동 최소화(좌하단 dock, `focusScope` index 0).
- 판단(decision) 노드 분기 엣지. 판단 노드에서 연결 시 Yes/No/기타 선택 모달(`edge-branch-modal.tsx`), 엣지 라벨에 디자인 알약 스타일(styledEdges labelStyle/Bg), 인스펙터에 Yes/No/기타 세그먼트 탭 전환 + 기타일 때만 라벨 직접 편집. 라벨 기반 종류 판정(`branchKindOf`, 백엔드 무변경 — 기존 label 영속). build green.
- 캔버스 무한 확장 → fit 비대화 개선. ① 오토레이아웃(`layoutWithDagre`) 결과를 좌상단(0,0) 기준 정규화(드리프트 누적 제거, bun 검증). ② 노드 위치·패닝 범위를 콘텐츠 bbox+여백(600)으로 제한(`contentExtent` → `nodeExtent`/`translateExtent`, 콘텐츠 늘면 확장) + `minZoom 0.2` 안전망. build green.
- 아웃라인 들여쓰기·키보드 편집. ① 들여쓰기는 하위 프로세스(계층)에만 — 병렬/분기는 같은 수준(`computeScopeFlow` 분기 indentation 제거, bun 검증). ② 키보드: 선택 상태 Enter=이름 편집 진입·재Enter=저장, Esc=취소, Tab=다음 노드(하위 프로세스 있으면 펼쳐 첫 자식 진입), 편집 중 Tab=저장+다음(`page.tsx handleOutlineNext`, 사이드바 onKeyDown·focus 관리). build green.
- 그룹 박스 외곽선 로직 = **기본 사각형(멤버 bbox) − 비멤버 notch**. 범위 안에 들어온 비그룹 노드를 가장 가까운 변쪽으로 직사각형으로 잘라내 제외(90° 직교, 연결 유지). `canvas.ts rectWithExclusions`/`nearestEdgeNotch`(좌표압축 격자, 의존성 0, bun 검증). `GroupBox`는 SVG fill+outline path(non-scaling-stroke). 반투명·z·타이틀바·합류 펄스 유지. build green. (이전 union/MST-다리 방식 대체)
- 그룹 = 다중 태그 모델로 전환(#6) — 중첩(parent_group_id) 미사용, 노드가 여러 그룹(태그) 동시 소속. 백엔드 `nodes.group_ids`(JSON, 레거시 `group_id` 로드시 병합·무손실, 스톱갭 컬럼, 검증·복제 갱신, 테스트). 프론트 `NodeData.groupIds[]`, 태그별 박스(멤버 많을수록 패딩↑로 감쌈), 반투명 fill(z 무관 모두 가시) + z는 멤버 적은 그룹이 위, 색 팔레트 순환. 백엔드 94 passed+ruff, 프론트 build green.
- 에디터 후속 7종(#0·3·4-1·4-2·4-3·5·10·14) — 노드 호버(그림자·살짝나옴·반투명), 이름 외 영역 더블클릭=요약창(타이틀 더블클릭=이름편집), 스왑 드롭존 좌하단 이동·엣지연결도 교환·드롭존 크기 프로세스 기준 고정, 엣지 선택 강조색, dagre 간격 확대(엣지-노드 겹침), 알림센터 z최상위. build green.
- 그룹 중첩(하위 그룹핑, 항목 8 — 옵션1 중첩만, 큰 다중소속 모델은 보류). 백엔드: `groups.parent_group_id`(자기참조, 스톱갭 컬럼 보강·고아/자기참조 정리·복제 리맵, 테스트). 프론트: 그룹 멤버 부분집합 선택→"그룹 생성" 시 하위 그룹으로 중첩, 해제 시 멤버·하위그룹 상위로 승격, 중첩 높이 기반 패딩으로 박스 포함 렌더, 일괄편집·색은 서브트리 대상. 순수 헬퍼 `collectKeptGroups`/`computeGroupHeights`/`groupSubtreeIds`(bun 검증). 백엔드 94 passed+ruff, 프론트 build green.
- 에디터 14종 배치(브랜치 `feat/editor-batch`) — 12개 구현(항목 8 중첩/다중그룹은 설계 협의 후 별도): 아웃라인 행 우클릭 메뉴·더블클릭 이름편집(#1), 노드 우클릭 "정보 수정" 모달(요약모달 편집 확장, 색 1줄+더보기)(#2), 더블클릭 인라인 이름편집·타이틀 I-beam(#3), 드롭존 중앙 위치교환(#4), 엣지 선택 강조 강화(#5), 다중선택→그룹 생성(#6), 그룹 해제(#7), Delete 전용 삭제(#9), dagre 간격·노드별 박스로 겹침 완화(#10), 엣지 스타일 맵 전역 선택(곡선/꺾은선/직선)(#11), 겹침 시 시야 자동 보정(#12), AI 채팅 풀 플로팅 창(ScopeWindow·dock 재사용)(#13), 알림센터 바깥클릭 닫힘(#14). eslint+build green.
- AI 채팅 패널 플로팅 슬라이드 인/아웃 + 일괄 속성 적용 후 입력·정책 초기화·"적용 완료" 토스트.
- 후속 UI: 미리보기 노드 호버 채우기, 일괄 속성 동일값 자동 스킵, 노드정보 박스 아웃라인 밖 이동·접기.
- 후속 UI: AI 패널 상시 표시(비활성 시 사유), 대시보드 상단 sticky·본문 스크롤, 서브프로세스 버튼 미리보기 이동, 그룹 일괄 편집 개선(개별선택 마법사·그룹명·기존값 팝오버), 노드정보 토글 스위치.
- 그룹 멤버 일괄 편집(색상·속성, 충돌 처리: 교체/추가/건너뛰기/개별) 신규 `group-bulk-modal.tsx`.
- UI 개선 4종: 대시보드 정리, 요약 모달 서브프로세스 진입 버튼, 드롭존 0.7배·겹침+dwell 트리거, 노드 표시 정보 선택(localStorage).
- DB 스톱갭(`app/db.py`)에 워크플로우 컬럼 보강(`status` DEFAULT 'draft' 백필) — 기존 DB 데이터 보존 자동 마이그레이션. `tests/test_db.py`.
- AI 모델 선택(프론트 드롭다운, `/v1/models` 프록시) + 접속 테스트 문서 `docs/ai-connectivity-test.md`.
- 온프레미스 AI 채팅 구현 — 백엔드 OpenAI 호환 프록시(`ai_client`/`ai_prompt`/`manual`/`routers/ai`, 502에 내부 URL 비노출, AI 서버 mock 테스트) + 프론트 채팅·미리보기·적용·매뉴얼. 설계 `specs/2026-06-15-ai-chat-flowchart-design.md`. **미해결: 실 vLLM `/v1/models` 502(서버측, 보류).**

## 2026-06-14
- 버전 승인 워크플로우 풀스택 완료 — Draft→Pending→Approved→Published(+Rejected), 맵별 만장일치 승인자, 수동 게시+구버전 강등, 인앱 알림. 대시보드(라이프사이클 stepper·승인자 체크리스트·높이조절), 상태배지·액션 버튼. 설계 `specs/2026-06-14-version-approval-workflow-design.md`.
- 워크플로우 버그·스모크 수정 — 모달 갇힘 portal화, Submit 막다른길 방지, `isMapOwner` 백엔드 정합.
- 캔버스 UX — 드롭존 줌 고정, 아웃라인/검색 선택 노드 보더, 방향별 엣지 색강조.

## 2026-06-13
- 에디터 UI 대개편 — 좌 사이드바(아웃라인 트리), 우 인스펙터 상시·폭조절, 컨텍스트 메뉴(divider), 단축키 레전드, 드래그-오버 드롭존(앞/뒤/그룹/하위), 비활성창 정적 SVG 프리뷰. 설계 `specs/2026-06-13-{editor-ui,drag-drop-zones,node-interactions}-design.md`.
- 그룹(업무 묶음) 풀스택 — `groups` 테이블+`nodes.group_id`, 그룹 박스·타이틀바(이름/색/이동/나가기). 노드·그룹 색 팔레트 무채도 톤으로 세련화.
- Whimsical 디자인 — 바이올렛 액센트(#6A41FF)·파스텔 노드·dot-grid·움직이는 엣지·겹침 방지. `specs/2026-06-13-whimsical-design-design.md`, `rules/frontend/design.md`.

## 2026-06-12
- OS형 자유 창(드릴인 윈도우 `ScopeWindow` — 이동/리사이즈/포커스/최소·최대/영속). `specs/2026-06-12-os-windows-design.md`.
- UI 디자인 시스템 — Tailwind4 `@theme` 토큰·Pretendard·Lucide, flat+hairline. `specs/2026-06-12-ui-design-system-design.md`.
- UI 개선 — 계단식 창, 전역 네비바+경량 i18n(en/ko), 박스선택·스페이스 팬. `specs/2026-06-12-ui-improvements-design.md`.
- 기능 확장 Phase A/B/C(`docs/spec.md` §7) — A: undo/redo·마우스위치 컨텍스트메뉴·자동저장·노드 색/모양/엣지 라벨. B: BPM 속성 4종·버전 diff(계보 매칭)·초성 검색·PNG. C: 체크아웃 잠금·노드 코멘트(폴링).
- 문서 명령 bash/PowerShell 병기 원칙(`rules/common/documentation.md`).

## 2026-06-11
- 초기 구축(spec §6 ①~⑤): 스캐폴딩(Next+FastAPI+nginx+compose, 9787) → 맵 CRUD+캔버스 → 계층(드릴다운·브레드크럼)+정렬(dagre) → 버전관리+비교 → Keycloak 인증(AUTH_ENABLED). 배포 준비 `docs/deploy.md`. 기능 명세 `docs/spec.md`. 프로젝트명 BPM 확정.
