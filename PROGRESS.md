# Progress

프로젝트 진행 현황 로그. 커밋 직전 갱신한다 (`rules/common/git.md` 규칙).

## 2026-06-14
- 비활성(조상) 창 정적 프리뷰 (브랜치 `feat/editor-ui`). 지금까지 조상 창은 타이틀바만(내용 비움)이라 라이브 ReactFlow는 활성 창 1개뿐. 사용자 요청대로 조상 창에도 내용을 보이되 N개 라이브 인스턴스 부하를 피하려 **정적 SVG 프리뷰**로: 신규 `scope-preview.tsx` — fullGraph에서 해당 스코프(parent_node_id) 노드/엣지를 골라 viewBox로 창 크기에 자동 맞춤, 노드는 둥근 rect(파스텔 fill+stroke)+제목, 엣지는 선. pointer-events-none이라 클릭은 창 onFocus로 전달돼 활성화. ReactFlow·ResizeObserver·store 없이 경량. 정확도는 마지막 저장본(fullGraph) 기준(조상은 편집 안 함+스코프 전환 시 저장·refresh). 검증: tsc/eslint/build green.
- 아웃라인 전체 프로젝트 트리화 + 클릭 포커싱 애니메이션 (브랜치 `feat/editor-ui`). ① 아웃라인을 **항상 프로젝트 루트(null) 기준 전체 트리**로 빌드(현재 스코프는 라이브 병합 유지) — 창을 옮겨도 전체 프로젝트를 일관 표시. 활성 스코프 경로(scopes의 drilled parentId)는 펼침 집합에 합성해 현재 위치 자동 노출(메모에서 파생 — set-state-in-effect 회피). ② 아웃라인 클릭 → 노드가 다른 스코프면 fullGraph로 스코프 체인 구성해 navigateTo+focusNodeIdRef, 같은 스코프면 즉시. 포커싱은 `fitView` duration 300→700(React Flow 기본 cubic-in-out=느림→빠름→느림 가감속이 또렷). 검증: tsc/eslint/build green.

## 2026-06-13
- 드롭/저장 버그 수정 + 드롭 영역 개선 (브랜치 `feat/editor-ui`). ① **422 방어**: `buildGraph`가 항상 자기완결적 payload를 만들도록 — 고아 group_id(payload에 그룹 없음)는 null, 양 끝이 payload 노드인 엣지만 포함. 드릴인/저장 시 그룹·엣지 참조 불일치로 인한 422를 구조적으로 차단. `api.ts` 요청 에러에 서버 detail 포함(진단용). ② **드롭 영역 개선**: 판정 기준을 노드 위치→**마우스 flow 좌표**(커서 아래 노드 대상, 커서 방향으로 zone), 링 반경 2배, 4타일을 **원주 위 cardinal 지점**에 배치(좌/우/위/아래), 타일 크기 30→44·아이콘 20, 등장 애니메이션 `zone-ring-in`/`zone-pop-in`(ease-overshoot). 검증: tsc/eslint/build green.
- 에디터 UI 2차 개선 (브랜치 `feat/editor-ui`). ① **아웃라인 분기 들여쓰기 + 하위 펼치기**: `buildOutline` 재작성 — 들여쓰기는 out-degree≥2(분기)일 때만(순차 전후는 같은 레벨), 입력을 OutlineNode/OutlineEdge로 분리. 하위 프로세스 있는 노드는 ▸/▾ 인라인 펼치기(전체 그래프 getFullGraph 1회 로드+현재 스코프는 라이브 상태 병합, 재귀, 하위는 다른 색·기본 접힘). 페이지에 fullGraph/expanded 상태 + refreshFullGraph(로드·저장 후). bun 검증 4케이스(순차평탄/분기/접힘/펼침) 통과. ② **Insert·Arrange·색 → 우클릭 메뉴**: 좌 사이드바는 아웃라인 전용으로 축소. ContextMenu에 색 스와치 행 타입 추가. 빈 캔버스 메뉴=타입별 추가(4)+자동배치·정렬·분배+PNG, 노드 메뉴=색 행+하위 열기+삭제. ③ **드롭 영역 4방향 재디자인**: dwell 시 B에 원형 링 + 4개 정사각 아이콘 타일(좌=앞 ←/우=뒤 →/위=그룹 ⊞/아래=하위로 넣기 ↳), A·B 중심 우세 방향으로 활성 판정(dragCenterRef x·y). **아래=신규 moveToChild**: A를 B 자식 스코프에 먼저 영속(재부모화)한 뒤 현재 스코프에서 A+연결엣지 제거(순서 보장으로 삭제 방지). 백엔드 변경 없음(기존 per-scope PUT의 parent upsert 활용). i18n 키 추가. 검증: tsc/eslint/build green. 시각·인터랙션은 로컬 확인 필요.
- 아웃라인 구조화 (브랜치 `feat/editor-ui`). `canvas.ts` 신규 순수 함수 `buildOutline(nodes, edges)` — 엣지(흐름) 기준 들여쓰기(depth)로 병렬/하위 구조 표현 + 무방향 union-find로 독립 연결요소를 블록(blockIndex)으로 분리(무연결 노드는 마지막 블록에 모음). DFS pre-order로 root(indegree 0)부터 자식 들여쓰기, 사이클은 fallback. 좌측 사이드바는 depth*12px 들여쓰기 + 블록 경계마다 스페이서(`border-t`) 렌더. bun 단위 검증 4케이스(체인·두체인·무연결·분기) ALL PASS, tsc/eslint/build green.
- 에디터 UI 개선 구현 완료 (브랜치 `feat/editor-ui`). ① **좌측 사이드바** 신규 `editor-left-sidebar.tsx` — Insert(타입별 노드 추가 4종 + 선택 노드 색 프리셋), Arrange(자동배치·정렬·분배, 헤더에서 이동), Outline(현재 스코프 노드 목록 클릭→선택+fitView 센터, 하위 드릴). 접기 가능. ② **우측 인스펙터** 기존 조건부 aside 2개를 상시 단일 패널로 통합(노드 속성 / 엣지 라벨 / 미선택 시 맵·노드수·그룹 요약), 좌측 가장자리 드래그 폭조절(220~480, `bpm.inspectorWidth` localStorage 영속), 헤더 토글로 숨김. ③ **컨텍스트 메뉴** `context-menu.tsx`에 `divider` 항목 타입 추가, 빈 캔버스 메뉴에 정렬·분배·PNG 편입하고 기능별 구분선. ④ **단축키 레전드** 신규 `shortcut-legend.tsx` — 우하단 `?` 버튼/`?`키 토글, 반투명(bg-surface/85 backdrop-blur) 패널에 9개 단축키. 헤더는 PNG·저장·인스펙터토글만 남겨 과밀 해소. handleAddNode에 nodeType 인자 추가(좌표 없으면 뷰포트 중앙). i18n sidebar.*/legend.*/inspector.* 키 추가(en/ko). 검증: tsc/eslint/build green. 시각·인터랙션은 로컬 확인 필요.
- 에디터 UI 개선 설계 확정 → `docs/superpowers/specs/2026-06-13-editor-ui-design.md` (브랜치 `feat/editor-ui`). 좌 사이드바(Insert 팔레트+Arrange 도구+Outline 트리), 우 인스펙터 상시화+폭조절+숨기기, 컨텍스트 메뉴 divider 분류+기존 기능 편입, 우하단 ?토글 반투명 단축키 레전드. 헤더에서 정렬·배치·노드추가를 좌측으로 이동해 과밀 해소.
- 그룹 박스가 노드를 가리던 문제 수정 (브랜치 `feat/drop-zones`). `.react-flow__viewport-portal`이 DOM상 노드 다음(마지막)이라 ViewportPortal 콘텐츠가 노드 위에 그려졌음. `.react-flow__viewport`가 z-index:2로 스택 컨텍스트를 만들고 포털 div는 z 없음을 확인 → 그룹 박스 래퍼 `zIndex: -1`로 변경(음수는 컨텍스트 내 노드 z:0보다 먼저 그려져 뒤로 깔림, dot-grid 위·노드 아래). tsc/eslint/build green.
- 그룹 기능 버그 수정 (브랜치 `feat/drop-zones`). ① **기존 DB 500 해결** — `nodes.group_id` 컬럼이 없는 구 DB에서 GET/PUT graph가 500(저장·로드 실패→그룹 유실의 근본 원인). `db.py` `init_models`에 경량 스톱갭 마이그레이션 `_add_missing_columns` 추가: startup에서 `_ADDED_COLUMNS` 목록(nodes.group_id VARCHAR(50))을 inspect로 검사해 누락 시 `ALTER TABLE ADD COLUMN`(sqlite/postgres 공통, 멱등). 시뮬레이션 검증(구 nodes 테이블→컬럼 추가, 기존 행 NULL, 재실행 무에러). DB 파일 삭제 불필요. ② **드롭 영역이 창 뒤에 가림** — 활성 창 zIndex=1000인데 오버레이가 z-20이라 가려짐 → 드롭 영역 `z-[1100]`·되묻기 팝오버 `z-[1110]`로 상향. 검증: 백엔드 46 passed + ruff, 프론트 tsc/eslint/build green.
- 드래그-오버 영역 선택 Phase 2(풀스택 그룹) 구현 완료 (브랜치 `feat/drop-zones`). **백엔드**: `groups` 테이블(id·version_id·parent_node_id 스코프·label·color, MapVersion에 cascade 관계) + `nodes.group_id`(nullable, FK 미설정 앱 관리). schemas `GroupIn` + `NodeIn.group_id` + `GraphIn/GraphOut.groups`. graph 라우터 — `_load_scope`가 그룹 로드, `replace_graph`가 그룹 스코프 교체·node.group_id upsert·미지 그룹 참조 422·서브트리 삭제 시 그룹 정리. versions `_clone_graph`가 그룹 신규 ID 복제 + node.group_id 리맵(selectinload(groups) 추가). 테스트 +4(그룹 라운드트립·미지그룹 422·삭제 정리·복제 계승), 빈그래프 단언 2건 groups 반영. **프론트**: api `Graph.groups`/`GraphNode.group_id`/`GraphGroup`. canvas `NodeData.groupId`. page — 드롭 영역 3분할(앞/그룹/뒤, B를 좌·중·우 1/3로 판정), 가운데=그룹 드롭 시 `addToGroup`(B 무소속이면 라벨=부서/담당자로 새 그룹 생성, A를 B 우측 배치+합류). 그룹 박스는 `groupBoxes`(멤버 bbox)로 산정해 `ViewportPortal`에 flow 좌표로 렌더(신규 `group-box.tsx`, 파스텔 fill·라벨, pointer-events-none 시각 전용). 그룹 상태/groupsRef, 그래프 로드·자동저장(buildGraph)에 groups 연결, 빈 그룹 저장 시 자동 제외. i18n `dropzone.group`. 검증: 백엔드 46 passed + ruff clean, 프론트 tsc/eslint/build green. **주의: 기존 로컬 sqlite는 nodes.group_id 새 컬럼이 없어 파일 삭제 후 재생성 필요(create_all은 컬럼 추가 안 함, Alembic 후속).** 미구현(후속): 그룹 이탈 UI(박스 밖 드래그 해제), 그룹 박스 드래그로 멤버 일괄 이동, 비교 화면 그룹 렌더. 인터랙션 시각은 로컬 확인 필요.
- 드래그-오버 영역 선택 Phase 1 구현 완료 (브랜치 `feat/drop-zones`, `feat/whimsical-design` 위). ① `canvas.ts` — `EDGE_DEFAULTS`를 이곳으로 중앙화(page.tsx는 import) + 순수 함수 `getIncomingEdges`/`getOutgoingEdges`/`insertNodeBefore`/`insertNodeAfter`(자기루프·중복 가드). bun 단위 검증 8케이스 ALL PASS. ② `page.tsx` — `onNodeDrag`에서 `getIntersectingNodes`로 A 아래 노드 감지, dwell 300ms(타이머 보강—가만히 둬도 발화) 후 B 양옆에 앞/뒤 영역 오버레이(`zone-wing` 등장 애니메이션, A 중심으로 활성 zone 하이라이트). `onNodeDragStop`에서 활성 zone이면 `handleZoneDrop`→`placeBeside`(B 좌/우 인접+resolveCollision)+흐름 삽입, 아니면 기존 겹침 밀어내기 폴백. 기존 엣지 충돌 시 유지/삽입 되묻기 팝오버(ESC·pane클릭 취소). 위치 계산(`screenRectOf`)은 이벤트에서만 호출해 rect를 상태에 저장(렌더 중 ref 접근 회피). ③ i18n `dropzone.*` 5키(en/ko), `globals.css` `zone-wing-in` 키프레임. **그룹 zone은 dead UI 회피 위해 Phase 1에선 미표시 — Phase 2(풀스택 groups)에서 추가** (스펙의 "자리만"에서 조정). 검증: tsc/eslint/build green + 순수함수 bun 실행. 인터랙션 시각·체감은 로컬(Windows) 확인 필요.
- 드래그-오버 영역 선택(앞/뒤/그룹) 설계 확정 → `docs/superpowers/specs/2026-06-13-drag-drop-zones-design.md`. 노드 A를 B 위에 dwell(300ms)하면 B가 확장되며 좌=앞/중앙=그룹/우=뒤 zone 표시, 드롭 위치로 동작 결정. 앞/뒤=흐름(엣지) 삽입(기존 엣지 충돌 시 유지/삽입 되묻기), 그룹=부서/담당자 업무 묶음(보이는 배경 사각형 박스, 멤버 bbox 자동산정·절대좌표 유지). 2단계 분할: Phase 1 프론트(인터랙션+앞/뒤 흐름삽입, canvas.ts 순수함수), Phase 2 풀스택(groups 테이블+nodes.group_id, 그룹 박스 렌더·멤버십). 기존 resolveCollision은 zone 밖 겹침 폴백으로 유지.
- Whimsical 선별+크롬 디자인 구현 완료 (브랜치 `feat/whimsical-design`). ① `globals.css` 토큰 — accent #0066cc→#6A41FF(바이올렛, 전역 자동 반영), accent-tint/canvas/canvas-dot/border-strong/shadow-sm·md·lg 추가, ink #16161D·hairline #E6E6EA 조정. ② `process-node.tsx` — 흰 배경+테두리 → 파스텔 fill(`color-mix(in srgb,{color} 18%,white)`, 데이터 모델 무변경)+1.5px 채도 stroke+hover shadow-sm, DEFAULT_COLORS Whimsical 8톤. ③ 엣지 — `EDGE_DEFAULTS`(smoothstep elbow + animated 점선 + ArrowClosed)를 toAppEdges·onConnect·completeConnect 모두에 적용(로딩/생성 일관), 선택 시 accent로 globals.css에서 전환. ④ 캔버스 — `bg-canvas` + `<Background variant=Dots gap=20 size=1.2>`, 에디터 캔버스 영역 bg-surface-alt→bg-canvas. ⑤ 겹침 방지 — canvas.ts 신규 `resolveCollision`(MTV 반복 분리+8px 스냅, 타입별 박스), `snapToGrid={[8,8]}` + `onNodeDragStop`에서 호출(readOnly 가드). ⑥ 크롬 — accent 토큰 자동 반영 + 드릴 창 shadow→shadow-md(플로팅 elevation), 컨텍스트 메뉴 shadow-lg는 토큰 재정의로 쿨톤화. 비교 화면도 elbow+화살표+dot-grid 적용(diff dash 의미 보존 위해 animated 미적용). `rules/frontend/design.md` 개정(액센트=바이올렛, flat→노드/엣지/플로팅 한정 shadow·파스텔·dot-grid 허용). Pretendard·타입스케일·radius·easing 유지. 검증: tsc/eslint/`next build` 모두 green. 시각 확인은 로컬(Windows) 실행 필요.
- Whimsical 선별+크롬 디자인 설계 확정 → `docs/superpowers/specs/2026-06-13-whimsical-design-design.md` (브랜치 `feat/whimsical-design`). 범위: 단일 바이올렛 액센트(#6A41FF)·파스텔 노드(color-mix 파생, 데이터 무변경)·dot-grid 캔버스·움직이는 점선+화살표 엣지·드래그 겹침 방지(resolveCollision)·플로팅 크롬, Pretendard 유지. `rules/frontend/design.md`의 "flat only" 원칙은 노드/엣지/크롬 한정 soft shadow·파스텔·dot-grid 허용으로 개정. 변경: globals.css 토큰, process-node, canvas.ts, 에디터/비교 page, design.md.
- 노드 인터랙션 개편 구현 완료 (브랜치 `feat/node-interactions`, 서브에이전트 주도 4태스크 + 통합리뷰). ① 새 창을 **마우스 위치에서 확장**(신규 `window-open` 키프레임 scale 0.4→1 transform-origin center, ScopeWindow 마운트 시 재생), `handleDrillIn(node,x,y)`가 컨테이너 상대 좌표로 새 창 중앙=마우스 배치(저장된 창은 위치 유지, `windowGeomRef`로 드래그 시 리렌더 churn 방지). ② 드릴인 트리거를 더블클릭→**노드 호버 우상단 아이콘 버튼**(신규 `NodeActionsContext` 기본값 no-op로 노드→page 통신 — Provider 없는 compare 페이지 안전, `onDrill` 있을 때만 버튼). ③ **더블클릭=연결 모드**(노드 A 더블클릭→`connectSource` 무장+소스 링+상단 배너, 다른 노드 클릭→A→B 엣지 one-shot 생성, Esc/빈곳/포커스전환 취소). 기존 핸들 드래그 연결·박스선택·undo/autosave·창 이동/리사이즈/영속은 불변(ReactFlow는 onNodeClick/DoubleClick/PaneClick만 변경). 검증: 태스크별 tsc/lint/build green, 스펙+품질 2단 리뷰 + 통합리뷰 "ready to merge"(perf churn 보강). 시각·인터랙션은 원격이라 사용자 수동 검증 필요.
- 노드 인터랙션 개편 설계 확정 → `docs/superpowers/specs/2026-06-13-node-interactions-design.md`. ① 새 창을 마우스 위치에서 확장 애니메이션(창 중앙=마우스, `window-open` 키프레임 transform-origin center), 새 창만 마우스 중앙·저장된 창은 위치 유지. ② 드릴인 트리거를 더블클릭→노드 호버 우상단 아이콘 버튼(신규 `NodeActionsContext`로 노드→page 통신). ③ 더블클릭=연결 모드(노드 A 더블클릭 후 다음 클릭 노드 B로 A→B 엣지, 한 번 후 종료, Esc/빈곳/포커스전환 취소, 소스 노드 ring-accent + 상단 배너). 기존 핸들 드래그 연결·이동/리사이즈·영속은 그대로. 순수 프론트엔드.

## 2026-06-12
- OS형 자유 창(드릴인 윈도우) 구현 완료 (브랜치 `feat/os-windows`, 서브에이전트 주도 3태스크 + 최종 통합리뷰 보강). 드릴인 계층을 OS 윈도우처럼: 신규 `ScopeWindow`(타이틀바 드래그 이동·우하단 코너 리사이즈·최소/최대/닫기·포커스 z-order·비활성 오버레이, 외부 라이브러리 없이 pointerCapture 직접 구현, pointercancel 보강), 신규 `window-store.ts`(기하 `localStorage` 영속 `bpm.windows.<mapId>`). page.tsx: 기존 `scopes` 체인·로드/저장 유지 + `activeIndex`(포커스=라이브)·`windowGeom`·`zOrder`·`bounds`(ResizeObserver) 상태, `currentParentId=scopes[activeIndex]`, 네비 의미 변경(드릴=activeIndex까지 truncate+push, 창 클릭=focusScope, 닫기=하위까지 정리·root 닫기불가, 브레드크럼=포커스), 디바운스 저장, bounds 변동 시 화면밖 창 clamp(다른 해상도 복구). 활성 창만 라이브 ReactFlow(27 props 전량 보존), 비활성은 "클릭해 편집" 오버레이. 검증: 태스크별 tsc/lint/build green, 스펙+품질 2단 리뷰 + 전체 통합리뷰 통과(화면밖 unreachable 버그 보강). 동시편집(MDI) 아님. 시각·인터랙션(드래그/리사이즈/포커스/최소·최대·닫기/영속)은 원격이라 사용자 수동 검증 필요.
- OS형 자유 창(드릴인 윈도우) 설계 확정 → `docs/superpowers/specs/2026-06-12-os-windows-design.md`. 활성 1개만 라이브(ReactFlow)·나머지는 이동가능 비활성 창(클릭=포커스→그 계층 로드), 신규 `ScopeWindow` 컴포넌트(타이틀바 드래그 이동·코너 리사이즈·최소/최대/닫기·z-order, 의존성 없이 pointer 직접 구현), 창 기하 `localStorage` 영속(`bpm.windows.<mapId>`), 진입 시 계단식 자동배치 후 자유 이동. 기존 `scopes` 체인·로드/저장 로직 유지 + `activeIndex`/`windowGeom` 상태 추가. 순수 프론트엔드, 동시편집(MDI) 아님.
- UI 디자인 시스템 도입 구현 완료 (브랜치 `feat/ui-design-system`, 서브에이전트 주도 8태스크 + 최종 통합리뷰 보강). 참고 `claude_design_template`의 Apple 파생 토큰을 이식. ① 디자인 룰 `rules/frontend/design.md`(+CLAUDE.md import). ② `globals.css` Tailwind4 `@theme` 토큰(accent #0066cc·surface·ink·hairline·divider·error + diff added/removed/changed, 타입스케일 tagline~fine, radius, easing), Pretendard woff2 번들·`@font-face`, 다크모드·Geist 제거. ③ lucide-react 도입, 이모지(🔒📝⚡👤💬↶↷▾✓) 전량 Lucide 16/14/12px·1.5로 교체. ④ 전 화면(layout·top-nav·home·editor·compare·process-node·comment-section·context-menu·providers) flat+hairline+surface 토큰화 — chrome shadow 제거(컨텍스트 메뉴 오버레이만 유지), 노드 평면화, 계단창 hairline/surface 색단계, compare diff 색 토큰화, 노드 타입색 var(--color-*). 검증: 태스크별 tsc/lint/build green, 각 스펙+품질 2단 리뷰 통과. chrome raw hex 0(데이터 예외 COLOR_PRESETS·swatch·PNG bg만), 이모지 0. 순수 스타일/토큰/아이콘 — 기능·ReactFlow props·t() 키 불변. 잔여 minor: 일부 raw `text-xs/text-sm`은 `text-fine`(line-height:1) 적용 시 다행 노드 라벨 cramp 위험으로 보류(색 토큰은 전량 적용). 시각 동작은 원격이라 빌드 통과 후 사용자 수동 검증 필요(Pretendard 렌더, flat 룩, 아이콘, accent).
- UI 디자인 시스템 도입 설계 확정 → `docs/superpowers/specs/2026-06-12-ui-design-system-design.md`. 참고 `Nocharm/claude_design_template`의 Apple 파생 토큰 시스템을 BPM에 이식: ①디자인 룰 `rules/frontend/design.md`(raw hex 금지·flat elevation·타입/굵기 사다리·Lucide), ②Tailwind4 `@theme` 토큰 인프라(accent/surface/ink/hairline/diff + radius/easing/duration/타입스케일, 다크모드 제거), ③Pretendard 번들, ④전 화면 리스타일(이모지→Lucide, chrome shadow 제거→hairline/surface 단계, 밀도 유지). 의존성 lucide-react 추가. 순수 프론트엔드·기능 로직 불변.
- 드릴인 계단식 창 재구현 — 1차 구현은 활성 캔버스가 `h-full bg-white`로 영역 전체를 덮어 조상 프레임이 안 보였음(계단 미표시). 각 계층을 우하향 오프셋(레벨당 32px) **윈도우**로 쌓고 inset(`top/left=index*step`, `right/bottom=(depth-index)*step`)으로 같은 크기 창을 계단 배치, **활성(최하위) 창만 라이브 ReactFlow**·조상 창은 타이틀바만 노출(클릭 시 해당 깊이 복귀), 활성 창은 `key=currentParentId`+`drill-canvas`로 좌상단 origin 확장 애니메이션. 검증: tsc/lint/build green. 시각 동작은 원격이라 사용자 수동 확인 필요.
- UI 개선 3종 구현 완료 (브랜치 `feat/ui-improvements`, 서브에이전트 주도 8태스크). ① **계단식 창 드릴인** — 활성 ReactFlow 캔버스 1개 + 조상 스코프를 우하향 오프셋 장식 프레임(클릭 시 상위 복귀)으로 표시, `key={currentParentId}` remount로 좌상단 origin 확장 애니메이션(globals.css `drill-in-open`, `prefers-reduced-motion` 가드) 재생, 기존 정적 2겹 카드 대체. ② **전역 네비바 + 앱 전체 i18n** — 경량 자체 i18n(`lib/i18n.tsx` LangProvider/useI18n/t, 의존성 0, 기본 영어, localStorage 영속, en 권위·ko `Record<MessageKey>`로 키 누락 tsc 강제), 전역 `top-nav.tsx`(브랜드·유저칩·EN/KO 토글), 유저는 AuthGate가 `lib/current-user.ts` 모듈 스토어에 발행→`useSyncExternalStore` 구독(로컬=Guest). home/editor/compare/process-node/comment-section/context-menu/AuthGate 전 문자열 `t()` 교체, `diff.ts`는 한국어 라벨→`ChangedField` 키 emit·compare가 번역, `canvas.ts` NODE_TYPE_OPTIONS→`labelKey`. ③ **단축키** — `selectionOnDrag`(좌-드래그 박스 다중선택)·`panActivationKeyCode="Space"`(스페이스+드래그 팬)·`panOnDrag={[1]}`(휠클릭 팬), Undo/Redo는 기존 구현이 이미 preventDefault 적용돼 검증만. 순수 프론트엔드(백엔드 변경 0). 검증: 태스크별 tsc/lint + 마일스톤 build 전부 green, 각 태스크 스펙+품질 2단 리뷰 통과, 전체 브랜치 통합 리뷰 "ready to merge". 잔여 한국어는 의도된 것만(코드 주석, 정적 metadata description, 맵 로드 시 덮어쓰는 `"홈"` 플레이스홀더, 한글 파일명/초성 정규식). UI 상호작용(드릴인 애니메이션·토글·박스선택·스페이스 팬)은 원격이라 빌드로 1차 확인 후 사용자 수동 검증 필요.
- UI 개선 3종 설계 확정 → `docs/superpowers/specs/2026-06-12-ui-improvements-design.md`. ① 드릴인 계단식 창 스택(활성 캔버스 1개+조상 장식 프레임, 좌상단 origin 확장 애니메이션), ② 전역 네비바+경량 자체 i18n(영어 기본, 한/영 토글, 유저 칩—로컬은 Guest, 앱 전체 번역), ③ 단축키(좌-드래그 박스선택·스페이스+드래그 팬, Undo/Redo 기존 구현 검증). 순수 프론트엔드, 백엔드 변경 없음.
- 문서 명령어 bash/PowerShell 병기 — 로컬 검증이 Windows PC라 README·CLAUDE.md의 로컬 실행 명령에 PowerShell 블록 추가(venv는 `.venv\Scripts\`), 서버(리눅스) 전용 절차는 bash 유지. 병기 원칙을 `rules/common/documentation.md`에 명문화 (이후 문서에도 적용).
- 기능 확장 Phase C 완료 — 협업(체크아웃+코멘트). backend: map_versions에 checked_out_by/at, app/checkout.py(TTL 판정 — sqlite naive/postgres aware datetime 통일), POST·DELETE /versions/{id}/checkout(같은 사용자 재호출=heartbeat 연장, force=잠금 인수, 타인 잠금이면 mine=false 반환), 잠금 중 graph PUT·버전 삭제는 423 거부. comments 테이블+라우터(작성/조회/해결 토글/작성자만 삭제 403, 노드 삭제 시 코멘트 명시 정리 — sqlite FK pragma 비활성 대비). CHECKOUT_TTL_MINUTES(기본 30분)는 Settings+.env.example+compose 반영. 테스트 12개 추가(42 passed — 멀티유저는 dev_user monkeypatch로 재현), ruff clean. frontend: 버전 진입 시 자동 체크아웃, 10초 heartbeat(선점 해제 시 자동 승격), 이탈 시 해제(실패는 TTL 보증), 타인 편집 중이면 읽기 전용 — 앰버 배너+강제 편집 버튼, 편집 경로 전부 가드(저장/자동저장/연결/추가/정렬/속성/삭제/메뉴), 조회·검색·드릴다운·코멘트는 허용. 코멘트 — 5초 폴링, 노드 💬 미해결 뱃지(렌더 시 주입, effect setState 회피), 사이드패널 코멘트 섹션(Ctrl+Enter 전송, 해결/재열기/삭제). 검증: tsc/eslint/build, API E2E(체크아웃 획득·heartbeat·해제, 코멘트 CRUD). 리뷰어 검수 통과(지적 없음). 참고: 로컬 AUTH 우회 모드는 전원이 dev_user라 잠금 충돌이 발생하지 않음 — 실효성은 서버(실사용자) 환경에서 확인.
- 기능 확장 Phase B 완료 — 데이터·조회. backend: nodes에 BPM 속성 4종(assignee/department/system/duration) + source_node_id(복제 계보 루트, 복제의 복제도 원본 유지 `or node.id` 전파, 클라이언트 위조 방지 위해 NodeIn 미포함) 컬럼, GET /versions/{id}/graph/all(전 계층 노드+엣지, 검색·diff용), upsert·클론에 신규 필드 반영. 테스트 3개 추가(30 passed, ruff clean). frontend: ① 사이드패널 BPM 속성 접이식 섹션 + 노드에 담당자 뱃지. ② 비교 화면 diff — lib/diff.ts(계보 우선 매칭→(부모계보,제목) fallback, 변경 필드 한국어 라벨, 위치(계층) 이동 감지, 하위 변경 ⚡ 뱃지, 엣지 추가/삭제), 추가=초록/삭제=빨강/변경=노랑 링, 범례+하단 변경 목록, 호버 툴팁. ③ 노드 검색 — lib/hangul.ts 초성 매칭(유니코드 분해), 헤더 검색창(Ctrl+K, ↑↓ Enter), 결과 경로 표시, 선택 시 스코프 점프+선택+fitView 센터링. ④ PNG 내보내기 — lib/export.ts(html-to-image, React Flow 공식 방식, 전체 노드 bounds, 4096px 상한), 파일명 맵_버전_타임스탬프. 검증: tsc/eslint/build, API E2E(전계층/속성/계보), diff·초성 순수 함수 bun 실행 검증. 리뷰어 검수 조건부 통과 — SHOULD 2건 미채택 사유: graph/all 페이지네이션은 YAGNI(현업 맵 규모), orphan 경로 마커는 백엔드 서브트리 연쇄 삭제로 발생 불가. 검색은 서버 저장본 기준(자동저장 2초 내 편집은 미반영) — 수용.
- 기능 확장 Phase A 완료 — 캔버스 편집 경험. backend: nodes.color 컬럼(hex, 빈 값=타입 기본색, 패턴 검증) + node_type 기본값 process, graph upsert·버전 복제에 color 반영, 테스트 2개 추가(27 passed, ruff clean). frontend: ① undo/redo — 스코프 단위 히스토리 스택(50개 상한), 드래그=1스냅샷·타이핑 2초 묶음, Ctrl+Z/Shift+Z/Y + 툴바 버튼, 스코프/버전 전환 시 리셋(디자이너 안은 스코프별 보존이었으나 서버 재로드와의 정합성 위해 단순화). ② 마우스 위치 컨텍스트 메뉴 — 캔버스/노드/엣지 우클릭, 커서 위치 노드 생성(screenToFlowPosition, 노드 중심 배치), 가장자리 잘림 보정, ESC/외부클릭/팬 시 닫힘(신규 components/context-menu.tsx). 디자이너 안의 '붙여넣기(비활성)'·'편집' 메뉴는 죽은 UI라 제외(우클릭 즉시 패널 열림). ③ 자동저장(2초 디바운스)+저장 상태 표시+beforeunload 이탈 경고, 수동 저장 유지. ④ 노드 타입 4종 모양(사각/마름모/알약)·색 프리셋 8+hex 입력(사이드패널), 엣지 선택·우클릭으로 라벨 편집. 검증: tsc/eslint/build 통과, API E2E(색·타입 라운드트립, 라벨, 422, 복제 색 계승) 확인. 리뷰어 페르소나 검수 통과(SHOULD 1건 반영). UI 인터랙션(메뉴 위치·undo 체감)은 브라우저 수동 확인 필요.
- 기능 확장 명세 확정 → `docs/spec.md` §7 추가: Phase A(undo/redo, 마우스 위치 컨텍스트 메뉴, 자동저장+이탈경고, 노드 색·모양/엣지 라벨), Phase B(노드 BPM 속성 4종, 복제 계보 기반 버전 diff 하이라이트, 초성 검색, PNG 내보내기), Phase C(버전 체크아웃 잠금, 노드 코멘트 5초 폴링). §2 데이터 모델·§6 구현 순서 갱신 — 배포(⑥→⑦)는 기능 확장 후 진행.

## 2026-06-11
- `/init`: CLAUDE.md에 Project/Architecture 섹션 추가. 프로세스맵 그리기 웹 서비스 — Next.js(front) / Python(back) / PostgreSQL / nginx 4컨테이너 모노레포 구성 명시. hub @import 구조는 유지.
- 운영 배경 반영: `.gitattributes`로 LF 고정(Windows 경유 CRLF 방지). CLAUDE.md에 Operations/Deployment 섹션 추가 — 파이프라인(GitHub→Windows 로컬 네이티브→서버 Docker), 제약(LF/로컬 Docker 없음/앱 nginx 443·80 미점유/env 분리). 서버 엣지 nginx 443·80 점유 가정 반영.
- ⑥ 배포 준비: `docker compose config` 정적 검증 통과(변수 보간·9787 게시·build args 확인). 배포 런북 `docs/deploy.md` 작성(Keycloak public 클라이언트 등록, .env, up, 헬스체크, 트러블슈팅, 롤백), README에서 링크. 실제 이미지 빌드/기동은 서버에서(샌드박스 Docker Hub rate limit).
- ⑤ Keycloak 인증 연동 완료. backend: app/auth.py — get_current_user 의존성(AUTH_ENABLED=False면 dev_user 우회, True면 realm JWKS로 RS256 검증, pyjwt[crypto]). maps/versions/graph 라우터에 router-level 의존성 적용(FastAPI 요청 캐시로 1회 검증), create_map은 created_by=user. health는 면제. 테스트 25개(auth 3 추가: 우회/401/health면제) 통과. frontend: react-oidc-context+oidc-client-ts, Providers(NEXT_PUBLIC_AUTH_ENABLED 게이트, useSyncExternalStore로 SSR-safe 마운트), AuthGate(미인증 시 signinRedirect, 토큰을 api setAuthToken으로 동기화), api request에 Bearer 첨부. compose/Dockerfile: AUTH_ENABLED·KEYCLOAK_ISSUER(backend env), NEXT_PUBLIC_*(frontend build args, 빌드타임 인라인). .env.example 인증 섹션. 검증: backend 25 passed, frontend tsc/eslint/build OK, E2E 우회모드 정상+AUTH_ENABLED 시 무토큰 401·health 200. 실제 Keycloak 토큰 검증은 서버에서 확인 필요(샌드박스에 Keycloak 없음).
- ④ 버전 관리 + 비교 완료. backend: versions 라우터 — POST /maps/{id}/versions(라벨, source_version_id 주면 깊은 복사: 노드/엣지 ID 재발급+계층 parent 재매핑, postgres 자기참조 FK 대비 2-pass 삽입), PATCH 이름변경, DELETE(마지막 버전은 409). 테스트 22개(versions 7 추가) 통과. frontend: api(createVersion/renameVersion/deleteVersion), 에디터 헤더에 버전 select+새버전(복제)/이름변경/삭제+비교 링크(버전 전환 시 현재 스코프 자동 저장), 신규 /maps/[id]/compare 페이지(두 버전 최상위 캔버스 읽기 전용 나란히). 검증: tsc/eslint/build OK, E2E로 복제(새 ID)·비교 200·마지막 삭제 409 확인.
- 프로젝트명 BPM(Business Process Management)으로 확정 — README/CLAUDE/spec 제목, layout metadata, FastAPI title, 목록 헤더 반영.
- ③ 계층 + 정렬 완료. backend: graph 라우터를 (version, parent) 스코프로 재설계 — GET/PUT에 `parent` 쿼리, PUT은 해당 스코프만 교체(upsert로 유지 노드 보존, 제거 노드는 서브트리 재귀 삭제, 형제 엣지 스코프 교체), GraphOut에 `has_children` 계산 필드. 테스트 16개(계층 격리/서브트리 삭제/unknown parent 404 추가) 통과, ruff clean. frontend: 커스텀 ProcessNode(좌우 핸들+하위 표시), canvas 헬퍼(dagre 자동 레이아웃, 선택 정렬/등간격), 에디터에 스코프 스택+브레드크럼(더블클릭 드릴다운, 이동 시 현재 스코프 자동 저장), 정렬 툴바, 깊이 카드 시각화. @dagrejs/dagre 추가. 검증: tsc/eslint/build OK, E2E로 드릴다운 스코프 격리·has_children 확인.
- ② 맵 CRUD + 캔버스 편집/저장 완료. backend: SQLAlchemy 2.0 async 모델(process_maps/map_versions/nodes/edges, 노드 PK=클라이언트 생성 string, parent_node_id 자기참조 FK는 계층 대비 미사용), maps CRUD 라우터(생성 시 As-Is 버전 자동), graph 라우터(GET/PUT 전체 교체+엣지 노드참조 검증), lifespan create_all. DB는 로컬 sqlite/서버 postgres(settings 기본 sqlite). 테스트 13개(maps 7 + graph 5 + health) 통과, ruff clean. frontend: api 클라이언트, 맵 목록 페이지(생성/삭제), [mapId] 에디터(React Flow — 노드 추가/이동/연결/삭제, 사이드패널 제목·설명 편집, 저장). 검증: tsc/eslint/build OK, backend+next dev E2E — 프록시 통한 맵 생성·에디터 200·그래프 저장/재로드 확인.
- 스캐폴딩(spec §6-①) 완료: frontend(Next 16 + @xyflow/react, standalone 출력, dev rewrites로 /api→backend 프록시), backend(FastAPI + pydantic-settings, /api/health + pytest), nginx(default.conf — /api→backend, /→frontend), docker-compose(9787 노출, Postgres 16 + healthcheck), .env.example, README 교체, CLAUDE.md Commands 채움. 검증: backend pytest·ruff 통과, uvicorn /api/health OK, frontend 프로덕션 빌드 OK, dev 서버에서 /api 프록시로 backend 응답 확인. compose 빌드는 샌드박스 Docker Hub rate limit으로 미검증 — 서버 배포 시 확인 필요. dev 의존성 httpx→httpx2 교체(starlette TestClient deprecation).
- 기능 명세 확정 → `docs/spec.md` 작성: 계층형 프로세스맵(선후=Edge, 상하=parent_node_id 재귀), 계층 UX는 오버레이 스택+브레드크럼(MDI 자유창 대신 — 사용자 제안 다듬음), As-Is/To-Be는 버전 관리+비교 화면, 정렬(자동 레이아웃+선택 정렬), Keycloak(ai-portal) 인증, 노출 포트 9787. CLAUDE.md Architecture를 확정 스택(FastAPI, @xyflow/react, 9787, Keycloak)으로 갱신.
