# Progress

프로젝트 진행 현황 로그. 커밋 직전 갱신한다 (`rules/common/git.md` 규칙).

## 2026-06-12
- UI 개선 3종 구현 완료 (브랜치 `feat/ui-improvements`, 서브에이전트 주도 8태스크). ① **계단식 창 드릴인** — 활성 ReactFlow 캔버스 1개 + 조상 스코프를 우하향 오프셋 장식 프레임(클릭 시 상위 복귀)으로 표시, `key={currentParentId}` remount로 좌상단 origin 확장 애니메이션(globals.css `drill-in-open`, `prefers-reduced-motion` 가드) 재생, 기존 정적 2겹 카드 대체. ② **전역 네비바 + 앱 전체 i18n** — 경량 자체 i18n(`lib/i18n.tsx` LangProvider/useI18n/t, 의존성 0, 기본 영어, localStorage 영속, en 권위·ko `Record<MessageKey>`로 키 누락 tsc 강제), 전역 `top-nav.tsx`(브랜드·유저칩·EN/KO 토글), 유저는 AuthGate가 `lib/current-user.ts` 모듈 스토어에 발행→`useSyncExternalStore` 구독(로컬=Guest). home/editor/compare/process-node/comment-section/context-menu/AuthGate 전 문자열 `t()` 교체, `diff.ts`는 한국어 라벨→`ChangedField` 키 emit·compare가 번역, `canvas.ts` NODE_TYPE_OPTIONS→`labelKey`. ③ **단축키** — `selectionOnDrag`(좌-드래그 박스 다중선택)·`panActivationKeyCode="Space"`(스페이스+드래그 팬)·`panOnDrag={[1]}`(휠클릭 팬), Undo/Redo는 기존 구현이 이미 preventDefault 적용돼 검증만. 순수 프론트엔드(백엔드 변경 0). 검증: 태스크별 tsc/lint + 마일스톤 build 전부 green, 각 태스크 스펙+품질 2단 리뷰 통과, 전체 브랜치 통합 리뷰 "ready to merge". 잔여 한국어는 의도된 것만(코드 주석, 정적 metadata description, 맵 로드 시 덮어쓰는 `"홈"` 플레이스홀더, 한글 파일명/초성 정규식). UI 상호작용(드릴인 애니메이션·토글·박스선택·스페이스 팬)은 원격이라 빌드로 1차 확인 후 사용자 수동 검증 필요.
- UI 개선 3종 설계 확정 → `docs/superpowers/specs/2026-06-12-ui-improvements-design.md`. ① 드릴인 계단식 창 스택(활성 캔버스 1개+조상 장식 프레임, 좌상단 origin 확장 애니메이션), ② 전역 네비바+경량 자체 i18n(영어 기본, 한/영 토글, 유저 칩—로컬은 Guest, 앱 전체 번역), ③ 단축키(좌-드래그 박스선택·스페이스+드래그 팬, Undo/Redo 기존 구현 검증). 순수 프론트엔드, 백엔드 변경 없음.
- 문서 명령어 bash/PowerShell 병기 — 로컬 검증이 Windows PC라 README·CLAUDE.md의 로컬 실행 명령에 PowerShell 블록 추가(venv는 `.venv\Scripts\`), 서버(리눅스) 전용 절차는 bash 유지. 병기 원칙을 `rules/common/documentation.md`에 명문화 (이후 문서에도 적용).
- 기능 확장 Phase C 완료 — 협업(체크아웃+코멘트). backend: map_versions에 checked_out_by/at, app/checkout.py(TTL 판정 — sqlite naive/postgres aware datetime 통일), POST·DELETE /versions/{id}/checkout(같은 사용자 재호출=heartbeat 연장, force=잠금 인수, 타인 잠금이면 mine=false 반환), 잠금 중 graph PUT·버전 삭제는 423 거부. comments 테이블+라우터(작성/조회/해결 토글/작성자만 삭제 403, 노드 삭제 시 코멘트 명시 정리 — sqlite FK pragma 비활성 대비). CHECKOUT_TTL_MINUTES(기본 30분)는 Settings+.env.example+compose 반영. 테스트 12개 추가(42 passed — 멀티유저는 dev_user monkeypatch로 재현), ruff clean. frontend: 버전 진입 시 자동 체크아웃, 10초 heartbeat(선점 해제 시 자동 승격), 이탈 시 해제(실패는 TTL 보증), 타인 편집 중이면 읽기 전용 — 앰버 배너+강제 편집 버튼, 편집 경로 전부 가드(저장/자동저장/연결/추가/정렬/속성/삭제/메뉴), 조회·검색·드릴다운·코멘트는 허용. 코멘트 — 5초 폴링, 노드 💬 미해결 뱃지(렌더 시 주입, effect setState 회피), 사이드패널 코멘트 섹션(Ctrl+Enter 전송, 해결/재열기/삭제). 검증: tsc/eslint/build, API E2E(체크아웃 획득·heartbeat·해제, 코멘트 CRUD). 리뷰어 검수 통과(지적 없음). 참고: 로컬 AUTH 우회 모드는 전원이 dev_user라 잠금 충돌이 발생하지 않음 — 실효성은 서버(실사용자) 환경에서 확인.
- 기능 확장 Phase B 완료 — 데이터·조회. backend: nodes에 BPM 속성 4종(assignee/department/system/duration) + source_node_id(복제 계보 루트, 복제의 복제도 원본 유지 `or node.id` 전파, 클라이언트 위조 방지 위해 NodeIn 미포함) 컬럼, GET /versions/{id}/graph/all(전 계층 노드+엣지, 검색·diff용), upsert·클론에 신규 필드 반영. 테스트 3개 추가(30 passed, ruff clean). frontend: ① 사이드패널 BPM 속성 접이식 섹션 + 노드에 담당자 뱃지. ② 비교 화면 diff — lib/diff.ts(계보 우선 매칭→(부모계보,제목) fallback, 변경 필드 한국어 라벨, 위치(계층) 이동 감지, 하위 변경 ⚡ 뱃지, 엣지 추가/삭제), 추가=초록/삭제=빨강/변경=노랑 링, 범례+하단 변경 목록, 호버 툴팁. ③ 노드 검색 — lib/hangul.ts 초성 매칭(유니코드 분해), 헤더 검색창(Ctrl+K, ↑↓ Enter), 결과 경로 표시, 선택 시 스코프 점프+선택+fitView 센터링. ④ PNG 내보내기 — lib/export.ts(html-to-image, React Flow 공식 방식, 전체 노드 bounds, 4096px 상한), 파일명 맵_버전_타임스탬프. 검증: tsc/eslint/build, API E2E(전계층/속성/계보), diff·초성 순수 함수 bun 실행 검증. 리뷰어 검수 조건부 통과 — SHOULD 2건 미채택 사유: graph/all 페이지네이션은 YAGNI(현업 맵 규모), orphan 경로 마커는 백엔드 서브트리 연쇄 삭제로 발생 불가. 검색은 서버 저장본 기준(자동저장 2초 내 편집은 미반영) — 수용.
- 기능 확장 Phase A 완료 — 캔버스 편집 경험. backend: nodes.color 컬럼(hex, 빈 값=타입 기본색, 패턴 검증) + node_type 기본값 process, graph upsert·버전 복제에 color 반영, 테스트 2개 추가(27 passed, ruff clean). frontend: ① undo/redo — 스코프 단위 히스토리 스택(50개 상한), 드래그=1스냅샷·타이핑 2초 묶음, Ctrl+Z/Shift+Z/Y + 툴바 버튼, 스코프/버전 전환 시 리셋(디자이너 안은 스코프별 보존이었으나 서버 재로드와의 정합성 위해 단순화). ② 마우스 위치 컨텍스트 메뉴 — 캔버스/노드/엣지 우클릭, 커서 위치 노드 생성(screenToFlowPosition, 노드 중심 배치), 가장자리 잘림 보정, ESC/외부클릭/팬 시 닫힘(신규 components/context-menu.tsx). 디자이너 안의 '붙여넣기(비활성)'·'편집' 메뉴는 죽은 UI라 제외(우클릭 즉시 패널 열림). ③ 자동저장(2초 디바운스)+저장 상태 표시+beforeunload 이탈 경고, 수동 저장 유지. ④ 노드 타입 4종 모양(사각/마름모/알약)·색 프리셋 8+hex 입력(사이드패널), 엣지 선택·우클릭으로 라벨 편집. 검증: tsc/eslint/build 통과, API E2E(색·타입 라운드트립, 라벨, 422, 복제 색 계승) 확인. 리뷰어 페르소나 검수 통과(SHOULD 1건 반영). UI 인터랙션(메뉴 위치·undo 체감)은 브라우저 수동 확인 필요.
- 기능 확장 명세 확정 → `docs/spec.md` §7 추가: Phase A(undo/redo, 마우스 위치 컨텍스트 메뉴, 자동저장+이탈경고, 노드 색·모양/엣지 라벨), Phase B(노드 BPM 속성 4종, 복제 계보 기반 버전 diff 하이라이트, 초성 검색, PNG 내보내기), Phase C(버전 체크아웃 잠금, 노드 코멘트 5초 폴링). §2 데이터 모델·§6 구현 순서 갱신 — 배포(⑥→⑦)는 기능 확장 후 진행.

## 2026-06-11
- `/init`: CLAUDE.md에 Project/Architecture 섹션 추가. 프로세스맵 그리기 웹 서비스 — Next.js(front) / Python(back) / PostgreSQL / nginx 4컨테이너 모노레포 구성 명시. hub @import 구조는 유지.
- 운영 배경 반영: `.gitattributes`로 LF 고정(Windows 경유 CRLF 방지). CLAUDE.md에 Operations/Deployment 섹션 추가 — 파이프라인(GitHub→Windows 로컬 네이티브→서버 Docker), 제약(LF/로컬 Docker 없음/앱 nginx 443·80 미점유/env 분리). 서버 엣지 nginx 443·80 점유 가정 반영.
- ⑥ 배포 준비: `docker compose config` 정적 검증 통과(변수 보간·9787 게시·build args 확인). 배포 런북 `docs/deploy.md` 작성(Keycloak public 클라이언트 등록, .env, up, 헬스체크, 트러블슈팅, 롤백), README에서 링크. 실제 이미지 빌드/기동은 서버에서(샌드박스 Docker Hub rate limit).
- ⑤ Keycloak 인증 연동 완료. backend: app/auth.py — get_current_user 의존성(AUTH_ENABLED=False면 dev_user 우회, True면 realm JWKS로 RS256 검증, pyjwt[crypto]). maps/versions/graph 라우터에 router-level 의존성 적용(FastAPI 요청 캐시로 1회 검증), create_map은 created_by=user. health는 면제. 테스트 25개(auth 3 추가: 우회/401/health면제) 통과. frontend: react-oidc-context+oidc-client-ts, Providers(NEXT_PUBLIC_AUTH_ENABLED 게이트, useSyncExternalStore로 SSR-safe 마운트), AuthGate(미인증 시 signinRedirect, 토큰을 api setAuthToken으로 동기화), api request에 Bearer 첨부. compose/Dockerfile: AUTH_ENABLED·KEYCLOAK_ISSUER(backend env), NEXT_PUBLIC_*(frontend build args, 빌드타임 인라인). .env.example 인증 섹션. 검증: backend 25 passed, frontend tsc/eslint/build OK, E2E 우회모드 정상+AUTH_ENABLED 시 무토큰 401·health 200. 실제 Keycloak 토큰 검증은 서버에서 확인 필요(샌드박스에 Keycloak 없음).
- ④ 버전 관리 + 비교 완료. backend: versions 라우터 — POST /maps/{id}/versions(라벨, source_version_id 주면 깊은 복사: 노드/엣지 ID 재발급+계층 parent 재매핑, postgres 자기참조 FK 대비 2-pass 삽입), PATCH 이름변경, DELETE(마지막 버전은 409). 테스트 22개(versions 7 추가) 통과. frontend: api(createVersion/renameVersion/deleteVersion), 에디터 헤더에 버전 select+새버전(복제)/이름변경/삭제+비교 링크(버전 전환 시 현재 스코프 자동 저장), 신규 /maps/[id]/compare 페이지(두 버전 최상위 캔버스 읽기 전용 나란히). 검증: tsc/eslint/build OK, E2E로 복제(새 ID)·비교 200·마지막 삭제 409 확인.
- 프로젝트명 BPM(Business Process Management)으로 확정 — README/CLAUDE/spec 제목, layout metadata, FastAPI title, 목록 헤더 반영.
- ③ 계층 + 정렬 완료. backend: graph 라우터를 (version, parent) 스코프로 재설계 — GET/PUT에 `parent` 쿼리, PUT은 해당 스코프만 교체(upsert로 유지 노드 보존, 제거 노드는 서브트리 재귀 삭제, 형제 엣지 스코프 교체), GraphOut에 `has_children` 계산 필드. 테스트 16개(계층 격리/서브트리 삭제/unknown parent 404 추가) 통과, ruff clean. frontend: 커스텀 ProcessNode(좌우 핸들+하위 표시), canvas 헬퍼(dagre 자동 레이아웃, 선택 정렬/등간격), 에디터에 스코프 스택+브레드크럼(더블클릭 드릴다운, 이동 시 현재 스코프 자동 저장), 정렬 툴바, 깊이 카드 시각화. @dagrejs/dagre 추가. 검증: tsc/eslint/build OK, E2E로 드릴다운 스코프 격리·has_children 확인.
- ② 맵 CRUD + 캔버스 편집/저장 완료. backend: SQLAlchemy 2.0 async 모델(process_maps/map_versions/nodes/edges, 노드 PK=클라이언트 생성 string, parent_node_id 자기참조 FK는 계층 대비 미사용), maps CRUD 라우터(생성 시 As-Is 버전 자동), graph 라우터(GET/PUT 전체 교체+엣지 노드참조 검증), lifespan create_all. DB는 로컬 sqlite/서버 postgres(settings 기본 sqlite). 테스트 13개(maps 7 + graph 5 + health) 통과, ruff clean. frontend: api 클라이언트, 맵 목록 페이지(생성/삭제), [mapId] 에디터(React Flow — 노드 추가/이동/연결/삭제, 사이드패널 제목·설명 편집, 저장). 검증: tsc/eslint/build OK, backend+next dev E2E — 프록시 통한 맵 생성·에디터 200·그래프 저장/재로드 확인.
- 스캐폴딩(spec §6-①) 완료: frontend(Next 16 + @xyflow/react, standalone 출력, dev rewrites로 /api→backend 프록시), backend(FastAPI + pydantic-settings, /api/health + pytest), nginx(default.conf — /api→backend, /→frontend), docker-compose(9787 노출, Postgres 16 + healthcheck), .env.example, README 교체, CLAUDE.md Commands 채움. 검증: backend pytest·ruff 통과, uvicorn /api/health OK, frontend 프로덕션 빌드 OK, dev 서버에서 /api 프록시로 backend 응답 확인. compose 빌드는 샌드박스 Docker Hub rate limit으로 미검증 — 서버 배포 시 확인 필요. dev 의존성 httpx→httpx2 교체(starlette TestClient deprecation).
- 기능 명세 확정 → `docs/spec.md` 작성: 계층형 프로세스맵(선후=Edge, 상하=parent_node_id 재귀), 계층 UX는 오버레이 스택+브레드크럼(MDI 자유창 대신 — 사용자 제안 다듬음), As-Is/To-Be는 버전 관리+비교 화면, 정렬(자동 레이아웃+선택 정렬), Keycloak(ai-portal) 인증, 노출 포트 9787. CLAUDE.md Architecture를 확정 스택(FastAPI, @xyflow/react, 9787, Keycloak)으로 갱신.
